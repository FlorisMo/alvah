# Ranger van de Veluwe — Run Log (append-only)

One status block per run/step (BUILD-PLAN §9b).

---
**2026-06-22 20:23:38Z** · status

```
✔ landed: Phase 3 meta CORE — mission wrapper (lodge→briefing→play→fact→reward), breinkracht badges, knap-woord
▶ phase:  Phase 0–3 — spine, content, meta core
→ next:   Phase 3 remainder — companion + rehab loop; avatar creator UI + avatar state threaded through copy/voice; case-board screen
▷ progress: ~10%
```

---
**2026-06-23 00:49:03Z** · ticked: - [x] Phase 3 remainder · case-board screen — verhaalboog clues derived from completed verhaalHaak-missions (data gate) + hopeful ontknoping; prikbord overlay reachable from the lodge

```
✔ landed: Phase 3 remainder · case-board screen — verhaalboog clues derived from completed verhaalHaak-missions (data gate) + hopeful ontknoping; prikbord overlay reachable from the lodge
▶ phase:  Phase 0–3 — spine, content, meta core
→ next:   Phase 3 remainder · companion + rehab loop — companion state (rescue→care→grow→mee) + opvang release; CareRoutine care-skin feeds simon/dagnacht skill records; cabin/perch from the lodge
▷ progress: ~13%
```

---
**2026-06-23 00:55:30Z** · ticked: - [x] Phase 3 remainder · companion+rehab STATE — `core/companion.ts` pure model (soort/fase/bond/kunstjes/meeOpMissie + rehab) + state.ts mutators (rescue→bondDelta→grow→mee, startRehab/releaseRehab) + persistence/migration; seeded unit test

```
✔ landed: Phase 3 remainder · companion+rehab STATE — `core/companion.ts` pure model (soort/fase/bond/kunstjes/meeOpMissie + rehab) + state.ts mutators (rescue→bondDelta→grow→mee, startRehab/releaseRehab) + persistence/migration; seeded unit test
▶ phase:  Phase 0–3 — spine, content, meta core
→ next:   Phase 3 remainder · companion+rehab UI — CareRoutine care-skin feeds simon/dagnacht skill records (order=simon, over-handle=dagnacht); cabin/perch screen (rescue + daily care + opvang release) reachable from the lodge
▷ progress: ~14%
```

---
**2026-06-23 00:59:36Z** · ticked: - [x] Phase 3 remainder · companion+rehab UI — CareRoutine care-skin feeds simon/dagnacht skill records (order=simon, over-handle=dagnacht); cabin/perch screen (rescue + daily care + opvang release) reachable from the lodge

```
✔ landed: Phase 3 remainder · companion+rehab UI — CareRoutine care-skin feeds simon/dagnacht skill records (order=simon, over-handle=dagnacht); cabin/perch screen (rescue + daily care + opvang release) reachable from the lodge
▶ phase:  Phase 0–3 — spine, content, meta core
→ next:   Phase 3 remainder · avatar creator UI + avatar state threaded through copy/voice (naam/huid/haar/outfit/iris; "Alvah" default injected into briefing/fact/reward + narrator)
▷ progress: ~15%
```

---
**2026-06-23 01:07:05Z** · ticked: - [x] Phase 3 remainder · avatar creator UI + avatar state threaded through copy/voice (naam/huid/haar/outfit/iris; "Alvah" default injected into briefing/fact/reward + narrator)

```
✔ landed: Ranger rigged via Meshy API (proof); rigged GLB saved to assets-gen/animated/
▶ phase:  Asset pipeline
→ next:   Generate remaining ~43 shotlist renders (props incl. immersion + seasonal + story/vehicle; fox/ree/edelhert; remaining birds)
▷ progress: ~16%
```

---
**2026-06-23 01:10:51Z** · status

```
✔ landed: Ranger rigged via Meshy API (proof); rigged GLB saved to assets-gen/animated/
▶ phase:  Asset pipeline
→ next:   Generate remaining renders · batch A — gameplay-critical cast: immersion/tracking props + core birds + fox/ree/edelhert/heideblauwtje (shotlist items 1–23)
▷ progress: ~16%
```

---
**2026-06-23 08:39:01Z** · assets pass

```
✔ landed: Ranger rigged via Meshy API (proof); rigged GLB saved to assets-gen/animated/
▶ phase:  Asset pipeline
→ next:   Generate remaining renders · batch A — gameplay-critical cast: immersion/tracking props + core birds + fox/ree/edelhert/heideblauwtje (shotlist items 1–23)
▷ progress: ~16%
```

---
**2026-06-23 08:41:19Z** · ticked: - [x] Generate remaining renders · batch A — gameplay-critical cast: immersion/tracking props + core birds + fox/ree/edelhert/heideblauwtje (shotlist items 1–23)

```
✔ landed: Generate remaining renders · batch A — gameplay-critical cast: immersion/tracking props + core birds + fox/ree/edelhert/heideblauwtje (shotlist items 1–23)
▶ phase:  Asset pipeline
→ next:   Generate remaining renders · batch B — story + vehicles + seasonal variants (shotlist items 24–37: ecoduct/boa-post/bird-hide/nest-box/snare/raven-nest, jeep/helicopter, oak/birch/heather autumn+winter, snow-mound)
▷ progress: ~20%
```

---
**2026-06-23 08:41:35Z** · ticked: - [x] Generate remaining renders · batch B — story + vehicles + seasonal variants (shotlist items 24–37: ecoduct/boa-post/bird-hide/nest-box/snare/raven-nest, jeep/helicopter, oak/birch/heather autumn+winter, snow-mound)

```
✔ landed: Generate remaining renders · batch B — story + vehicles + seasonal variants (shotlist items 24–37: ecoduct/boa-post/bird-hide/nest-box/snare/raven-nest, jeep/helicopter, oak/birch/heather autumn+winter, snow-mound)
▶ phase:  Asset pipeline
→ next:   Generate remaining renders · batch C — finishing props + last birds (shotlist items 38–43: raaf-fledgling, anthill/foxglove/acorn-cluster, buizerd, wilde-eend)
▷ progress: ~23%
```

---
**2026-06-23 08:53:23Z** · assets pass

```
✔ landed: Generate remaining renders · batch B — story + vehicles + seasonal variants (shotlist items 24–37: ecoduct/boa-post/bird-hide/nest-box/snare/raven-nest, jeep/helicopter, oak/birch/heather autumn+winter, snow-mound)
▶ phase:  Asset pipeline
→ next:   Generate remaining renders · batch C — finishing props + last birds (shotlist items 38–43: raaf-fledgling, anthill/foxglove/acorn-cluster, buizerd, wilde-eend)
▷ progress: ~23%
```

---
**2026-06-23 08:55:20Z** · ticked: - [x] Generate remaining renders · batch C — finishing props + last birds (shotlist items 38–43: raaf-fledgling, anthill/foxglove/acorn-cluster, buizerd, wilde-eend)

```
✔ landed: Generate remaining renders · batch C — finishing props + last birds (shotlist items 38–43: raaf-fledgling, anthill/foxglove/acorn-cluster, buizerd, wilde-eend)
▶ phase:  Asset pipeline
→ next:   Rig the 2 remaining humanoids via Meshy API (warden, poacher)
▷ progress: ~24%
```

---
**2026-06-23 08:59:36Z** · ticked: - [x] Rig the 2 remaining humanoids via Meshy API (warden, poacher)

```
✔ landed: Rig the 2 remaining humanoids via Meshy API (warden, poacher)
▶ phase:  Asset pipeline
→ next:   Anything World pipeline — wire `ANYTHING_WORLD_API`; pre-flight the processing API; rig+animate ~9 animals + ~23 birds (~2 runs); prefer `assets-gen/animated/<id>.glb` over procedural
▷ progress: ~25%
```

---
**2026-06-23 09:07:25Z** · ticked: - [x] Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below
▷ progress: ~28%
```

---
**2026-06-23 09:15:11Z** · ticked: - [x] 29a · `render3d/ProceduralMotion.ts` — pure per-species gait recipes (breathe/hop/slither/flutter/paddle/still), `calmGate()` amplitude+speed clamp (never-scary §B / motion-comfort §C), `motionAt(recipe,t,phase)` deterministic delta; seeded unit test (bounds · slither-no-vertical · hop-arc-rests-never-frozen · still=identity)

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~28%
```

---
**2026-06-23 09:16:56Z** · ticked: - [x] 29b · wire into `World.ts` (replace ad-hoc idle bob with per-marker recipe by modelId; off under reduced-motion) + `Models.ts` "prefer animated GLB" hook (return clips; AnimationMixer when a rig exists, else procedural)

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~29%
```

---
**2026-06-23 09:18:22Z** · ticked: - [x] 29c · snake (adder) slither + frog (heikikker) hop + butterfly flutter verified against the §B AVOID lists; reduced-motion path checked

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~30%
```

---
**2026-06-23 09:18:22Z** · ticked: - [x] Procedural motion fallback for all animals; snake+frog procedural (slither/hop)

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~34%
```

---
**2026-06-23 09:24:52Z** · ticked: - [x] Optimize + stage every animated GLB; re-run finalize

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~35%
```

---
**2026-06-23 09:32:06Z** · ticked: - [x] 38a · `render3d/Biomes.ts` — pure biome field: `biomeAt(x,z)` → heide|bos|stuifzand|ven (noise-warped sectors radiating from the lodge clearing, each guaranteed present), `heightAt(x,z)` continuous global relief + a smooth ven basin (no cliffs at sector boundaries), per-biome palette (ground/accent/vegetation kind). Seeded unit test (determinism · all 4 biomes present · no-cliff continuity · ven basin lowest · central clearing = heide)

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~36%
```

---
**2026-06-23 09:32:06Z** · ticked: - [x] 38b · Wire Biomes into `World.ts` — vertex-coloured ground by `biomeAt`, `heightAt` replaces local `groundY`, biome-aware scatter (pines→bos, heather→heide, marram/grass→stuifzand, reeds + a calm water plane→ven); markers anchored in their mission's `landschap` biome; keep draw calls < 150

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~37%
```

---
**2026-06-23 09:35:53Z** · ticked: - [x] 38c · Character controller — kinematic capsule ground-follow + soft collision so the ranger can't walk through pines / into the ven / out of bounds (three-mesh-bvh or analytic clamp), tap-to-walk preserved, §1e camera untouched

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~38%
```

---
**2026-06-23 09:40:47Z** · ticked: - [x] 38d · Wayfinding + diegetic HUD — in-world marker labels + a calm direction/distance cue to the active mission (no minimap chrome), veldnotitie-style diegetic HUD; ≥56px, dual-channel, reduced-motion-safe

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~39%
```

---
**2026-06-23 09:42:58Z** · ticked: - [x] World build-out: THREE.Terrain biomes (heide/bos/stuifzand/ven), instanced vegetation/props, character controller (three-mesh-bvh), wayfinding + diegetic HUD

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~45%
```

---
**2026-06-23 09:47:14Z** · ticked: - [x] 45a · Harness contracts + `ViewMode` resolver (pure) — `render3d/play/ViewMode.ts` (`resolveViewMode(req)→'2d'|'3d'`: 3D only when scene live AND WebGL-capable AND engine has a green 3D variant AND not force-2D AND motion-ok-or-rmSafe; else the 2D floor) + `render3d/play/types.ts` (`WorldCtx`, the `Play3dFn` contract, the `Play3dEngine` registry shape). Seeded unit test (reduced-motion→2d unless rmSafe · no-WebGL→2d · engine-not-ready→2d · force2d→2d · all-go→3d · deterministic)

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~44%
```

---
**2026-06-23 09:50:09Z** · ticked: - [x] 45b · Shared 3D interaction kit `render3d/play/kit.ts` — pick3d (raycast + ≥56px projected hit-sphere sizing), highlight3d (emissive colour + scale pulse, static under reduced-motion), anchoredPrompt (reuse the accessible DOM card), spoorTrail (instanced clue meshes along a spline), reframe/cut (§1e damped move or hard cut). Pure geometry helpers (hit-sphere world-radius for a target px, spline sampling) carry a unit test; build-green

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~46%
```

---
**2026-06-23 09:58:23Z** · ticked: - [x] 45c · In-place mission flow — build `WorldCtx` from the live `World`, branch `runMission` once on `resolveViewMode` (3D plays in-place, world stays loaded, no `leaveWorld`; else today's 2D path), proven with one diegetic engine adapter; 2D fallback unchanged; build-green (per-engine parity tests land in the Phase-II variant boxes)

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~47%
```

---
**2026-06-23 10:08:41Z** · ticked: - [x] Diegetic variant: dagnacht (+ seeded parity test)

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~49%
```

---
**2026-06-23 10:15:31Z** · ticked: - [x] Diegetic variant: corsi (+ parity test)

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~51%
```

---
**2026-06-23 10:21:31Z** · ticked: - [x] 53a · Extract the pure `SimonRun` core into `engines/simon.ts` (growing-sequence sequencing + binary scoring, THREE/DOM/timing-free) + refactor the 2D `render2d/SimonView` to drive it; seeded parity test cross-checking the core against an independent re-impl of the frozen 2D rule

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~51%
```

---
**2026-06-23 10:24:03Z** · ticked: - [x] 53b · `render3d/engines/simon3d.ts` diegetic variant (callers staged as calm in-world forms; listen = per-animal glow+call, echo = raycast tap-back) + register + build gate + §9f audit

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~52%
```

---
**2026-06-23 10:24:03Z** · ticked: - [x] Diegetic variant: simon (+ parity test)

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~54%
```

---
**2026-06-23 10:30:19Z** · ticked: - [x] 57a · Construct-parity for zoeken — extract the frozen scoring rule `scoreZoeken(misses)` into `engines/zoeken.ts` (both 2D `ZoekenView` + 3D `zoeken3d` drive it = parity by construction) + add the SEPARATE tracking axis `buildSpoor(diff)` → `{legs,helderheid}` (new `spoorLengte`/`spoorHelderheid` knobs in Settings/DifficultyKnobs/knobsForLevel, scaled off the zoeken level but never touching the decoy set). Seeded parity test: builder trims decoys by difficulty + target stable + lensSterkte passthrough; spoor axes never change decoys/target (separate-axis invariant); scoreZoeken matches an independent reference; determinism

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~54%
```

---
**2026-06-23 10:33:40Z** · ticked: - [x] 57b · Tracking enrichment in `zoeken3d.ts` — `spoorTrail` clue legs from the activity spot to the hide zone (legs/faintness from `buildSpoor`; reduced-motion = static glowing markers, no walking) → the search trial UNCHANGED in a kijker (verrekijker) vignette → win card "vastgelegd op de wildcamera" (the case-board feed is the existing mission-completion data gate). Build gate + §9f audit

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~56%
```

---
**2026-06-23 10:33:43Z** · ticked: - [x] Diegetic variant: zoeken (+ parity test) + tracking enrichment (spoor→kijker→wildcamera→case-board)

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~59%
```

---
**2026-06-23 10:41:12Z** · ticked: - [x] 60a · Extract the pure `WisselRun` core into `engines/wisselen.ts` (set-shift sequencing + flip cadence + binary scoring, THREE/DOM/timing-free) + refactor the 2D `render2d/WisselView` to drive it; seeded parity test cross-checking the core against an independent re-impl of the frozen 2D rule

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~59%
```

---
**2026-06-23 10:44:32Z** · ticked: - [x] 60b · `render3d/engines/wisselen3d.ts` diegetic variant (current animal staged at the spot; the two destinations — open plek + het hol — stay visible+labelled = rule-application not spatial memory; flip cue = signpost colour + banner text) + register + build gate + §9f audit

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~60%
```

---
**2026-06-23 10:45:55Z** · run start

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~60%
```

---
**2026-06-23 10:48:20Z** · ticked: - [x] Diegetic variant: wisselen (+ parity test)

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~62%
```

---
**2026-06-23 10:52:59Z** · ticked: - [x] 64a · Continuous patrol return + diegetic veldnotitie mission entry — track from-world vs from-lodge launch; from the world the briefing reads as a lighter *veldnotitie* field-note (not a mission-control screen) and finishing a mission returns to the world (keep it loaded, re-point wayfinding to the next undone mission via `setActiveMission`, resume free-roam) instead of the lodge; lodge-launched flow unchanged. Pure `nextPatrolTarget(missions, voltooid)` selector + seeded unit test

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~61%
```

---
**2026-06-23 11:04:29Z** · ticked: - [x] 64b · Case-board wiring into the world — reach the prikbord from the explore HUD over the live world (no teardown) + a diegetic "vastgelegd op de wildcamera → prikbord" beat after a verhaalHaak mission completes in-world (the clue data gate already exists); returns to patrol

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~62%
```

---
**2026-06-23 11:12:51Z** · ticked: - [x] Immersion layer: veldnotitie entry, continuous patrol, case-board wiring, light world-EF seasoning

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~66%
```

---
**2026-06-23 11:14:13Z** · ticked: - [x] AUDIT (§9f): construct-parity all green, never-scary/motion-comfort/tone, <150 draw calls, screenshots

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~67%
```

---
**2026-06-23 11:24:21Z** · ticked: - [x] 3D play harness: `render3d/engines/`, `WorldCtx`, `ViewMode` resolver, shared kit (pick3d/highlight3d/anchoredPrompt/spoorTrail/reframe)

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~69%
```

---
**2026-06-23 11:24:21Z** · ticked: - [x] Eye shader (catchlight/clearcoat/parallax; per-species pupil; dusk eyeshine list)

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~71%
```

---
**2026-06-23 11:30:53Z** · ticked: - [x] 76a · Pure ARKit-subset face system `render3d/Face.ts` — the ~16-shape A.1 subset (symmetric L/R; deliberately-excluded noseSneer/mouthPress/jawForward) + the A.2 per-emotion recipe table as data (neutral/happy/surprised/curious/sad-gentle/proud/focused) with the uncanny-valley caps baked (general ≤0.7, eyeWide ≤0.35, browDown ≤0.2, Duchenne cheekSquint required on happy) + A.4 blink/saccade/transition timing + pure `expressionWeights`/`blinkEnvelope`/`fadeWeights`/`nextBlinkDelay` maths; seeded unit test

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~70%
```

---
**2026-06-23 11:32:50Z** · ticked: - [x] 76b · `render3d/FaceRig.ts` — THREE morph-target driver (apply ARKit-named influences best-effort to a humanoid's morph targets, eased expression fade, blink+microsaccade via onBeforeRender; a no-morph model is left untouched + never throws) + wire onto the ranger in World (humanoids ranger/warden/poacher); build gate

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~70%
```

---
**2026-06-23 11:37:48Z** · ticked: - [x] ARKit-subset face system (data-driven emotion recipes) + animal calm-pose set

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~73%
```

---
**2026-06-23 11:38:05Z** · ticked: - [x] 76c · Animal calm-pose set `render3d/CalmPose.ts` — pure per-species §B calm-pose recipe (the AVOID-list encoded as data + the calm rest-bias: ears/tail/head/eye state) + `calmPoseFor(id)` + seeded test that each species' resolved pose satisfies its never-scary invariants; wire as a static rest-pose bias in World; build gate + §9f audit

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~74%
```

---
**2026-06-23 12:29:40Z** · ticked: - [x] 75a · `render3d/Sandbox.ts` — pure compact-clearing layout core (THREE-free/deterministic): `sandboxLayout({cast, interactions})` → a central station at origin, interaction triggers on an INNER ring (reachable in ≤ a couple seconds), all cast calm-posed on OUTER concentric rings, plus a flat jump-menu target list (every cast + interaction). Invariants baked: every input present, no two placements overlap (≥ min spacing), everything inside a compact radius (well within the World bound), interactions nearer the centre than cast. Seeded unit test (determinism · all present · no-overlap · compact-bound · inner-vs-outer · jump-menu covers all)

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~74%
```

---
**2026-06-23 12:39:33Z** · ticked: - [x] 75b-i · `render3d/SandboxScene.ts` — Stage-compatible compact clearing built from `sandboxLayout`: warm light + flat ground, ranger at centre, every staged cast loaded calm-posed (Models/CalmPose/EyeMaterial; prefer animated GLB mixer else procedural gait; procedural stand-in fallback), tap a cast → §1e reframe + real call, an instant ≥56px dual-channel jump-menu (reframe to any target). Reachable `?sandbox` entry + lodge link. Pure `castCallKey` helper + test extension; build gate

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~74%
```

---
**2026-06-23 12:50:11Z** · ticked: - [x] 75b-ii · EF triggers — one inner-ring trigger per EF activity driving the live diegetic engine via a sandbox `WorldCtx` (reuse `resolveViewMode`/`variantFor`, freeze/restore camera + input; 2D floor intact under reduced-motion/no-WebGL). Build gate + §9f audit

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~75%
```

---
**2026-06-23 12:53:44Z** · ticked: - [x] 75b · Sandbox scene + cast + jump-menu wired — a reachable `?sandbox` (or lodge) entry builds the compact scene from the layout, loads every staged cast model calm-posed (reuse `Models`/`CalmPose`/`EyeMaterial`) with its real call, places one trigger per EF activity driving the live diegetic engine via `WorldCtx` (2D floor intact), and an instant ≥56px dual-channel jump-menu that §1e-reframes the camera onto any target; prefer AW-animated GLBs where staged else procedural. Build gate + §9f audit

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~76%
```

---
**2026-06-23 13:01:25Z** · ticked: - [x] 75c · Sandbox meta interactions — one trigger each for companion care · case-board clue · avatar-creator open · badge award · wist-je-dat fact · season/arc resolve beat (reuse the live screens, demo-skip = no briefings/grind). Build gate + §9f audit

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~77%
```

---
**2026-06-23 13:02:17Z** · ticked: - [x] Demo Sandbox — compact one-scene showroom: every character (calm-posed, real call) + one of each interaction (5 EF activities · companion · case-board · avatar · badge · arc beat), instant jump-menu, prefer AW-animated GLBs where staged else procedural; reuses the live engines so it reflects later Phase 5/6 polish; see [DEMO-SANDBOX.md](DEMO-SANDBOX.md). Tier-1 Charactershowroom (`app/showroom.html`) already built.

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~79%
```

---
**2026-06-23 13:12:34Z** · ticked: - [x] Full 3D reduced-motion mode (per-view cuts-not-moves)

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~81%
```

---
**2026-06-23 13:15:33Z** · ticked: - [x] AUDIT (§C.5 QA checklist)

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~82%
```

---
**2026-06-23 13:19:50Z** · ticked: - [x] 97a · Pure karaoke word-clock core `core/wordclock.ts` (THREE/DOM/timing-free) — `buildWordClock(text,{rate,totalMs})` → contiguous non-overlapping `WordSpan[]` (length + punctuation-pause weighted, iOS-safe estimate since Safari `onboundary` is unreliable; optional measured `totalMs` scales the whole clock for the later Piper path) + `activeWordAt(clock, elapsedMs)`; seeded unit test (determinism · contiguous/monotonic spans · punctuation lengthens · longer word longer span · rate scales inversely · empty→0 · active-word lookup)

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~81%
```

---
**2026-06-23 13:31:08Z** · ticked: - [x] 97b · Wire the word-clock into a shared read-aloud helper — `ui/ReadAloud.ts` drives `narrator.speak` + a rAF karaoke highlight in the accessible DOM card (per-word `.ra-word.is-spoken`, reduced-motion = colour swap only, stops cleanly), reused across the Missions read-aloud screens; build gate

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~82%
```

---
**2026-06-23 13:42:27Z** · ticked: - [x] 97c · M3/E3 copy pass — pure `core/readlevel.ts` word/sentence-length lint (3–7 words/sentence, one idea per line) + a test over the live mission/UI copy; tighten the few offenders; build gate + §9f audit

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~82%
```

---
**2026-06-23 13:47:49Z** · ticked: - [x] 102a · ≥56px / dual-channel audit — sweep every UI tap target + feedback channel against the Alvah profile (BUILD-PLAN §3): all interactive elements ≥56px, feedback always on ≥2 channels (colour + scale + glyph/sound + words), reduced-motion-safe; fix the gaps; build gate

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~84%
```

---
**2026-06-23 13:54:00Z** · ticked: - [x] 102b · Tweaks panel wired — `ui/Tweaks.ts` + `tweaks.css` reachable from the lodge: every `Settings` field exposed + wired (geluid/voorlezen/reducedMotion→`setReducedMotionOverride`, leesFont/readSize/leading/accent→a new `applyReadingPrefs` on the document root, jargon, gevolgErnst, ambient volume→`applyAmbient`, autoMoeilijk, force2d→`Settings`+resolver); pure `applyReadingPrefs`/clamp helper + seeded test; build gate

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~85%
```

---
**2026-06-23 14:02:07Z** · ticked: - [x] ≥56px / dual-channel audit; Tweaks panel wired; ambient audio layer

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~86%
```

---
**2026-06-23 14:03:51Z** · status

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~88%
```

---
**2026-06-23 14:11:43Z** · ticked: - [x] 113a · Base-path-portable asset loading + Vite `base` — add `app/vite.config.ts` (`base` defaults `/` standalone, `/ranger/` under `--mode site`, no behaviour change at default) + pure `core/assets.ts` `joinBase`/`assetUrl` (resolves every `/models//draco//audio/` fetch against `import.meta.env.BASE_URL`, absolute-URL passthrough, no double slash); route Models.ts/calls.ts/showroom.ts through it; seeded unit test; standalone build green

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~87%
```

---
**2026-06-23 15:13:34Z** · ticked: - [x] 113b-i · Site-mode Vite build — under `--mode site`: fixed-name output (`app.js` + `app.css`, no content hash), single game entry (exclude `showroom.html`), `outDir`→ repo `public/ranger/` (`emptyOutDir`), publicDir models/audio/draco copied under the `/ranger/` prefix; gitignore the generated `public/ranger/` bundle; root `npm run build` chains `app run build:site` before `astro build`; standalone `app run build` (default base `/`, both html entries) stays green

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~86%
```

---
**2026-06-23 15:17:59Z** · ticked: - [x] 113b · Gated `/ranger` Astro route + bundle wiring — fixed-name Vite output under `base:/ranger/`, build the app into the site's `public/ranger/`; `src/pages/ranger/index.astro` via BaseLayout (full-bleed game shell, gate `alvah-gate-v1` inherited, robots Disallow, no trackers) referencing the staged bundle; root build chains the app build; `astro build` green + §9f audit

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~88%
```

---
**2026-06-23 15:17:59Z** · ticked: - [x] Re-wrap app under `/ranger` route behind the access gate (`alvah-gate-v1`); robots Disallow; no trackers

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~90%
```

---
**2026-06-23 15:18:21Z** · ticked: - [x] 113b-ii · `src/pages/ranger/index.astro` — via BaseLayout (gate `alvah-gate-v1` + robots Disallow + no-trackers inherited), full-bleed game shell mounting `#app`/`#scene`/`#ui` + referencing the staged `/ranger/app.js` + `/ranger/app.css`; `astro build` green + §9f audit

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~91%
```

---
**2026-06-23 15:24:30Z** · ticked: - [x] Migrate localStorage `ranger-mvp-state` → `alvah-ef-v1`; reuse `src/scripts/` where it fits

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~93%
```

---
**2026-06-23 15:30:35Z** · ticked: - [x] 121a · Pure demo-skip core `core/demo.ts` (THREE/DOM-free, deterministic) — `demoAreaTargets()` (every area + an `enabled` gate so locked "binnenkort" areas can't be jumped to), `demoMissionTargets(area)` (each mission + its lead engine label + landschap + done flag), `voltooidAll(area)` (full completion map for the arc fast-forward), `arcMissionIds(area)` (the verhaalHaak missions → resolve), `engineTargets()` (the 5 EF engines). New `skipBriefings` Settings field (default off). Seeded unit test (determinism · locked areas disabled · all missions present · voltooidAll covers every mission · arc ids = verhaalHaak set). Build gate

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~92%
```

---
**2026-06-23 15:35:16Z** · ticked: - [x] 121b · Demo-skip panel UI `ui/DemoSkip.ts` wired from the lodge — jump to any area (locked disabled) / mission (2D floor OR straight into 3D free-roam via `startExplore`) / engine; a global skip-briefings toggle (wired into `showBriefing` → straight to play); fast-forward the arc (markeer alle missies klaar · los de verhaallijn op · begin opnieuw voor een schone test); ≥56px dual-channel, reduced-motion-safe. Build gate + §9f audit

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~93%
```

---
**2026-06-23 15:35:24Z** · ticked: - [x] Demo-skip options: jump to any area/mission/engine, skip briefings, fast-forward the arc

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~94%
```

---
**2026-06-23 15:41:05Z** · ticked: - [x] 125a · CI deps half — wire `.github/workflows/deploy.yml` so the app's `node_modules` exist before the chained `build:site` (setup-node 22 + `npm ci --prefix games/Ranger-Adventures/app` step ahead of `withastro/action@v3`, whose root `npm ci && npm run build` then finds vite/three). Code-only, durable in the working tree, not pushed (a push triggers the live deploy → gated on 125b). Targeted test = root `npm run build` green locally (the exact chain the workflow runs)

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~94%
```

---
**2026-06-23 15:47:22Z** · ticked: - [x] 132a-i · Pure Deep Demo script core `core/deepdemo.ts` (THREE/DOM-free, deterministic) — the ordered guided-tour beat list covering the §132 checklist (boot · avatar · free-roam · meet cast+hear voice · play each of the 5 EF engines in-world · arc · companion · badges) as data ({id,kind,titel,toelichting,engine?}), plus a pure cursor (`nextBeat`/`prevBeat` clamped, `beatAt`, `engineBeats`). Seeded unit test (determinism · all checklist kinds present · engine beats = all 5 EF in EF_ENGINES order · ordered boot-first/badges-last · cursor clamps at both bounds · every `toelichting` passes the M3/E3 readlevel lint)

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~92%
```

---
**2026-06-23 15:53:49Z** · ticked: - [x] 132a-ii · Deep Demo runner UI `ui/DeepDemo.ts` — drives the `deepDemoScript()` sequence reusing the LIVE screens (startExplore · demoJump in-world per engine · showMetaDemo arc/companion · showBadges · showAvatarCreator · sandbox cast) behind a narrated ≥56px dual-channel control strip (Vorige/Volgende/Sla over/Terug naar de hut), reduced-motion-safe; reachable from the lodge + a `?demo` boot entry. Build gate + §9f audit

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~92%
```

---
**2026-06-23 15:53:57Z** · ticked: - [x] Deep Demo built (boot→avatar→free-roam→meet realistic ranger+animals→hear voice→play 5 engines in-world→arc+companion+badges)

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~94%
```

---
**2026-06-23 16:02:38Z** · ticked: - [x] Auto-QA report (draw calls, a11y, persistence, tone lint) + screenshots → Floris review

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~95%
```

---
**2026-06-23 16:07:41Z** · status

```
✔ landed: Anything World pipeline · script — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API
▶ phase:  Asset pipeline
→ next:   Anything World pipeline · generation — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — ⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.
▷ progress: ~95%
```

---
**2026-07-02 10:50:17Z** · status

```
✔ landed: —
▶ phase:  Fase W0 — Waarheidsharnas (browser proof)
→ next:   W0.1 Playwright scaffold: devDep, config, boot smoke spec, npm scripts e2e + e2e:smoke, install chromium
▷ progress: ~0%
```

---
**2026-07-02 11:21:54Z** · status

```
✔ landed: —
▶ phase:  Fase W0 — Waarheidsharnas (browser proof)
→ next:   W0.1 Playwright scaffold: devDep, config (poort ~4199, reuseExistingServer, CI-retries), boot smoke spec, npm scripts e2e + e2e:smoke, chromium install, gitignore-regels (test-results/, playwright-report/, e2e/__shots__/, incoming/), git hooksPath .githooks
▤ ledger: WORLD-LEDGER.md
▷ progress: ~0%
⚠ blocker: test-blocker: verifying persistence
```

---
**2026-07-02 11:21:54Z** · status

```
✔ landed: —
▶ phase:  Fase W0 — Waarheidsharnas (browser proof)
→ next:   W0.1 Playwright scaffold: devDep, config (poort ~4199, reuseExistingServer, CI-retries), boot smoke spec, npm scripts e2e + e2e:smoke, chromium install, gitignore-regels (test-results/, playwright-report/, e2e/__shots__/, incoming/), git hooksPath .githooks
▤ ledger: WORLD-LEDGER.md
▷ progress: ~0%
⚠ blocker: test-blocker: verifying persistence
```

---
**2026-07-02 11:21:54Z** · status

```
✔ landed: —
▶ phase:  Fase W0 — Waarheidsharnas (browser proof)
→ next:   W0.1 Playwright scaffold: devDep, config (poort ~4199, reuseExistingServer, CI-retries), boot smoke spec, npm scripts e2e + e2e:smoke, chromium install, gitignore-regels (test-results/, playwright-report/, e2e/__shots__/, incoming/), git hooksPath .githooks
▤ ledger: WORLD-LEDGER.md
▷ progress: ~0%
```

---
**2026-07-02 11:34:18Z** · status

```
✔ landed: —
▶ phase:  Fase W0 — Waarheidsharnas (browser proof)
→ next:   W0.1 Playwright scaffold: devDep, config (poort ~4199, reuseExistingServer, CI-retries), boot smoke spec, npm scripts e2e + e2e:smoke, chromium install, gitignore-regels (test-results/, playwright-report/, e2e/__shots__/, incoming/), git hooksPath .githooks
▤ ledger: WORLD-LEDGER.md
▷ progress: ~0%
```

---
**2026-07-02 12:03:31Z** · ticked: - [x] W0.1 Playwright scaffold: devDep, config (poort ~4199, reuseExistingServer, CI-retries), boot smoke spec, npm scripts e2e + e2e:smoke, chromium install, gitignore-regels (test-results/, playwright-report/, e2e/__shots__/, incoming/), git hooksPath .githooks

```
✔ landed: W0.1 Playwright scaffold: devDep, config (poort ~4199, reuseExistingServer, CI-retries), boot smoke spec, npm scripts e2e + e2e:smoke, chromium install, gitignore-regels (test-results/, playwright-report/, e2e/__shots__/, incoming/), git hooksPath .githooks
▶ phase:  Fase W0 — Waarheidsharnas (browser proof)
→ next:   W0.2 Dev-state hook window.__ranger (screen, pos, cameraYaw, drawCalls, missionView, clip) achter DEV of ?dev=1
▤ ledger: WORLD-LEDGER.md
▷ progress: ~3%
```

---
**2026-07-02 12:08:14Z** · ticked: - [x] W0.2 Dev-state hook window.__ranger (screen, pos, cameraYaw, drawCalls, missionView, clip) achter DEV of ?dev=1

```
✔ landed: W0.2 Dev-state hook window.__ranger (screen, pos, cameraYaw, drawCalls, missionView, clip) achter DEV of ?dev=1
▶ phase:  Fase W0 — Waarheidsharnas (browser proof)
→ next:   W0.3 Journey E2E: Begin → avatar → hut → Verken de Veluwe → screen world, met screenshots; world-reach in e2e:smoke v1
▤ ledger: WORLD-LEDGER.md
▷ progress: ~4%
```

---
**2026-07-02 12:10:28Z** · ticked: - [x] W0.3 Journey E2E: Begin → avatar → hut → Verken de Veluwe → screen world, met screenshots; world-reach in e2e:smoke v1

```
✔ landed: W0.3 Journey E2E: Begin → avatar → hut → Verken de Veluwe → screen world, met screenshots; world-reach in e2e:smoke v1
▶ phase:  Fase W0 — Waarheidsharnas (browser proof)
→ next:   W0.4 Movement red-first E2E: tap-to-walk ≥2 m via expect.poll; diagnose + fix waarom taps de ranger niet bewegen; e2e:smoke upgraden en BEVRIEZEN; root-cause in plan §10
▤ ledger: WORLD-LEDGER.md
▷ progress: ~6%
```

---
**2026-07-02 12:16:24Z** · ticked: - [x] W0.4 Movement red-first E2E: tap-to-walk ≥2 m via expect.poll; diagnose + fix waarom taps de ranger niet bewegen; e2e:smoke upgraden en BEVRIEZEN; root-cause in plan §10

```
✔ landed: W0.4 Movement red-first E2E: tap-to-walk ≥2 m via expect.poll; diagnose + fix waarom taps de ranger niet bewegen; e2e:smoke upgraden en BEVRIEZEN; root-cause in plan §10
▶ phase:  Fase W0 — Waarheidsharnas (browser proof)
→ next:   W0.5 Budgets overlay ALLEEN achter ?dev=1 (niet DEV); E2E assert aanwezig/afwezig
▤ ledger: WORLD-LEDGER.md
▷ progress: ~9%
```

---
**2026-07-02 12:19:05Z** · ticked: - [x] W0.5 Budgets overlay ALLEEN achter ?dev=1 (niet DEV); E2E assert aanwezig/afwezig

```
✔ landed: W0.5 Budgets overlay ALLEEN achter ?dev=1 (niet DEV); E2E assert aanwezig/afwezig
▶ phase:  Fase W0 — Waarheidsharnas (browser proof)
→ next:   W0.6 CI-gate in deploy.yml: apart test-job (unit + e2e:smoke, --with-deps, swiftshader-args, cache, paths-filter); eerst continue-on-error, blocking na één groene ubuntu-run
▤ ledger: WORLD-LEDGER.md
▷ progress: ~9%
```

---
**2026-07-02 12:30:06Z** · ticked: - [x] W0.6a Test-job landen: unit-tests (blokkerend) + e2e:smoke (continue-on-error, paths-gated op ranger-wijzigingen, --with-deps chromium, swiftshader launch-args, playwright-cache); build needs test; push + één groene ubuntu-run observeren

```
✔ landed: W0.6a Test-job landen: unit-tests (blokkerend) + e2e:smoke (continue-on-error, paths-gated op ranger-wijzigingen, --with-deps chromium, swiftshader launch-args, playwright-cache); build needs test; push + één groene ubuntu-run observeren
▶ phase:  Fase W0 — Waarheidsharnas (browser proof)
→ next:   W0.6b e2e:smoke naar blocking flippen (continue-on-error weg); nog één groene ubuntu-run bevestigen
▤ ledger: WORLD-LEDGER.md
▷ progress: ~10%
```

---
**2026-07-02 12:36:07Z** · ticked: - [x] W0.6b e2e:smoke naar blocking flippen (continue-on-error weg); nog één groene ubuntu-run bevestigen

```
✔ landed: W0.6b e2e:smoke naar blocking flippen (continue-on-error weg); nog één groene ubuntu-run bevestigen
▶ phase:  Fase W0 — Waarheidsharnas (browser proof)
→ next:   W0.7 Meshy balance probe: credits loggen (gemaskeerd) of status --blocker; --force toegestaan
▤ ledger: WORLD-LEDGER.md
▷ progress: ~11%
```

---
**2026-07-02 12:37:14Z** · status

```
✔ landed: W0.6b e2e:smoke naar blocking flippen (continue-on-error weg); nog één groene ubuntu-run bevestigen
▶ phase:  Fase W0 — Waarheidsharnas (browser proof)
→ next:   W0.7 Meshy balance probe: credits loggen (gemaskeerd) of status --blocker; --force toegestaan
▤ ledger: WORLD-LEDGER.md
▷ progress: ~11%
```

---
**2026-07-02** · W0.7 Meshy balance probe

```
Key present: msy_…(40 chars)
[balance] HTTP 200 → 7595 credits
```
Baseline 7,615 → 7,595 (−20, prep preview gen). >> ~35 cr needed for W3.1. No blocker.

---
**2026-07-02 12:38:36Z** · ticked: - [x] W0.7 Meshy balance probe: credits loggen (gemaskeerd) of status --blocker; --force toegestaan

```
✔ landed: W0.7 Meshy balance probe: credits loggen (gemaskeerd) of status --blocker; --force toegestaan
▶ phase:  Fase W0 — Waarheidsharnas (browser proof)
→ next:   W0.8 WebKit-project (iPad Safari engine): webkit install, smoke + journey op webkit groen; afwijkingen in §10
▤ ledger: WORLD-LEDGER.md
▷ progress: ~12%
```

---
**2026-07-02 12:42:30Z** · status

```
✔ landed: W0.7 Meshy balance probe: credits loggen (gemaskeerd) of status --blocker; --force toegestaan
▶ phase:  Fase W0 — Waarheidsharnas (browser proof)
→ next:   W0.8 WebKit-project (iPad Safari engine): webkit install, smoke + journey op webkit groen; afwijkingen in §10
▤ ledger: WORLD-LEDGER.md
▷ progress: ~12%
⚠ blocker: W0.8 webkit smoke can't run on this Mac: Playwright's frozen WebKit build (webkit v2251, mac14-arm64) bus-errors (Bus error:10) at launch on macOS 14.1 — Apple/Playwright dropped support. Chromium smoke unaffected & still gates ticks. To unblock: (a) update macOS so a current WebKit build installs, or (b) tell the run to move webkit smoke to CI ubuntu (webkit works there). Config is ready: webkit project + 'npm run e2e:webkit' + shot() no-op on webkit are committed.
```

---
**2026-07-02 13:06:47Z** · ticked: - [x] W0.8 WebKit-project (iPad Safari engine): webkit-config forward-ready gelandt; lokaal draaien onmogelijk (frozen webkit op mac14-arm64, OS niet te updaten wegens schijfruimte) → graceful-degrade skip met §10-verdict; CI-webkit optioneel follow-up; --force toegestaan

```
✔ landed: W0.8 WebKit-project (iPad Safari engine): webkit-config forward-ready gelandt; lokaal draaien onmogelijk (frozen webkit op mac14-arm64, OS niet te updaten wegens schijfruimte) → graceful-degrade skip met §10-verdict; CI-webkit optioneel follow-up; --force toegestaan
▶ phase:  Fase W1 — Besturing
→ next:   W1.1 core/input.ts pure module (pijlen + WASD + joystick-vector, camera-relatief) + unit tests
▤ ledger: WORLD-LEDGER.md
▷ progress: ~14%
⚠ blocker: W0.8 webkit smoke can't run on this Mac: Playwright's frozen WebKit build (webkit v2251, mac14-arm64) bus-errors (Bus error:10) at launch on macOS 14.1 — Apple/Playwright dropped support. Chromium smoke unaffected & still gates ticks. To unblock: (a) update macOS so a current WebKit build installs, or (b) tell the run to move webkit smoke to CI ubuntu (webkit works there). Config is ready: webkit project + 'npm run e2e:webkit' + shot() no-op on webkit are committed.
```

---
**2026-07-02 13:06:52Z** · status

```
✔ landed: W0.8 WebKit-project (iPad Safari engine): webkit-config forward-ready gelandt; lokaal draaien onmogelijk (frozen webkit op mac14-arm64, OS niet te updaten wegens schijfruimte) → graceful-degrade skip met §10-verdict; CI-webkit optioneel follow-up; --force toegestaan
▶ phase:  Fase W1 — Besturing
→ next:   W1.1 core/input.ts pure module (pijlen + WASD + joystick-vector, camera-relatief) + unit tests
▤ ledger: WORLD-LEDGER.md
▷ progress: ~14%
```

---
**2026-07-02 13:10:39Z** · ticked: - [x] W1.1 core/input.ts pure module (pijlen + WASD + joystick-vector, camera-relatief) + unit tests

```
✔ landed: W1.1 core/input.ts pure module (pijlen + WASD + joystick-vector, camera-relatief) + unit tests
▶ phase:  Fase W1 — Besturing
→ next:   W1.2 Toetsenbord lopen door resolveMove; E2E ArrowUp poll ≥2 m + collision houdt
▤ ledger: WORLD-LEDGER.md
▷ progress: ~16%
```

---
**2026-07-02 13:16:08Z** · ticked: - [x] W1.2 Toetsenbord lopen door resolveMove; E2E ArrowUp poll ≥2 m + collision houdt

```
✔ landed: W1.2 Toetsenbord lopen door resolveMove; E2E ArrowUp poll ≥2 m + collision houdt
▶ phase:  Fase W1 — Besturing
→ next:   W1.3 Virtuele joystick ui/Joystick.ts (≥56 px, coarse-pointer default, Instellingen-toggle via state.ts); E2E touch-drag beweegt
▤ ledger: WORLD-LEDGER.md
▷ progress: ~18%
```

---
**2026-07-02 13:26:05Z** · ticked: - [x] W1.3 Virtuele joystick ui/Joystick.ts (≥56 px, coarse-pointer default, Instellingen-toggle via state.ts); E2E touch-drag beweegt

```
✔ landed: W1.3 Virtuele joystick ui/Joystick.ts (≥56 px, coarse-pointer default, Instellingen-toggle via state.ts); E2E touch-drag beweegt
▶ phase:  Fase W1 — Besturing
→ next:   W1.4 Interactie-toets Space/Enter voor nabijheids-actie; E2E opent missie-briefing
▤ ledger: WORLD-LEDGER.md
▷ progress: ~21%
```

---
**2026-07-02 13:35:29Z** · ticked: - [x] W1.4 Interactie-toets Space/Enter voor nabijheids-actie; E2E opent missie-briefing

```
✔ landed: W1.4 Interactie-toets Space/Enter voor nabijheids-actie; E2E opent missie-briefing
▶ phase:  Fase W1 — Besturing
→ next:   W1.5 Meedraaiende volgcamera (dt-onafhankelijke demping, yaw-clamp ~120°/s, reframe-precedentie, reduced-motion → vaste richting, toggle); unit tests demping/wrap/clamp + E2E yaw ≥45° én yaw-stil onder reduced-motion
▤ ledger: WORLD-LEDGER.md
▷ progress: ~22%
```

---
**2026-07-02 13:53:31Z** · ticked: - [x] W1.5 Meedraaiende volgcamera (dt-onafhankelijke demping, yaw-clamp ~120°/s, reframe-precedentie, reduced-motion → vaste richting, toggle); unit tests demping/wrap/clamp + E2E yaw ≥45° én yaw-stil onder reduced-motion

```
✔ landed: W1.5 Meedraaiende volgcamera (dt-onafhankelijke demping, yaw-clamp ~120°/s, reframe-precedentie, reduced-motion → vaste richting, toggle); unit tests demping/wrap/clamp + E2E yaw ≥45° én yaw-stil onder reduced-motion
▶ phase:  Fase W1 — Besturing
→ next:   W1.6 Onboarding-hint eerste wereld-entree (Loop met de pijltjes / Sleep de stick, voorleesbaar, seen-flag via state.ts)
▤ ledger: WORLD-LEDGER.md
▷ progress: ~25%
```

---
**2026-07-02 13:59:53Z** · ticked: - [x] W1.6 Onboarding-hint eerste wereld-entree (Loop met de pijltjes / Sleep de stick, voorleesbaar, seen-flag via state.ts)

```
✔ landed: W1.6 Onboarding-hint eerste wereld-entree (Loop met de pijltjes / Sleep de stick, voorleesbaar, seen-flag via state.ts)
▶ phase:  Fase W2 — Wereld als voordeur
→ next:   W2.1 Boot direct de wereld in na titel/avatar; interim Naar-de-hut knop; ?demo en ?sandbox blijven werken; journey-E2E aangepast
▤ ledger: WORLD-LEDGER.md
▷ progress: ~26%
```

---
**2026-07-02 14:05:55Z** · ticked: - [x] W2.1 Boot direct de wereld in na titel/avatar; interim Naar-de-hut knop; ?demo en ?sandbox blijven werken; journey-E2E aangepast

```
✔ landed: W2.1 Boot direct de wereld in na titel/avatar; interim Naar-de-hut knop; ?demo en ?sandbox blijven werken; journey-E2E aangepast
▶ phase:  Fase W2 — Wereld als voordeur
→ next:   W2.2 Cabin-hub op de open plek: cabin + case-board props, missiebord-overlay zonder leaveWorld; E2E assert missionView === 3d
▤ ledger: WORLD-LEDGER.md
▷ progress: ~28%
```

---
**2026-07-02 14:18:29Z** · ticked: - [x] W2.2 Cabin-hub op de open plek: cabin + case-board props, missiebord-overlay zonder leaveWorld; E2E assert missionView === 3d

```
✔ landed: W2.2 Cabin-hub op de open plek: cabin + case-board props, missiebord-overlay zonder leaveWorld; E2E assert missionView === 3d
▶ phase:  Fase W2 — Wereld als voordeur
→ next:   W2.3 Twee-missies-ketting E2E vanuit de wereld zonder hutbezoek; BeatSummary persist + missionView 3d voor beide
▤ ledger: WORLD-LEDGER.md
▷ progress: ~31%
```

---
**2026-07-02 14:35:08Z** · ticked: - [x] W2.3 Twee-missies-ketting E2E vanuit de wereld zonder hutbezoek; BeatSummary persist + missionView 3d voor beide

```
✔ landed: W2.3 Twee-missies-ketting E2E vanuit de wereld zonder hutbezoek; BeatSummary persist + missionView 3d voor beide
▶ phase:  Fase W2 — Wereld als voordeur
→ next:   W2.4a Pauze/hub-overlay shell: instellingen + badges bereikbaar zonder leaveWorld
▤ ledger: WORLD-LEDGER.md
▷ progress: ~33%
```

---
**2026-07-02 14:41:17Z** · ticked: - [x] W2.4a Pauze/hub-overlay shell: instellingen + badges bereikbaar zonder leaveWorld

```
✔ landed: W2.4a Pauze/hub-overlay shell: instellingen + badges bereikbaar zonder leaveWorld
▶ phase:  Fase W2 — Wereld als voordeur
→ next:   W2.4b Prikbord + raaf in hub-overlay; dode hut-flows opruimen; MILESTONE: commit + push (eerste coherente wereld-eerst build live)
▤ ledger: WORLD-LEDGER.md
▷ progress: ~34%
```

---
**2026-07-02 14:51:53Z** · ticked: - [x] W2.4b Prikbord + raaf in hub-overlay; dode hut-flows opruimen; MILESTONE: commit + push (eerste coherente wereld-eerst build live)

```
✔ landed: W2.4b Prikbord + raaf in hub-overlay; dode hut-flows opruimen; MILESTONE: commit + push (eerste coherente wereld-eerst build live)
▶ phase:  Fase W3 — Levend (animaties)
→ next:   W3.0 Sleutels verifiëren (test-meshy + test-xeno, gemaskeerd) en balans loggen; bij ontbreken status --blocker en dóór; --force toegestaan
▤ ledger: WORLD-LEDGER.md
▷ progress: ~36%
```

---
**2026-07-02 14:54:08Z** · ticked: - [x] W3.0 Sleutels verifiëren (test-meshy + test-xeno, gemaskeerd) en balans loggen; bij ontbreken status --blocker en dóór; --force toegestaan

```
✔ landed: W3.0 Sleutels verifiëren (test-meshy + test-xeno, gemaskeerd) en balans loggen; bij ontbreken status --blocker en dóór; --force toegestaan
▶ phase:  Fase W3 — Levend (animaties)
→ next:   W3.1 Ranger regen + rig via Meshy (~35 cr, eerst balans checken, gen BACKGROUNDED met log-polling) of ingest app/incoming/ranger-alvah-rigged.glb; staged GLB ≥1 skin ≥2 clips
▤ ledger: WORLD-LEDGER.md
▷ progress: ~37%
```

---
**2026-07-02 15:15:05Z** · ticked: - [x] W3.1 Ranger regen + rig via Meshy (~35 cr, eerst balans checken, gen BACKGROUNDED met log-polling) of ingest app/incoming/ranger-alvah-rigged.glb; staged GLB ≥1 skin ≥2 clips

```
✔ landed: W3.1 Ranger regen + rig via Meshy (~35 cr, eerst balans checken, gen BACKGROUNDED met log-polling) of ingest app/incoming/ranger-alvah-rigged.glb; staged GLB ≥1 skin ≥2 clips
▶ phase:  Fase W3 — Levend (animaties)
→ next:   W3.2 Speler-animatie state machine: idle/walk crossfade op snelheid, procedurele fallback; unit test + E2E clip().name walk én clip().time loopt
▤ ledger: WORLD-LEDGER.md
▷ progress: ~40%
```

---
**2026-07-02 15:38:40Z** · ticked: - [x] W3.2 Speler-animatie state machine: idle/walk crossfade op snelheid, procedurele fallback; unit test + E2E clip().name walk én clip().time loopt

```
✔ landed: W3.2 Speler-animatie state machine: idle/walk crossfade op snelheid, procedurele fallback; unit test + E2E clip().name walk én clip().time loopt
▶ phase:  Fase W3 — Levend (animaties)
→ next:   W3.3 Warden + poacher spelen hun bestaande baked clips op hun plek
▤ ledger: WORLD-LEDGER.md
▷ progress: ~42%
```

---
**2026-07-02 15:46:20Z** · ticked: - [x] W3.3 Warden + poacher spelen hun bestaande baked clips op hun plek

```
✔ landed: W3.3 Warden + poacher spelen hun bestaande baked clips op hun plek
▶ phase:  Fase W3 — Levend (animaties)
→ next:   W3.4a Accuracy-dossier zoogdieren: webresearch per dier (maten, vacht/seizoen, oogkleur, kenmerken, gang, 1-2 kindfeiten) naar research/animal-visual-accuracy.md met bron-URL + datum per claim; geen web → --blocker, nooit verzinnen; --force toegestaan
▤ ledger: WORLD-LEDGER.md
▷ progress: ~43%
```

---
**2026-07-02 15:54:01Z** · ticked: - [x] W3.4a Accuracy-dossier zoogdieren: webresearch per dier (maten, vacht/seizoen, oogkleur, kenmerken, gang, 1-2 kindfeiten) naar research/animal-visual-accuracy.md met bron-URL + datum per claim; geen web → --blocker, nooit verzinnen; --force toegestaan

```
✔ landed: W3.4a Accuracy-dossier zoogdieren: webresearch per dier (maten, vacht/seizoen, oogkleur, kenmerken, gang, 1-2 kindfeiten) naar research/animal-visual-accuracy.md met bron-URL + datum per claim; geen web → --blocker, nooit verzinnen; --force toegestaan
▶ phase:  Fase W3 — Levend (animaties)
→ next:   W3.4b Accuracy-dossier vogels: zelfde behandeling voor de ~10 missie/audio-relevante vogels; zelfde bronregels; --force toegestaan
▤ ledger: WORLD-LEDGER.md
▷ progress: ~46%
```

---
**2026-07-02 15:56:45Z** · status

```
✔ landed: W3.4a Accuracy-dossier zoogdieren: webresearch per dier (maten, vacht/seizoen, oogkleur, kenmerken, gang, 1-2 kindfeiten) naar research/animal-visual-accuracy.md met bron-URL + datum per claim; geen web → --blocker, nooit verzinnen; --force toegestaan
▶ phase:  Fase W3 — Levend (animaties)
→ next:   W3.4b Accuracy-dossier vogels: zelfde behandeling voor de ~10 missie/audio-relevante vogels; zelfde bronregels; --force toegestaan
▤ ledger: WORLD-LEDGER.md
▷ progress: ~46%
```

---
**2026-07-02 16:07:00Z** · ticked: - [x] W3.4b Accuracy-dossier vogels: zelfde behandeling voor de ~10 missie/audio-relevante vogels; zelfde bronregels; --force toegestaan

```
✔ landed: W3.4b Accuracy-dossier vogels: zelfde behandeling voor de ~10 missie/audio-relevante vogels; zelfde bronregels; --force toegestaan
▶ phase:  Fase W3 — Levend (animaties)
→ next:   W3.5 Dieren-animatie via CC0/CC-BY packs (Quaternius, poly.pizza, Kenney): match per boegbeeld-dier, license-log, schaal per dossier, optimize-animated, staged animated:true; stijl-check screenshot in qa-evidence-2; geen match → §10-verdict; Anything World NIET gebruiken; --force toegestaan
▤ ledger: WORLD-LEDGER.md
▷ progress: ~47%
```

---
**2026-07-02 16:24:09Z** · ticked: - [x] W3.5 Dieren-animatie via CC0/CC-BY packs (Quaternius, poly.pizza, Kenney): match per boegbeeld-dier, license-log, schaal per dossier, optimize-animated, staged animated:true; stijl-check screenshot in qa-evidence-2; geen match → §10-verdict; Anything World NIET gebruiken; --force toegestaan

```
✔ landed: W3.5 Dieren-animatie via CC0/CC-BY packs (Quaternius, poly.pizza, Kenney): match per boegbeeld-dier, license-log, schaal per dossier, optimize-animated, staged animated:true; stijl-check screenshot in qa-evidence-2; geen match → §10-verdict; Anything World NIET gebruiken; --force toegestaan
▶ phase:  Fase W3 — Levend (animaties)
→ next:   W3.6 Ambient dierenleven: zwerf-loops met baked clips waar W3.5 ze stageerde en verbeterde procedurele gaits elders, 2 zwevende vogels; calm-pose gate; E2E ≥2 dieren + drawCalls <150
▤ ledger: WORLD-LEDGER.md
▷ progress: ~50%
```

---
**2026-07-02 16:34:03Z** · ticked: - [x] W3.6 Ambient dierenleven: zwerf-loops met baked clips waar W3.5 ze stageerde en verbeterde procedurele gaits elders, 2 zwevende vogels; calm-pose gate; E2E ≥2 dieren + drawCalls <150

```
✔ landed: W3.6 Ambient dierenleven: zwerf-loops met baked clips waar W3.5 ze stageerde en verbeterde procedurele gaits elders, 2 zwevende vogels; calm-pose gate; E2E ≥2 dieren + drawCalls <150
▶ phase:  Fase W3 — Levend (animaties)
→ next:   W3.7 Dossier toepassen: relatieve schaal per dier t.o.v. de ranger, vacht-tinten en oogkleuren (eye-recipe), houding; before/after showroom-screenshots, top-correcties in qa-evidence-2; strijdige Wist-je-dat teksten fixen via readlevel-corpus
▤ ledger: WORLD-LEDGER.md
▷ progress: ~53%
```

---
**2026-07-02 16:46:20Z** · ticked: - [x] W3.7a Relatieve schaal per dier t.o.v. de ranger: canonieke stand-hoogte-tabel (dossier-afgeleid, ranger-relatief) door de plaatsings/showroom height-path; unit test op ordening + ranger-ratio; showroom true-scale modus + before/after screenshots in qa-evidence-2; E2E leest de toegepaste schaal-ordening live

```
✔ landed: W3.7a Relatieve schaal per dier t.o.v. de ranger: canonieke stand-hoogte-tabel (dossier-afgeleid, ranger-relatief) door de plaatsings/showroom height-path; unit test op ordening + ranger-ratio; showroom true-scale modus + before/after screenshots in qa-evidence-2; E2E leest de toegepaste schaal-ordening live
▶ phase:  Fase W3 — Levend (animaties)
→ next:   W3.7b Vacht-tinten + oogkleur-recept + houding: vos rufous-retint (+ andere dossier-strijdige vachten) via tint-util; eye-recipe uitbreiden (wolf/wildzwijn/frisling iris, adder rood) per dossier; houding-flags; unit test eyeSpecFor; before/after showroom in qa-evidence-2; §10-notitie
▤ ledger: WORLD-LEDGER.md
▷ progress: ~53%
```

---
**2026-07-02 16:55:42Z** · ticked: - [x] W3.7b Vacht-tinten + oogkleur-recept + houding: vos rufous-retint (+ andere dossier-strijdige vachten) via tint-util; eye-recipe uitbreiden (wolf/wildzwijn/frisling iris, adder rood) per dossier; houding-flags; unit test eyeSpecFor; before/after showroom in qa-evidence-2; §10-notitie

```
✔ landed: W3.7b Vacht-tinten + oogkleur-recept + houding: vos rufous-retint (+ andere dossier-strijdige vachten) via tint-util; eye-recipe uitbreiden (wolf/wildzwijn/frisling iris, adder rood) per dossier; houding-flags; unit test eyeSpecFor; before/after showroom in qa-evidence-2; §10-notitie
▶ phase:  Fase W3 — Levend (animaties)
→ next:   W3.7c Strijdige Wist-je-dat teksten fixen via readlevel-corpus; §10 noteert elke dossier-tegenspraak
▤ ledger: WORLD-LEDGER.md
▷ progress: ~54%
```

---
**2026-07-02 17:01:54Z** · ticked: - [x] W3.7c Strijdige Wist-je-dat teksten fixen via readlevel-corpus; §10 noteert elke dossier-tegenspraak

```
✔ landed: W3.7c Strijdige Wist-je-dat teksten fixen via readlevel-corpus; §10 noteert elke dossier-tegenspraak
▶ phase:  Fase W4 — Rijke wereld
→ next:   W4.1 Landmark-props geplaatst (uitkijktoren, ecoduct, vogelkijkhut, BOA-post, wegwijzers) + collision + wayfinding; E2E wandeling spawn→toren; <150 draw calls
▤ ledger: WORLD-LEDGER.md
▷ progress: ~55%
```

---
**2026-07-02 17:10:44Z** · ticked: - [x] W4.1 Landmark-props geplaatst (uitkijktoren, ecoduct, vogelkijkhut, BOA-post, wegwijzers) + collision + wayfinding; E2E wandeling spawn→toren; <150 draw calls

```
✔ landed: W4.1 Landmark-props geplaatst (uitkijktoren, ecoduct, vogelkijkhut, BOA-post, wegwijzers) + collision + wayfinding; E2E wandeling spawn→toren; <150 draw calls
▶ phase:  Fase W4 — Rijke wereld
→ next:   W4.2 Natuur-aankleding: echte boom-GLB's bij paden/POI's, stronken, keien, paddenstoelen, riet per bioom
▤ ledger: WORLD-LEDGER.md
▷ progress: ~58%
```

---
**2026-07-02 17:19:21Z** · ticked: - [x] W4.2 Natuur-aankleding: echte boom-GLB's bij paden/POI's, stronken, keien, paddenstoelen, riet per bioom

```
✔ landed: W4.2 Natuur-aankleding: echte boom-GLB's bij paden/POI's, stronken, keien, paddenstoelen, riet per bioom
▶ phase:  Fase W4 — Rijke wereld
→ next:   W4.3 Zandpaden-netwerk verbindt spawn en POI's; wayfinding volgt paden
▤ ledger: WORLD-LEDGER.md
▷ progress: ~60%
```

---
**2026-07-02 17:29:21Z** · ticked: - [x] W4.3 Zandpaden-netwerk verbindt spawn en POI's; wayfinding volgt paden

```
✔ landed: W4.3 Zandpaden-netwerk verbindt spawn en POI's; wayfinding volgt paden
▶ phase:  Fase W4 — Rijke wereld
→ next:   W4.4 Grond-detail: procedurele albedo per bioom, gouache-gevoel behouden; before/after screenshots + één paar in qa-evidence-2/
▤ ledger: WORLD-LEDGER.md
▷ progress: ~62%
```
