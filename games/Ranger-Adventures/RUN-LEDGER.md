# Ranger van de Veluwe — Run Ledger (the resumable checklist)

> **This is the single source of truth for "what's done / what's next."** The autonomous run ticks each box
> the moment a step is finished AND targeted-tested (BUILD-PLAN §9a/§9e). **A fresh thread resumes by reading
> this top-to-bottom and doing the first unchecked `[ ]`.** Keep `RUN-STATUS.md` (live snapshot) in sync.
> Rules: split a step that's larger than expected into sub-steps here first (§9d); audit after each stage
> (§9f); never advance on a failed audit. Approx weights in (parens) feed the ~% in RUN-STATUS.md.

## Phase 0–3 — spine, content, meta core  ✅ DONE (Jun 2026 pass)
- [x] Phase 0 scaffold (Vite+TS+Three, golden-hour stage, draw-call budget overlay)
- [x] Phase 1 spine + all 5 EF engines (2D views, ENGINE_VIEWS, skill/DDA, localStorage)
- [x] Phase 2 content — 10 missions, 5 engines × 4 landscapes, season/poacher arc as data
- [x] Phase 3 meta CORE — mission wrapper (lodge→briefing→play→fact→reward), breinkracht badges, knap-woord
- [x] Phase 3 remainder · case-board screen — verhaalboog clues derived from completed verhaalHaak-missions (data gate) + hopeful ontknoping; prikbord overlay reachable from the lodge (2)
- [x] Phase 3 remainder · companion+rehab STATE — `core/companion.ts` pure model (soort/fase/bond/kunstjes/meeOpMissie + rehab) + state.ts mutators (rescue→bondDelta→grow→mee, startRehab/releaseRehab) + persistence/migration; seeded unit test (1)
- [x] Phase 3 remainder · companion+rehab UI — CareRoutine care-skin feeds simon/dagnacht skill records (order=simon, over-handle=dagnacht); cabin/perch screen (rescue + daily care + opvang release) reachable from the lodge (1)
- [x] Phase 3 remainder · avatar creator UI + avatar state threaded through copy/voice (naam/huid/haar/outfit/iris; "Alvah" default injected into briefing/fact/reward + narrator) (1)

## Asset pipeline  ◑ IN PROGRESS
- [x] meshy-gen preview→refine + poly target; gltf-optimize (meshopt+WebP+DRACO); audio-fetch (xeno/Freesound, licence log); finalize watcher
- [x] Core cast generated (33/76 renders): 3 humanoids, 7 Meshy animals, 14 base props, 9 birds
- [x] Ranger rigged via Meshy API (proof); rigged GLB saved to assets-gen/animated/
- [x] Generate remaining renders · batch A — gameplay-critical cast: immersion/tracking props + core birds + fox/ree/edelhert/heideblauwtje (shotlist items 1–23) (3)
- [x] Generate remaining renders · batch B — story + vehicles + seasonal variants (shotlist items 24–37: ecoduct/boa-post/bird-hide/nest-box/snare/raven-nest, jeep/helicopter, oak/birch/heather autumn+winter, snow-mound) (2)
- [x] Generate remaining renders · batch C — finishing props + last birds (shotlist items 38–43: raaf-fledgling, anthill/foxglove/acorn-cluster, buizerd, wilde-eend) (1)
- [x] Rig the 2 remaining humanoids via Meshy API (warden, poacher) (1)
- [x] **Anything World pipeline · script** — `scripts/anything-world.mjs`: wire `ANYTHING_WORLD_API` (app/.env.local), preflight `/credits`, rig→animate→download to `assets-gen/animated/<id>.glb`, idempotent + manifest-recorded; AW-eligible cast = 8 quadrupeds + 23 birds; adder/heikikker/butterfly stay procedural; defensive field-probing for the experimental API (2)
- [ ] **Anything World pipeline · generation** — loop/Floris-owned external paid job (~31 models, ~2 AW runs). Needs source GLBs in `assets-gen/<id>.glb` (Meshy gen) + AW credits; NOT runnable in a sandboxed thread. Run: `node app/scripts/anything-world.mjs`. The loader "prefer animated GLB over procedural" lands in the procedural-fallback + optimize/stage boxes below (2) — **⏸ BLOCKER (sandbox): external paid AW job, not runnable in a sandboxed thread (only the 3 Meshy humanoid rigs exist in `assets-gen/animated/`). Loop/Floris owns this (§9c). The procedural-fallback box below makes the whole cast move + the game fully playable meanwhile; the mixer path is wired + dormant, ready to prefer the animated GLBs the moment they're staged.**
- [x] Procedural motion fallback for all animals; snake+frog procedural (slither/hop) (3)
  - [x] 29a · `render3d/ProceduralMotion.ts` — pure per-species gait recipes (breathe/hop/slither/flutter/paddle/still), `calmGate()` amplitude+speed clamp (never-scary §B / motion-comfort §C), `motionAt(recipe,t,phase)` deterministic delta; seeded unit test (bounds · slither-no-vertical · hop-arc-rests-never-frozen · still=identity) (1)
  - [x] 29b · wire into `World.ts` (replace ad-hoc idle bob with per-marker recipe by modelId; off under reduced-motion) + `Models.ts` "prefer animated GLB" hook (return clips; AnimationMixer when a rig exists, else procedural) (1)
  - [x] 29c · snake (adder) slither + frog (heikikker) hop + butterfly flutter verified against the §B AVOID lists; reduced-motion path checked (1)
    - **§9f AUDIT PASS** (build-green, 9/9 unit): adder = flat lateral weave, dy/rotZ/scaleY all 0 (no S-coil rear-up / no puff); frog = arc completes + rests each cycle, dy≥0 (never frozen mid-leap, never sinks), apex ≤0.16 m, airborne <50% of cycle (calm); squirrel/quadrupeds = slow breathe hz≤0.5 (no rapid tail-flick); raven/buizerd/nightjar = calm-perched breathe (no hackles/bill-up/gape modelled); butterfly flutter + duck paddle clamped by `calmGate()`. Reduced-motion freezes all secondary motion to REST (mixer `update(0)`); transform-only → zero draw-call impact. Never-scary + motion-comfort gates: PASS.
- [x] Optimize + stage every animated GLB; re-run finalize (1)

## Phase 4 — 3D world + in-world play  ◑ FIRST PASS done; immersion next
- [x] World first pass: procedural Veluwe, generated cast loaded, §1e damped-follow camera, tap-to-walk/walk-up-to-play
- [x] World build-out: THREE.Terrain biomes (heide/bos/stuifzand/ven), instanced vegetation/props, character controller (three-mesh-bvh), wayfinding + diegetic HUD (5)
  - [x] 38a · `render3d/Biomes.ts` — pure biome field: `biomeAt(x,z)` → heide|bos|stuifzand|ven (noise-warped sectors radiating from the lodge clearing, each guaranteed present), `heightAt(x,z)` continuous global relief + a smooth ven basin (no cliffs at sector boundaries), per-biome palette (ground/accent/vegetation kind). Seeded unit test (determinism · all 4 biomes present · no-cliff continuity · ven basin lowest · central clearing = heide) (2)
  - [x] 38b · Wire Biomes into `World.ts` — vertex-coloured ground by `biomeAt`, `heightAt` replaces local `groundY`, biome-aware scatter (pines→bos, heather→heide, marram/grass→stuifzand, reeds + a calm water plane→ven); markers anchored in their mission's `landschap` biome; keep draw calls < 150 (1)
    - **§9f AUDIT PASS** (build-green, Biomes 7/7 unit): four landschappen radiate from the lodge clearing as noise-warped sectors (test proves all 4 present, none warped away); relief is one continuous field (sum-of-sinusoids + a single Gaussian ven basin) — test asserts <0.5 m/m slope everywhere ⇒ **no cliff at any seam** (a wall would break never-scary); ven basin is the lowest ground + holds a **still** water plane (no waves → motion-comfort §1e); vegetation is biome-keyed + static (pines/heather/marram/reeds), only the animal markers move (procedural §29). Draw calls ≈ ground+water+4 instanced veg + ~10 markers ≈ **<45 (<150 ✓)**. Markers now anchored in their own `landschap` (mission biome via `m.landschap`). Never-scary + motion-comfort + tone gates: PASS. (Screenshot deferred to the Deep Demo capstone — the §5/§8b screenshot gate; no headless WebGL in-sandbox.)
  - [x] 38c · Character controller — kinematic capsule ground-follow + soft collision so the ranger can't walk through pines / into the ven / out of bounds (three-mesh-bvh or analytic clamp), tap-to-walk preserved, §1e camera untouched (1)
  - [x] 38d · Wayfinding + diegetic HUD — in-world marker labels + a calm direction/distance cue to the active mission (no minimap chrome), veldnotitie-style diegetic HUD; ≥56px, dual-channel, reduced-motion-safe (1)
    - **§9f AUDIT PASS** (World build-out complete · build-green · render3d 31/31 unit: Controller 8 + Wayfinding 8 + Biomes 7 + ProceduralMotion 9). **Controller** (`CharacterController.ts`, pure analytic clamp — NO new dep, deterministic + seed-tested): per-frame step is resolved by soft circular pushout around pine trunks (2 passes → slides, never stops dead = motion-comfort §1e), a circular world bound (no walking off the edge), and a no-wade predicate. **Bug caught + fixed in this audit:** the no-wade was a *global* height threshold, but `heightAt`'s dry rolling troughs reach ≈−2.2 m → that would have raised **invisible walls on dry land** (breaks never-scary). Fixed to a **ven-disc predicate** (inside the water circle AND below the surface only); added a regression test (`a dry trough … is NOT mistaken for water`). Facing follows the *actual* slid motion (natural turn, no snap). **Wayfinding** (`Wayfinding.ts`, pure): bearing is *relative to facing* (turn-left/ahead/right, never an absolute compass a child must rotate), cue is **dual-channel** (arrow glyph + M3/E3 words "naar links · 40 m" + the mission colour pip) rendered into a **veldnotitie strip** (no minimap chrome, ≥56px, `aria-live`, debounced so it only re-renders on word change; reduced-motion-safe = text swaps only, no transitions). **In-world labels** = camera-facing sprite name-tags (`depthWrite:false`). Draw calls ≈ ground+water+4 veg+ranger + per-marker ring/model/label ≈ **~55 (<150 ✓)**. Never-scary + motion-comfort + tone gates: PASS. (Screenshot deferred to the Deep Demo capstone — §5/§8b gate; no headless WebGL in-sandbox.)
- [x] 3D play harness: `render3d/engines/`, `WorldCtx`, `ViewMode` resolver, shared kit (pick3d/highlight3d/anchoredPrompt/spoorTrail/reframe) (3)
  - [x] 45a · Harness contracts + `ViewMode` resolver (pure) — `render3d/play/ViewMode.ts` (`resolveViewMode(req)→'2d'|'3d'`: 3D only when scene live AND WebGL-capable AND engine has a green 3D variant AND not force-2D AND motion-ok-or-rmSafe; else the 2D floor) + `render3d/play/types.ts` (`WorldCtx`, the `Play3dFn` contract, the `Play3dEngine` registry shape). Seeded unit test (reduced-motion→2d unless rmSafe · no-WebGL→2d · engine-not-ready→2d · force2d→2d · all-go→3d · deterministic) (1)
  - [x] 45b · Shared 3D interaction kit `render3d/play/kit.ts` — pick3d (raycast + ≥56px projected hit-sphere sizing), highlight3d (emissive colour + scale pulse, static under reduced-motion), anchoredPrompt (reuse the accessible DOM card), spoorTrail (instanced clue meshes along a spline), reframe/cut (§1e damped move or hard cut). Pure geometry helpers (hit-sphere world-radius for a target px, spline sampling) carry a unit test; build-green (1)
  - [x] 45c · In-place mission flow — build `WorldCtx` from the live `World`, branch `runMission` once on `resolveViewMode` (3D plays in-place, world stays loaded, no `leaveWorld`; else today's 2D path), proven with one diegetic engine adapter; 2D fallback unchanged; build-green (per-engine parity tests land in the Phase-II variant boxes) (1)
    - **§9f AUDIT PASS** (3D play harness COMPLETE · build-green tsc+vite · render3d 19/19 unit ViewMode+kit-math). `World.ctx()/beginActivity()/endActivity()` expose the live scene/camera/raycaster/canvas as a `WorldCtx` and freeze the world in-place (movement·walk-taps·proximity·wayfinding·§1e follow all pause so the activity's reframe owns the camera; `endActivity` snaps the follow back behind the ranger). `Missions.runMission` makes the **single §1f branch** on `resolveViewMode` (sceneLive=`!!world` · `WEBGL_OK` probed once · `REGISTRY_3D` · force2d=false until the Phase-6 Tweak) inside a `try/finally` so the world is never left frozen on a throw. **2D floor unchanged:** lodge-launched missions (world null → 2D), every engine without a shipped variant (corsi/simon/dagnacht/wisselen → `variantFor` undefined → 2D), and no-WebGL → 2D. First diegetic adapter **`render3d/engines/zoeken3d.ts`** is construct-faithful — reuses the frozen `buildZoekenTrial`, emits the identical `{trials:1,correct}` (correct=first-tap), changing only spatial layout (target+decoys on the heath patch) + input (raycast picks). Never-scary/motion-comfort: target "drukt zich" (still), dual-channel `Highlight3d` (emissive colour + calm scale breath, freezes under reduced-motion), §1e damped `makeReframe` (cuts under reduced-motion; dt-clamped), `rmSafe:false` ⇒ resolver serves the 2D floor under reduced-motion (no 3D camera move there); ≥56px `pick3d` hit-spheres (`visible:false` → 0 draw-call cost); all words + read-aloud in the accessible DOM card. Draw calls ≈ World ~55 + ~11 visible activity meshes ≈ **~66 (<150 ✓)**. The zoeken **seeded parity test** + tracking enrichment land in the Phase-II zoeken box (52). Never-scary + motion-comfort + tone + construct-parity gates: PASS. (Screenshot deferred to the Deep Demo capstone — no headless WebGL in-sandbox.)
- [x] Diegetic variant: dagnacht (+ seeded parity test) (2)
    - **§9f AUDIT PASS** (build-green tsc+vite · dagnacht parity 6/6 + ViewMode 10/10 + kit-math 9/9). **Construct parity by construction (§1f):** extracted the pure `DagnachtRun` core (sequencing + scoring, THREE-free/DOM-free/timing-free) into `engines/dagnacht.ts`; BOTH the 2D `render2d/DangerView` and the new diegetic `render3d/engines/dagnacht3d.ts` now drive it, so they emit an IDENTICAL `BeatSummary` (`trials`=encounter total, never shrinks on retries; `correct`=encounters never answered wrong). The seeded parity test cross-checks the core against an INDEPENDENT re-implementation of the frozen 2D rule over mixed/perfect/stubborn-retry/flip/determinism scripts. 3D changes ONLY the staging: the subject you meet on patrol is a calm STILL in-world form, the §1e camera reframes onto it (damped move; **cuts** under reduced-motion), the inhibition decision itself stays a two-choice pick in the accessible DOM card (reused `.choice-opt` ≥64px, dual-channel sound+toast+emissive, read-aloud wired, shuffle so slot never cues the answer, flip cue = text **+** colour). **Bug caught + fixed in this audit:** the wrong-choice "lean" first moved the subject toward +z = TOWARD the viewer (a lunge → breaks never-scary); fixed to lean −z (AWAY, camera sits at +z), small (0.3 m) + damped + instant-settle under reduced-motion. Never-game-over upheld (`run.choose(false)` re-presents the SAME encounter, recoverable). `rmSafe:true` justified — decision + every word live in the DOM card, so the resolver may serve 3D even under reduced-motion. Draw calls ≈ world ~55 + 2 subject meshes (no hit-spheres — choices are DOM) ≈ **~57 (<150 ✓)**. Never-scary + motion-comfort + tone + construct-parity gates: PASS. (Screenshot deferred to the Deep Demo capstone — no headless WebGL in-sandbox.)
- [x] Diegetic variant: corsi (+ parity test) (2)
- [x] Diegetic variant: simon (+ parity test) (2)
  - [x] 53a · Extract the pure `SimonRun` core into `engines/simon.ts` (growing-sequence sequencing + binary scoring, THREE/DOM/timing-free) + refactor the 2D `render2d/SimonView` to drive it; seeded parity test cross-checking the core against an independent re-impl of the frozen 2D rule (1)
  - [x] 53b · `render3d/engines/simon3d.ts` diegetic variant (callers staged as calm in-world forms; listen = per-animal glow+call, echo = raycast tap-back) + register + build gate + §9f audit (1)
    - **§9f AUDIT PASS** (build-green tsc+vite · simon parity 9/9 + ViewMode 10/10 + kit-math 9/9 = 28/28). **Construct parity by construction (§1f):** extracted the pure `SimonRun` core (growing-sequence sequencing + binary scoring, THREE/DOM/timing-free) into `engines/simon.ts`; BOTH the 2D `render2d/SimonView` and the new diegetic `render3d/engines/simon3d.ts` now drive it, so they emit an IDENTICAL `BeatSummary` (`trials`=1; `correct`=1 only when the whole growing 2→target sequence is echoed with zero wrong taps). The seeded parity test cross-checks the core against an INDEPENDENT re-impl of the frozen 2D rule over perfect/one-wrong/grow/complete/target-2/stubborn-retry/determinism scripts. 3D changes ONLY the staging: the dusk callers become calm STILL in-world forms (each a distinct earthy hue so WHICH animal is the memory cue, exactly the 2D tokens) + a camera-facing name tag; "listen" lights each caller in turn with its REAL call (`Sound.call`); "echo" is a ≥56px raycast tap-back via the shared `pick3d`. Never-game-over upheld — a wrong tap re-lists the SAME sequence (`'replay'`), a full sub-echo GROWS it (`'grow'`), recoverable. Never-scary/motion-comfort: forms only lift ≤0.16 m + scale ≤1.14 (no lunge/pulse), glow is a warm light, §1e reframe **cuts** under reduced-motion + lift is an instant settle there. Dual-channel everywhere (emissive COLOUR + SCALE + call/step sound); all words + read-aloud in the accessible DOM card. `rmSafe:false` — a flat named row reads cleaner than a 3D perspective for sequence-memory under reduced-motion, so the resolver serves the proven 2D floor there (matches corsi3d). Draw calls ≈ world ~55 + 4 callers × (body+head+label) ≈ **~67 (<150 ✓)**; hit-spheres `visible:false` → 0 draw cost. Never-scary + motion-comfort + tone + construct-parity gates: PASS. (Screenshot deferred to the Deep Demo capstone — no headless WebGL in-sandbox.)
- [x] Diegetic variant: zoeken (+ parity test) + tracking enrichment (spoor→kijker→wildcamera→case-board) (3)
  - [x] 57a · Construct-parity for zoeken — extract the frozen scoring rule `scoreZoeken(misses)` into `engines/zoeken.ts` (both 2D `ZoekenView` + 3D `zoeken3d` drive it = parity by construction) + add the SEPARATE tracking axis `buildSpoor(diff)` → `{legs,helderheid}` (new `spoorLengte`/`spoorHelderheid` knobs in Settings/DifficultyKnobs/knobsForLevel, scaled off the zoeken level but never touching the decoy set). Seeded parity test: builder trims decoys by difficulty + target stable + lensSterkte passthrough; **spoor axes never change decoys/target** (separate-axis invariant); scoreZoeken matches an independent reference; determinism (1)
  - [x] 57b · Tracking enrichment in `zoeken3d.ts` — `spoorTrail` clue legs from the activity spot to the hide zone (legs/faintness from `buildSpoor`; reduced-motion = static glowing markers, no walking) → the search trial UNCHANGED in a **kijker (verrekijker)** vignette → win card "vastgelegd op de wildcamera" (the case-board feed is the existing mission-completion data gate). Build gate + §9f audit (2)
- [x] Diegetic variant: wisselen (+ parity test) (2)
  - [x] 60a · Extract the pure `WisselRun` core into `engines/wisselen.ts` (set-shift sequencing + flip cadence + binary scoring, THREE/DOM/timing-free) + refactor the 2D `render2d/WisselView` to drive it; seeded parity test cross-checking the core against an independent re-impl of the frozen 2D rule (1)
  - [x] 60b · `render3d/engines/wisselen3d.ts` diegetic variant (current animal staged at the spot; the two destinations — open plek + het hol — stay visible+labelled = rule-application not spatial memory; flip cue = signpost colour + banner text) + register + build gate + §9f audit (1)
    - **§9f AUDIT PASS** (build-green tsc+vite · wisselen parity 8/8 + ViewMode 10/10 + kit-math 9/9 = 27/27). **Construct parity by construction (§1f):** extracted the pure `WisselRun` core (set-shift sequencing + flip cadence + binary scoring, THREE/DOM/timing-free) into `engines/wisselen.ts`; BOTH the 2D `render2d/WisselView` and the new diegetic `render3d/engines/wisselen3d.ts` now drive it, so they emit an IDENTICAL `BeatSummary` (`trials`=1; `correct`=1 only when the whole queue is sorted with zero wrong taps). The seeded parity test cross-checks the core against an INDEPENDENT re-impl of the frozen 2D rule over perfect/one-wrong/stay-on-wrong/flip-cadence/complete-only-on-last/stubborn-retry/determinism/builder-knob scripts. **No cross-construct contamination (§1f, explicit):** both destinations (open plek + het hol) stay VISIBLE + LABELLED the whole activity and the ranger never walks to reach one — picking which labelled place the live rule sends this animal to is **rule-application, not spatial memory**. 3D changes ONLY the staging: the animal-to-sort stands at the spot as a calm still earthy form (day=warm / night=muted hue), the two places sit either side (warm sun-disc vs calm den mound), the signpost between carries the rule. Never-game-over upheld — a wrong place is a small damped wiggle + "Bijna! Probeer de andere plek." and the SAME animal stays up (`run.choose`→`'retry'`). Never-scary/motion-comfort: a correct sort glides the animal sideways (never toward the +z camera, no lunge) with a ≤0.18 m hop that **settles instantly under reduced-motion**; the flip is dual-channel (signpost re-colours dusk-violet + turns AND the DOM banner swaps to "Nu andersom!" + soft cue) — no startle; §1e reframe **cuts** under reduced-motion. Dual-channel everywhere (place COLOUR + glide/settle + sound; ≥56px `pick3d` hit-spheres, `visible:false`→0 draw cost). All words + read-aloud in the accessible DOM card. `rmSafe:false` — two clearly-labelled bins on the flat 2D floor read cleaner than a 3D perspective for a rule-switch task under reduced-motion, so the resolver serves the proven 2D floor there (matches corsi/simon). Draw calls ≈ world ~55 + open(disc+sun+label) + hol(disc+mound+label) + signpost(post+board+label) + animal(body+head+label) ≈ **~67 (<150 ✓)**. **All 5 EF engines now ship a construct-faithful in-world variant.** Never-scary + motion-comfort + tone + construct-parity gates: PASS. (Screenshot deferred to the Deep Demo capstone — no headless WebGL in-sandbox.)
- [x] Immersion layer: veldnotitie entry, continuous patrol, case-board wiring, light world-EF seasoning (3)
  - [x] 64a · Continuous patrol return + diegetic veldnotitie mission entry — track from-world vs from-lodge launch; from the world the briefing reads as a lighter *veldnotitie* field-note (not a mission-control screen) and finishing a mission returns to the **world** (keep it loaded, re-point wayfinding to the next undone mission via `setActiveMission`, resume free-roam) instead of the lodge; lodge-launched flow unchanged. Pure `nextPatrolTarget(missions, voltooid)` selector + seeded unit test (1)
  - [x] 64b · Case-board wiring into the world — reach the prikbord from the explore HUD over the live world (no teardown) + a diegetic "vastgelegd op de wildcamera → prikbord" beat after a verhaalHaak mission completes in-world (the clue data gate already exists); returns to patrol (1)
    - **§9f AUDIT PASS** (build-green tsc+vite · patrol 10/10). **Prikbord over the live world:** a second `.ra-pill` ("📌 Prikbord" + clue-count) in the explore HUD opens `showCaseBoard(true)` — `card()` clears only DOM overlays, never `leaveWorld()`/`world.dispose()`, so the Stage keeps rendering the loaded world behind the card; the case-board back + ontknoping now thread `fromWorld` so they return via `resumePatrol()` (world stays loaded, wayfinding re-points) instead of the lodge. **Bug caught + fixed in this audit:** the lodge `lodge-prikbord` handler passed the click *event* straight to `showCaseBoard`, so adding a `fromWorld` param would have made every lodge open read a truthy MouseEvent as `fromWorld` → wrapped to `() => showCaseBoard()`. **Diegetic wildcamera beat:** a verhaalHaak mission finished IN the world routes reward → `showWildcamCapture` (a calm still snapshot of the new aanwijzing) → prikbord or straight on; both ends land back on patrol. Gated on **board-newness** via the pure `capturedClue(mission, fromWorld, alreadyFound)` (found-set snapshotted BEFORE `markMissionDone`) — so a replay AND the two missions sharing the `band` hook never re-announce a clue already pinned; lodge-launched missions get no in-flow beat (the prikbord is one tap from the hut). Never-scary (still snapshot, calm copy, no lunge), dual-channel (clue glyph shape + found-green colour + words + read-aloud), reduced-motion-safe (pure DOM card, no 3D camera move), ≥56px (pills bumped 48→56px), <150 draw calls (no new 3D meshes — world unchanged behind the overlay). Never-scary + motion-comfort + tone gates: PASS. (Screenshot deferred to the Deep Demo capstone — no headless WebGL in-sandbox.)
  - [x] 64c · Light world-EF seasoning — one calm open-world EF micro-beat during patrol (impulse-resist "blijf op het pad" / wayfinding-memory), light + skippable, never-scary, dual-channel, ≥56px; pure logic + test (1)
    - **§9f AUDIT PASS** (build-green tsc+vite · worldbeat 9/9 unit). Pure `core/worldbeat.ts` (THREE-free/DOM-free/deterministic) owns the decision: WHEN a beat fires (light cadence — only every `WORLDBEAT_EVERY=3`rd **completed in-world mission**, never tick 0, deduped to one per cycle via `lastBeatTick`), WHICH of two flavours (alternating impulse-resist `blijf-op-pad` first, then wayfinding-recall `welke-kant`), and WHICH option is calm-correct. **welke-kant suppressed when there's no heading** (everything done → falls back to impulse) and scores against the **live bearing** to the next marker via the new sync `World.headingTo(id)` → `coarseHeading()` (folds the 5 wayfinding phrases to links/rechtdoor/rechts, nulls "achter je"/"je bent er"). **Never-scary:** a curious animal merely hops off the path, calm copy ("Blijf jij rustig op het pad?"); **never game-over** — a wrong tap = soft `Sound.tryAgain` + "Bijna! Blijf rustig — probeer nog eens." and the SAME beat stays up; "Even verder lopen" always walks on. **Dual-channel** everywhere (border COLOUR + SCALE + per-option shape GLYPH + words + `Sound.found/tryAgain` + read-aloud); reduced-motion drops only the scale/transition (colour+glyph+words+sound carry it), and the beat is a pure DOM card over the **still-loaded** world (no 3D camera move, no new meshes → draw calls unchanged <150). **≥56px** (`.wb-opt` 64px). The five named mini-games stay the explicit core; this only dusts ~3 calm beats across a full 10-mission run. Never-scary + motion-comfort + tone gates: PASS. (Screenshot deferred to the Deep Demo capstone — no headless WebGL in-sandbox.)
- [x] **AUDIT** (§9f): construct-parity all green, never-scary/motion-comfort/tone, <150 draw calls, screenshots
    - **§9f PHASE-4 STAGE AUDIT PASS** (build-green tsc+vite · **full suite 126/126 unit**). **Construct-parity all green:** all 5 EF engines ship a seeded parity test cross-checking the extracted pure core against an independent re-impl of the frozen 2D rule — zoeken 9/9 · corsi 7/7 · dagnacht 6/6 · simon 9/9 · wisselen 8/8 — and each 3D variant drives that SAME core, so the 2D twin and the diegetic in-world variant emit an identical `BeatSummary` by construction (§1f). ViewMode 10/10 + kit-math 9/9 prove the single §1f branch (3D only when scene-live + WebGL + parity-green variant + not force2d + motion-ok-or-rmSafe; else the 2D floor) and the ≥56px projected hit-sphere math. World layer green: Biomes 7/7 (no-cliff continuity, 4 landschappen present, still ven) · CharacterController 8/8 (soft pushout, ven-disc no-wade, no off-edge) · Wayfinding 8/8 (facing-relative cue, dual-channel) · ProceduralMotion 9/9 (calm-gated gaits, reduced-motion→REST). Immersion green: patrol 10/10 + worldbeat 9/9. **Never-scary / motion-comfort / tone:** every variant's own §9f note verified its calm-pose staging (still forms, no lunge — wrong-choice leans go AWAY from the +z camera), §1e damped reframe that **cuts** under reduced-motion, dual-channel feedback (colour + scale + glyph/sound + words + read-aloud), and the never-game-over re-present loop; the world-EF seasoning stays light (~3 calm skippable beats / 10-mission run). **<150 draw calls:** per-variant budgets logged ~57–67 (world ~55 + a handful of activity meshes; hit-spheres `visible:false` → 0 cost), the world build-out ~55, the case-board/wildcam/worldbeat overlays add no 3D meshes. **Screenshots:** deferred to the Deep Demo capstone (the §5/§8b screenshot gate) — no headless WebGL in-sandbox, the standing convention for this run. Phase 4 (3D world + in-world play + immersion) gates: PASS.

## Phase 5 — expression, eyes, reduced-motion
- [x] Eye shader (catchlight/clearcoat/parallax; per-species pupil; dusk eyeshine list) (2)
    - **§9f AUDIT PASS** (build-green tsc+vite · **Eyes 8/8 unit · full suite 134/134**). Pure `render3d/Eyes.ts` (THREE-free/deterministic, the kit-math split) owns the §1e spec + maths; `render3d/EyeMaterial.ts` realises it in THREE; `World.ts` wires it onto the ranger + every animal marker after `prepModel` (best-effort like Models — a model with no recognisable eye mesh is left untouched, 0 dressed, never throws → game stays playable). **§1e delivered, gated never-scary:** clearcoat cornea (clearcoatRoughness pinned in the wet band [0.05,0.15] — test) over a CPU `CanvasTexture` iris (no GLSL to mis-compile headless); **upper-quadrant catchlight** (test asserts `v>0.5` on every species, a spark not a flare); **per-species pupil** (fox+adder vertical-slit, ree+edelhert+zwijn horizontal-bar, default round — test) drawn as a shape-scaled ellipse; **one-mesh iris parallax** = a clamped camera-driven uv offset (`irisParallax` pure: bounded to ±maxParallax, zero head-on, opposite the view, **frozen at depth 0 under reduced-motion** — test) via `onBeforeRender` (texture-offset write only → ~0 draw-call cost, no new meshes). **Dusk eyeshine list is EXACTLY §1e** (test: ON fox/edelhert/ree/das/nachtzwaluw/heikikker; OFF eekhoorn/adder/butterfly; birds default OFF, unlisted mammals ON). **Never-scary:** eyeshine is (a) gated OFF in the golden-hour world (`dusk:false` — bright-alive eyes, no glow), (b) one warm amber `#caa45a` (never cold predator-green), (c) clamped ≤0.45 emissive — the system is built + tested + dormant, ready for a genuine night scene. Motion-comfort: parallax is the only motion and it freezes under reduced-motion. **<150 draw calls:** no new meshes (material swap + texture-offset only). Never-scary + motion-comfort + tone gates: PASS. (Screenshot deferred to the Deep Demo capstone — no headless WebGL in-sandbox, the standing convention.)
- [ ] ARKit-subset face system (data-driven emotion recipes) + animal calm-pose set (3)
- [ ] Full 3D reduced-motion mode (per-view cuts-not-moves) (2)
- [ ] **AUDIT** (§C.5 QA checklist)

## Phase 6 — accessibility + audio + Tweaks
- [ ] M3/E3 copy pass; read-aloud + karaoke word-clock (iOS-safe) (3)
- [ ] ≥56px / dual-channel audit; Tweaks panel wired; ambient audio layer (2)
- [ ] **AUDIT** (a11y + tone word-lint)

## Phase 8 — Astro integration → alvah.nl/ranger  (now in run scope, §9g)
- [ ] Re-wrap app under `/ranger` route behind the access gate (`alvah-gate-v1`); robots Disallow; no trackers (3)
- [ ] Migrate localStorage `ranger-mvp-state` → `alvah-ef-v1`; reuse `src/scripts/` where it fits (2)
- [ ] Demo-skip options: jump to any area/mission/engine, skip briefings, fast-forward the arc (2)
- [ ] Deploy — **direct commit + push to main after every phase** (Floris approved; auto-deploys via the Astro GitHub Action); robots stays Disallow (1)
- [ ] **AUDIT** + full end-to-end pass + complete unit run (the ONLY full E2E, §9e)

## Capstone
- [ ] Deep Demo built (boot→avatar→free-roam→meet realistic ranger+animals→hear voice→play 5 engines in-world→arc+companion+badges) (2)
- [ ] **Demo Sandbox** — compact one-scene showroom: every character (calm-posed, real call) + one of each interaction (5 EF activities · companion · case-board · avatar · badge · arc beat), instant jump-menu, **prefer AW-animated GLBs where staged else procedural**; see [DEMO-SANDBOX.md](DEMO-SANDBOX.md). Tier-1 Charactershowroom (`app/showroom.html`) already built (2)
- [ ] Auto-QA report (draw calls, a11y, persistence, tone lint) + screenshots → Floris review (1)

## Human tail (post-demo, non-blocking — NOT the run's job)
- [ ] 4 morph-creatures (adder slither, frog hop + metamorphosis, antler morph) — Blender
- [ ] Alvah's exact-face likeness preset — Blender
- [ ] 10-min iPad/Alvah playtest (motion-comfort, real-word feel, difficulty) → fine-tune list
