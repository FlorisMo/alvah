# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# generated 3D assets (kept on disk; out of git to avoid binary bloat)
assets-gen/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# public/{audio,models,draco}: now TRACKED — the optimized ~25MB cast is the deploy
# payload; CI's build:site reads them to emit /ranger. (Raw 790MB intermediates in
# assets-gen/ stay ignored.)

# Playwright browser-proof harness (WORLD-PLAN §3.1): bulk artifacts stay out of
# git; curated proof screenshots live in games/Ranger-Adventures/qa-evidence-2/.
test-results/
playwright-report/
e2e/__shots__/
# Optional Meshy/Mixamo ingest drop (W3.1) — raw GLBs never committed here.
incoming/
