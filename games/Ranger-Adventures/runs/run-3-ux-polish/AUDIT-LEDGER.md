# Run 3 · Run A — AUDIT LEDGER (Fable works down this list)

> The supervisor (`audit-run-loop.sh`) does the FIRST unchecked `- [ ]` box each
> sitting. Read [AUDIT-PLAN.md](AUDIT-PLAN.md) first. One box = look at that
> capture group on BOTH platforms → write findings into
> [AUDIT-FINDINGS.md](AUDIT-FINDINGS.md) → tick the box. Stop after 1–2 boxes.
> Every finding follows the §3 schema (defect · platform · evidence · severity ·
> concrete fix · verify-by · confidence · contract-check).

## Capture groups to critique

- [x] A1 · **Boot** (title + avatar) — first impression, avatar-maker; is the ranger appealing, text readable? `audit-evidence/*/01-title, 02-avatar` → F-01..F-04 (clipped avatar card / sub-56px targets / 8-word line / bare backdrop)
- [x] A2 · **Wereld / world-entry** — THE camera-framing + avatar-scale finding (#1). Clipped inside the character? feet-only? `03-world-entry` → F-05 (blocker: camera fully inside mesh, world invisible), F-06 (16-word toast + 3 hints at once)
- [x] A3 · **Lopen (walk burst)** — gliding vs walking (#2). Use per-frame Δpos + `clip` in the annotation: pos moves while clip is null → gliding. `04..08-walk-1..5` → F-07..F-12 (giant avatar scale / near-jeep foot speed + stride slip / spawn faces void / gait-GAP re-judge after camera fix / no world boundary / half-screen shadow band). Clip `walk` binds + articulates on both platforms — the run-2 null-clip glide signature is gone in data.
- [x] A4 · **Besturing / controls HUD** — laptop vs iPad. iPad: joystick present, ≥56 px, tap-to-walk hint? laptop: what control is shown? `09-controls-hud` → F-13..F-15 (joystick covers tracker chip on iPad / Pauze chip <56 px both platforms / hint persistence inverted + bare laptop HUD); F-06 amended: the 16-word toast never dismisses. Joystick itself passes (~100+ CSS px, persistent, correct corner).
- [x] A5 · **Laptop-camera** — trackpad zoom + drag orbit ATTEMPTS (#4). Judge the before/after stills + `cameraYaw` unchanged → controls absent. (laptop only) `10..12-camera-*` → F-16..F-18 (zoom dead: frames 09/10/11 pixel-identical / drag doesn't orbit — it moves the player 0.93 m + flips facing 180°, tracker naar links→rechts / dev-hook cameraYaw+drawCalls changed between pixel-identical frames — telemetry decoupled from render; A9 must NOT trust cameraYaw alone). NB the box's own inference rule inverted: yaw DID change while controls do NOT exist — pixels, not the hook, settled it.
- [x] A6 · **Missiebord** — approach affordance + open board: layout, legibility, tap-target size. `board-affordance, board-open` → F-19..F-23 (board edge-on in avatar murk / exit is an 18 px text link / iPad GAP: capture dies before board+mission+reduce-motion / world label clipped by geometry / text-only cards). Positives: "Bekijk het missiebord" button 270×62 ≥56 ✓; the open overlay is the best-designed surface of the set. iPad half is ALL GAP (F-21).
- [x] A7 · **Missie (3D in-place)** — how a real mission looks; framing, prompt overlap, readability. `mission-3d` → F-24..F-26 (mission = floating props in the void, board from behind, no ground / prompt: 2 sentences in 2 sizes on one 11-word line + speaker ~46 px / HUD stripped in-mission: no Pauze, no exit at all). Positive: read-aloud speaker affordance EXISTS in the prompt bar. F-22 amended (label clip recurs in-mission). iPad: GAP (F-21).
- [x] A8 · **Pauze / menu** — is there a clear "back / hoofdmenu" path (#5)? `pause-hub` → F-27..F-29 (NO hoofdmenu/title path — structural, code-confirmed: zero exits below the title screen when combined with F-26 / four destinations are 16 px fine-print links — hit area IS 56 ✓, the affordance fails / no scrim: modal floats on F-07/F-09/F-12 murk). Positives: resume button 294×62 ✓ and correctly the visual primary; all strings ≤7 words; narrator stops on pause. F-20 amended (`ra-text-btn` min-height 56 — hit vs look).
- [x] A9 · **Jeep** — model clarity vs blur/DOF (#3, render) + steering across the drive burst (#3, dead steering): `heading`/`cameraYaw` unchanged with turn held. `jeep-near, jeep-in, jeep-drive-1..4` → F-30..F-33 (blocker: driving is blind — camera stays at the standing giant, yaw slaved to the jeep (camYaw≡heading+π in all 8 frames), 6/8 drive frames pure murk / the ranger never boards: the jeep circles EMPTY, clip stays idle / steering NOT frozen (heading turns continuously, doughnuts in a 10 m box) but unverifiable — no control-condition capture, iPad touch = GAP — and spin-top turn rate + instant max speed need comfort tuning / "blur/DOF" does not exist as an effect: no postprocess pass in render3d, the murk is 10–30× magnified avatar textures). NB the box's inference rule was again the wrong one: heading/camYaw DID change — pixels + veh data settled it, per F-18. Positives: jeep model very good, a HELICOPTER on a pad discovered (`crops/a9-heli.png`), Stap-in/uit buttons exactly ≥56 ✓ with speaker icon, tracker keeps working while driving. F-22 addendum: 2 more clipped labels ("Het s…", "…tille zanc…" = Stille zandverstuiving?).
- [x] A10 · **Reduce-Motion wereld** — with Verminder-beweging ON, does the world still look good or flat/broken? `reduce-motion-world` → F-34 (double GAP: zero RM pixels on EITHER platform. Laptop: deterministic harness bug — boot 2's first-run helper waits for the avatar-maker that `main.ts:90` skips once `avatarGemaakt` persists; iPad: F-21's crash cascade, and the boot() bug would have killed it anyway — two independent kill conditions. Even a successful single still can't prove the temporal §1e policy (cuts-not-moves, secondary freeze) → re-capture needs bursts + the in-game "Rustige beweging" toggle path. Feature exists in code with unit tests; VISUAL evidence is zero — the world-look question stays OPEN. F-01's GAP corroboration amended.)
- [x] A11 · **Synthesis** — group findings by fix-area, sort by severity, dedupe, number F-01… ensure all six FINDINGS.md punch-list items are covered, and write the "punch-list at a glance" table at the top of AUDIT-FINDINGS.md. → DONE: 34 findings (3 blocker / 21 major / 7 minor / 3 polish), numbering F-01..F-34 sequential with no dupes (candidate merge pairs kept separate + cross-referenced — each carries its own assert); all six punch-list items mapped (coverage list under the table); nine fix-areas A–I with the coupling/order view (scale→camera core first; verification substrate F-21/F-34/F-18 lands EARLY or Run B's gates are blind on iPad/RM). Table + legend + coverage + fix-areas written at the top of AUDIT-FINDINGS.md.
- [x] A12 · **Deep self-audit** — write the §Self-audit section per AUDIT-PLAN §6 (will it look+control right on both platforms? what's uncovered? coupled fixes? needs-demo items? build order?). → DONE: §Self-audit written at the bottom of AUDIT-FINDINGS.md. Verdict: laptop looks+controls right after the list IF the phase-1 reveal round is budgeted (the audit judged through the murk keyhole — the composed world has never been seen); iPad pending F-21's re-capture + the demo (Chromium caveat). Nine never-captured surfaces named — sharpest: ZERO touch input anywhere in the run (verified capture.spec.ts — all locomotion keyboard on both projects, boarding via Space; joystick never dragged). Coupling map (scale→camera→everything; F-05⊕F-18 together; chip token + F-01 as one change; substrate+evidence-archive before ANY re-capture). Ten +demo items grouped (comfort/input-feel/audio/Safari) with the "implemented — awaiting demo, never fixed" rule. Six-phase build order; highest-leverage fix = F-07 (~1.7 m normalize), landed as a pair with F-05, scale first.

## Notes
- If a group's capture is missing/GAP (harness didn't reach it), record that as a
  finding and tick — do not block the loop.
- Re-run the capture anytime from `app/`: `npm run capture`.
