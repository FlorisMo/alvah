# Run 3 · Run B — the OPUS BUILD (fixes the game, screenshot-in-the-loop)

> Master brief for the build sittings. The tickable checklist is
> [BUILD-LEDGER.md](BUILD-LEDGER.md); the punch-list it executes is
> [AUDIT-FINDINGS.md](AUDIT-FINDINGS.md) (Fable wrote it in Run A). The
> supervisor is `build-run-loop.sh` (Opus `--effort max`, with a Fable re-judge
> at each phase boundary). START by re-reading AUDIT-FINDINGS.md § Self-audit
> §5 (phase order) + §3 (coupling), then [FINDINGS.md](FINDINGS.md) (why run 3
> exists). This is the **hands** run — Run A was the eyes.

> **SCOPE — LAPTOP-ONLY automated verification (Floris, 2026-07-03).** The loop
> captures the **laptop** project only (`CAPTURE_PROJECTS=laptop`, set by
> `build-run-loop.sh`); the iPad leg hangs the software renderer before the
> jeep/board/mission/RM scenes (F-21) and re-stalls the loop. So: grade every
> finding on **laptop** pixels + asserts; make the shared-code fix for "both"
> findings; and treat **all iPad-specific / iPad-pixel verification** (F-13,
> iPad tap targets, touch steering, board-vs-joystick corner, real-Safari) as
> **demo-gated — Floris on the real device** (BUILD-LEDGER DEMO section).
> iPad auto-capture returns with `CAPTURE_PROJECTS=laptop,ipad` once the render
> hang is solved. This does not change any fix — only where it is *verified*.

## 0. What this run is (and is NOT)

Run A looked at the game and produced 34 findings. Run B **fixes them**, and —
this is the whole point — it does NOT grade its own homework the way run 2 did.
Run 2 shipped a giant avatar and a gliding capsule because its gate proved
*mechanics* ("position moved ≥2 m", "a clip named walk exists") and never looked
at the screen. Run B closes that with a **two-judge visual gate** (§8).

- **You change game code** under `app/src/**` — that is the job. But every fix
  is gated on a fresh rendered screenshot, not on the fact that the code
  compiles.
- **You may re-run and harden the capture harness** (`app/e2e-capture/**`) — it
  is the shared verification substrate.
- **You never touch or weaken the frozen `app/e2e/**` specs, the `@smoke`
  suite, or `playwright.config.ts`.** They are the regression guard.

## 1. The per-box gate (screenshot-in-the-loop)

For each work box, in order:

1. **Read the finding** in AUDIT-FINDINGS.md (defect, concrete fix, verify-by,
   contract-check). Make the fix under `app/src/**`.
2. **Expose what the assert needs.** Many verify-bys reference a dev-hook field
   (`avatar.height`, `cam.dist`, `cam.target`, ground speed, quaternion yaw,
   same-frame `drawCalls`). Add those to the dev hook as part of the fix, and —
   if the assert needs a new scene/input/idle-pair — extend the capture harness
   (`app/e2e-capture/**`, allowed) so its `annotations-<platform>.json` carries
   the evidence. This is how a finding's "E2E assert" is realised **without
   touching the frozen `app/e2e/**` tree**: the hardened capture harness writes
   the field, and you grade it. (If an assert is genuinely cleaner as a
   standalone spec, add a NEW file in a NEW tree — never edit `app/e2e/**`.)
3. **Re-capture:** from `app/`, `npm run capture`. This overwrites
   `audit-evidence/` with fresh PNGs + annotations for both platforms. (The Run
   A frames are safe — Phase 0 archived them to `audit-evidence-baseline-run-a/`.)
4. **Grade your OWN fresh screenshot** against the finding's visual criterion —
   actually LOOK at the PNG with the Read tool, on BOTH platforms where the
   finding says `both`. AND check the finding's named field/condition in
   `annotations-<platform>.json`.
5. **Regression gate:** tick via `node scripts/ranger-run.mjs tick "<needle>"`
   — it refuses the tick unless `npm run build` AND the frozen `npm run
   e2e:smoke` are green. (Set `RUN_LEDGER=runs/run-3-ux-polish/BUILD-LEDGER.md`
   — the supervisor exports it for you.)
6. **Tick ONLY if all pass** — own-screenshot + annotation assert + build +
   smoke. If your own grade fails, DO NOT tick; leave the box open, note why,
   and stop so the next sitting retries. A repeatedly-failing box self-pauses
   the loop for Floris (stall guard) — that is correct.

The supervisor commits + pushes once per phase (at the `GATE`), reusing
`ranger-run.mjs commit`. You do not commit inside a work sitting.

## 2. The HYBRID phase gate (AUDIT-PLAN §8, Floris 2026-07-03)

The builder does NOT close a phase alone. Each phase ends with a `GATE-Pn` box
run by an **independent Fable sitting** (`claude-fable-5`):

- Fable re-captures (a fresh set reflecting the whole phase), re-judges that
  phase's screenshots against every phase-finding's criterion, and either
  **ticks the GATE** (it agrees the phase's boxes are genuinely fixed) or
  **re-opens** one or more boxes (`[x]`→`[ ]`, gate left unticked) so the loop
  returns to Opus to redo them.
- **A finding is only "fixed" when BOTH agree** (builder's own capture AND
  Fable's re-judge) **and the E2E/smoke asserts are green.**
- **`needs-Floris-demo` items are never closed by either model.** They may reach
  "implemented — awaiting demo"; the FEEL/AUDIO/real-Safari verdict is Floris's
  on-device call only. The +demo findings are **F-06, F-08, F-10, F-15, F-16,
  F-17, F-25, F-30, F-32, F-34** (§ Self-audit §4). The DEMO section of the
  ledger is Floris-only; the supervisor pauses there with NEEDS-FLORIS.

## 3. Phase order (FROZEN — AUDIT-FINDINGS § Self-audit §5)

Do not resequence. Each phase ends with a both-platform re-capture + Fable
re-judge.

- **Phase 0 — protect + see (no game code):** archive the Run A evidence FIRST;
  F-21 harness hardening (scene-isolated pages, TOUCH-driven walkTo/steering on
  the iPad project, crash retry); F-34a `boot()` returning-player fix. Exit: a
  full both-platform capture set (board + mission + reduce-motion included, iPad
  driven by touch) exists.
- **Phase 1 — core (area A):** F-07 scale → F-05 camera + F-18 pose fields →
  F-09 spawn → F-08 speed/stride; re-check F-10/F-12/F-33; triage the expected
  new world-look findings before Phase 2.
- **Phase 2 — controls (B + C):** F-16 zoom, F-17 orbit (tap-to-walk seam
  asserted both ways); F-30 vehicle camera → F-31 boarding → F-32 steering
  asserts + comfort tuning.
- **Phase 3 — navigation + HUD (D + E + F):** one ≥56 px chip token
  (F-02/F-14/F-20/F-25/F-28 + F-01 card-fit); one navigation model
  (F-27/F-26/F-20); one hint system (F-06/F-15 + F-11-hint + F-13).
- **Phase 4 — reading + dressing (G + H):** F-03, F-25, F-22, F-23, F-04, F-11,
  F-29.
- **Phase 5 — RM + final sweep:** F-34b–d (both gates, burst pairs, cut-not-move
  assert), full re-capture, final Fable re-judge, then the Floris demo.

### Coupling rules (§ Self-audit §3 — respect them)

- **F-07 → F-05 → {F-08, F-09, F-16, F-17, F-19, F-24, F-30}.** Scale FIRST; no
  camera work before F-07 (F-05's clamp is defined off the avatar's radius,
  F-08's speed is giant-tuned). Anything tuned before the rescale is redone.
- **F-05 ⊕ F-18 land together** — the rebuilt rig must expose the REAL pose
  fields, or every downstream camera assert (F-16/F-17/F-30/F-32/F-34d) sits on
  telemetry that already lied once (pixel-identical frames, moving yaw).
- **F-33, F-12, F-10 are re-checks, not fixes** — expected to dissolve after
  F-07/F-05; spend fix effort only on survivors.
- **F-17 ↔ tap-to-walk seam** — the click-vs-drag discriminator must kill the
  orbit misfire WITHOUT breaking tap-to-walk; assert both sides.
- **F-30 reuses F-05's rig; F-31's fallback (visible driver vs hidden) sets
  F-30/F-31's verify criterion; F-32's asserts need F-30 + F-18 (+ P0.2 touch
  for iPad).**
- **One chip token: F-02 + F-14 + F-20 + F-25 + F-28, with F-01 in the same
  change** (bigger targets push the avatar card off-fold unless compaction ships
  with them).
- **One hint system: F-06 + F-15 (+ F-11 boundary hint + F-13 tracker move).**
- **One navigation model: F-27 + F-26 + F-20** — a rule, not three patches.
- **Substrate before gates: F-21 + F-34a + evidence archiving precede ALL
  re-captures** (`audit-evidence/` is git-ignored; the first capture destroys
  the frames the findings cite unless archived first).

## 4. The verification substrate (what the asserts read)

The capture harness boots the real game and writes, per shot, the live dev-hook
state into `annotations-<platform>.json`: `screen`, `pos`, `cameraYaw`,
`drawCalls`, `missionView`, `clip`, `veh` (heading/speed/inVehicle), `board`,
`vehicle`. Run B's fixes ADD fields (`avatar.height`, `cam.dist`, `cam.target`,
quaternion-derived yaw/pitch, same-frame `drawCalls`, ground speed) and, where
needed, ADD scenes/inputs (idle-stability pair, control-condition steering,
touch drive). You grade the finding's numeric criterion off those fields.

**Pixels are the court of appeal.** F-18 proved the hook can contradict the
render (yaw moved between pixel-identical frames). For every camera claim, the
screenshot outranks the field — if they disagree, trust the pixels and fix the
telemetry.

## 5. Frozen contracts (a fix that breaks one is NOT a fix)

Carry these into every change (FINDINGS.md / WORLD-PLAN §3.4 / root CLAUDE.md):

- **Motion-comfort camera law:** fixed FOV, roll 0, no head-bob / motion-blur /
  snap-rotate / FOV-kick / screen-shake; reduced-motion turns camera *moves*
  into cuts; locomotion itself always allowed. Any orbit/zoom/reframe must be
  player-initiated + damped.
- **Never-scary / never game-over**, calm-pose gate for animals. Leaving/stopping
  reads as neutral navigation — no "weet je het zeker?" guilt copy.
- **≥56 px tap targets; <150 draw calls; pixelRatio ≤ 2; iPad-first.**
- Persistence ONLY via `state.ts`/`persist.ts` in the `alvah-ef-v1` `ranger`
  namespace — **no new localStorage keys** (one-time-hint flags included).
- Reading M3/E3, ≤7 words per visual line, **read-aloud on new strings**.
- **No new dependencies** without Floris's OK. Assets via `assetUrl`
  (hand-drawn in-repo vectors need no pipeline). No surnames, no third-party
  runtime scripts. **Never print `.env.local` values.**

## 6. Hard prohibitions

- Do NOT touch/weaken `app/e2e/**`, the `@smoke` suite, or
  `playwright.config.ts`. New verification lives in the capture harness or a NEW
  separate tree.
- Do NOT resequence the phases or skip the F-07-first ordering (§3).
- Do NOT mark any +demo finding "fixed" — "implemented — awaiting demo" is its
  ceiling for any automated gate.
- Do NOT invent a pass. If your own screenshot fails the criterion, leave the
  box open and say so. Honest red beats a false green — that is the entire
  reason this run exists.
- Never commit `.env.local` or `assets-gen/`; never print secret values.

## 7. Build order recap + the single highest-leverage fix

**F-07 (normalize the avatar to ~1.7 m) is upstream of both blockers' geometry,
of F-08's speed, and of the murk behind F-12/F-19/F-24/F-29 and the whole F-33
"blur".** It converts five findings into re-checks and unveils the world every
later judgement needs. F-05 is the fix the player *feels* first; F-07 is the
root it hangs off. Land them as a pair, **scale first**, and re-capture before
touching anything else.

## 8. Run B log (append-only, one line per surprise)

- (add entries here as the build learns things about the harness, the engine, or a finding that turned out different once the murk cleared)
- **P0.1** — deliberately ran NO `npm run capture` (its verify-by is "nothing captured yet"; the first capture would overwrite the very frames F-01..F-34 cite — §3 substrate rule). Graded by folder-contents instead: baseline holds 22 laptop + 16 ipad + 18 crop PNGs + both annotations (byte-identical, `diff -rq` clean); source mtimes unchanged. Mirrored the existing `audit-evidence/*/` .gitignore rule onto `audit-evidence-baseline-run-a/*/` + its `index.html` so the supervisor's `git add -A` tracks only the annotations + README, not the ~56 heavy PNGs (honours the box's "git-add the annotations + a README").
- **P0.3 (F-34a) — GRADE FAIL, box left open.** The fresh 17:0x laptop capture stalled 30 min at the **board** group (`annotations-laptop.json` GAP: `locator.click: Test timeout of 1800000ms exceeded … waiting for locator('.mission-card')`) and never reached mission/reduce-motion, so NO fresh `reduce-motion-world.png` exists (newest on disk is 15:11, ~2 h stale; `find -newermt 17:00` → none). Verify-by ("laptop RM scene captures a PNG with no multi-minute stall") unmet on BOTH counts. The returning-player `boot()` fix may be correct but is untestable — the stall moved UPSTREAM to the board→open step (`.mission-card` never mounts). Next iteration must fix that board/mission-card wait (bounded timeout + returning-player-aware branch/retry) so the harness reaches RM at all.
- **GATE-P0 — PASSED (Fable re-judge, 17:5x capture).** Full laptop set exists: 23 shots 17:52:40→17:55:25 (2m45s, largest gap 55 s — the old 30-min `.mission-card` stall is gone via bounded 15 s presses in capture.spec.ts), all `ok:true`, no GAPs; board-open shows the mission-card grid, mission-3d + RM captured; baseline-run-a intact (22+16+18 PNGs + annotations + README, clean in git). CAVEAT for later judges: the capture does NOT clean `laptop/` — stale OLD-numbering leftovers (15-board-affordance/16-board-open/17-mission-3d/18-reduce-motion-world, mtime 15:2x) sit beside the fresh set where 15-18 are now JEEP frames and board/mission/RM live at 20-23; grade by mtime + annotations `file:` field, not filename memory. (Fresh 23-RM is byte-identical to stale 18-RM — expected: no game code changed in P0, deterministic spawn render; board/mission frames DO differ between runs.)
- **P1.1 (F-07) — GRADE FAIL (shot gate), box left open.** Assert is GREEN: `annotations-laptop.json` reads `avatar.height`=1.7 on every in-world shot (world-entry/walk1-5/controls-hud/jeep/board/mission/RM), in-band [1.5,2.0], and it's an HONEST posed-bbox measurement (`World.avatarState()` does `updateWorldMatrix(true,true)`→`Box3.setFromObject`, not the target echoed) — so the scale IS genuinely ~1.7 m and the old giant-boot-dwarfing-conifers in 06-walk-3 is gone (fresh cones read normal). BUT the shot criterion "ranger at believable proportion next to tree/hut" is UNCONFIRMABLE on the fresh laptop set: the still-open F-05 camera clips so close that 03-world-entry / 14-jeep-near / 20-board-affordance are pure reddish avatar-murk (hut/jeep/board fully occluded by the avatar's own body) and the yawed 09-controls-hud STILL shows the avatar torso arcing over the hut "like a bridge" — the exact frame the finding cited as the giant. Pixels don't contradict the hook (no F-18 lie — scale is real); they just can't show the proportion through the un-rebuilt camera. Per §0/§4 a shot-gated box can't tick on the hook alone. NEXT ITERATION: land F-05 ⊕ F-18 alongside F-07 (§7 "as a pair, scale first") so a composed frame shows ground+horizon+whole ranger next to a tree — then F-07's proportion shot is judgeable and both grade together.
- **P1.2 (F-05 ⊕ F-18) — FIX landed (code only; NOT captured/ticked/committed).** SURPRISE that changes P1.1's read: the "avatar-murk" is NOT the ranger. A headless three test confirms `Box3.setFromObject` measures a posed SkinnedMesh correctly (skin-aware `computeBoundingBox`), so `avatar.height`=1.7 is real AND the render is genuinely ~1.7 m — F-07 is honestly done. The murk is the FOLLOW BOOM burying itself in whatever solid stands behind the ranger at a POI: the spawn-hub props, the stuifzand dune by the parked jeep (see 15-jeep-in — dark mass = dune, not avatar), a tree behind the board. The clearing has NO scattered pines (candidates() skips r<12), so at spawn the boom sits among the +z hub dressing. FIX: `placeCamera` now (a) spherecasts the boom against the existing `obstacles` circles → pull-in, (b) lifts the lens over terrain, (c) clamps it outside the avatar radius, (d) FADES the ranger if it still collapses (a rig that ever renders larger than its honest height still can't smear the lens — §4). F-18: `cam{dist,yaw,pitch,x,y,z,target,avatarInView}` read off the REAL camera after the frame update (World.camState); drawCalls is now a same-frame post-render sample (Stage). CONSTRAINT: the frozen tap-to-walk @smoke taps 32 % from the top expecting ground, which needs ~16° downpitch — so the boom is eye 2.4 / boom 4.6 (F-05's lower/closer intent, clamped by the frozen contract), NOT F-05's literal 1.6–1.8 m / 4–5 m. Harness records `cam` + a `world-idle`/`world-idle-hold` pair for the ±0.01 idle assert. `npm run build` + frozen `@smoke` (4/4) + `camera.spec` (both) + `keyboard.spec` (both) all GREEN; the full-suite reds are parallel-software-WebGL overload (page crashes / 90 s timeouts) + pre-existing flakes (anim fails on the stashed baseline too). Supervisor: capture next; the shot (world-entry ground+horizon+whole ranger) + `cam.dist`≥3 + `avatarInView` + idle-stability are the grade.
- **P1.1 (F-07) RE-GRADE after the P1.2 camera land — GRADE FAIL (shot), box left open.** Assert GREEN: fresh `annotations-laptop.json` (25 shots, 19:43–19:46, RENUMBERED — world-entry=03, walk=06-10, controls-hud=11, jeep=16-21, board=22-23, mission=24, RM=25) reads `avatar.height`=1.7 on every in-world shot; camera fields now present + self-consistent (`cam.dist`≈4.85, `avatarInView`=true; the 03/04/05 idle triple is BYTE-IDENTICAL md5 → idle stable to ~6e-8, pixels not just the hook). WORLD scale is genuinely fixed — 08-walk-3 / 10-walk-5 show normal-size conifers on a real ground+horizon, the giant-boot-dwarfing-conifers signature is GONE, and the P1.1 "torso arcing over the hut like a bridge" is RESOLVED (11-controls-hud's brown mass is BILATERAL foreground terrain/foliage framing the hut at believable size, not an avatar). BUT the shot criterion "ranger next to tree/hut at believable proportion" is STILL unconfirmable: the F-05 boom keeps burying in the reddish spawn-hub/POI foreground (03-world-entry + 16-jeep-near are reddish DOF murk), at the hub (11-controls-hud / 14-camera-orbit) the avatar is FADED (P1.2 fade-on-collapse) leaving hut+terrain only, and the walk-frame centers are empty ground (edge masses flip sides 08↔10 → terrain, not the followed avatar). So the ranger itself is never shown at proportion — per §0/§4/§6 (pixels are the court of appeal; the height-field alone is the run-2 trap) this cannot tick. NEXT ITERATION: harden F-05 so world-entry composes ground+horizon+WHOLE ranger (stop the boom burying in the hub dunes / don't fade the avatar at spawn) — then F-07's proportion shot grades green alongside F-05.
- **P1.1 (F-07) RE-GRADE after the F-05 world-entry hardening (20:25 laptop capture, 25 shots) — GRADE FAIL (shot), box left open.** Assert STILL GREEN (`avatar.height`=1.7 on every in-world shot, `cam.dist`≈4.85 idle-stable across the 03/04/05 triple, `avatarInView`=true) and WORLD scale is genuinely fixed — 08-walk-3/10-walk-5 conifers, the 11/12/14 log cabin and the 16-jeep-near jeep all read at believable normal size; the giant signature is gone. BUT the shot criterion "ranger next to tree/hut at believable proportion" fails identically to the prior three grades: the F-05 boom KEEPS burying in reddish near-foreground DOF murk at spawn + POIs (03-world-entry, 22-board-affordance, 16-jeep-near background are pure reddish murk — the "compose the WHOLE ranger at world-entry" fix did NOT land), and at the hub (11-controls-hud/12-zoom-in/14-orbit) the avatar is STILL faded/absent (P1.2 fade-on-collapse still firing at the hub) leaving believable hut+terrain but no visible ranger; walk-frame centers (08/10) are empty ground with ambiguous DOF edge terrain. Per §4/§6 the height field alone is the run-2 trap and `avatarInView`=true is the hook, not the pixels — the ranger is never SHOWN at proportion, so no tick. NEXT ITERATION: F-05 must stop the boom burying (kill the reddish near-DOF at spawn/board/jeep) AND suppress the spawn-time avatar fade so 03-world-entry composes ground+horizon+WHOLE un-faded ranger next to the cabin — only then does F-07's proportion shot grade green.
- **⇒ MONITOR STEER (Floris + monitor thread, 2026-07-03, at the self-pause) — READ THIS BEFORE touching the camera again: the loop is iterating the WRONG lever.** Four straight P1.1 re-grades fail identically because `placeCamera` (World.ts ~2699–2731) FIGHTS ITSELF at the hero/POI shots: a solid (the cabin) or the stuifzand dune sits behind the spawn ranger along the fixed +z bearing (`FIXED_FOLLOW_YAW`=π), so (a) `boomClearFraction` collapses `dist` to `minFrac` → lens ~0.4 m off the coat = the reddish murk, and then (b) the fade rail (`near=r+0.3, far=r+1.1`) drives `avatarOpacity`→0 = the "hut visible, no ranger" frames. Hardening the pull-in/fade CANNOT yield the shot — by construction it hides the very subject the grade needs SHOWN. Stop hardening the boom; remove the CAUSE. **Land #1 first (deterministic, no camera math):**
  1. **Compose open ground BEHIND spawn.** Clear the ~5 m +z boom volume straight behind the spawn ranger: no cabin, no hub dressing, no dune inside it. Keep a landmark (cabin/tree) IN FRAME but OFF TO THE SIDE, with flat open ground + horizon directly behind. Then the boom never collapses, the fade never fires, and 03-world-entry naturally composes ground+horizon+WHOLE un-faded ranger next to a landmark — satisfying F-33 AND F-07's proportion shot together. Re-check the same at every POI the grader shoots (world-entry, board, jeep).
  2. **If a solid is genuinely adjacent, LIFT/ORBIT over it — never pull straight in + fade.** In `boomClearFraction` skip obstacles the ranger stands adjacent to (within ~`avatarRadius + o.r + margin`); respond to true occlusion by raising eye height + steepening pitch (clear OVER the prop), not by collapsing `dist`. A settled hero/POI frame must keep the whole ranger SOLID (`avatarOpacity`==1).
  - **Machine signal (kills the blind retries):** expose the applied `avatarOpacity` on the dev hook and assert it ==1 on the world-entry/POI shots. If the fade fired, `opacity<1` predicts the shot-fail BEFORE the judge sees it — so the loop stops burning sittings on frames it can already know are empty.
- **P1.1 (F-07) — MACHINE SIGNAL landed (code only; NOT captured/ticked/committed); the steer's boom/fade mechanism is DISPROVEN by the 20:25 data.** Exposed `avatarOpacity` on the dev hook per the steer (`CamState.avatarOpacity` in devhook.ts, returned from `World.camState()`, typed into the capture harness so `cam.avatarOpacity` lands in `annotations-laptop.json`). `tsc` clean; frozen `e2e/**` + `@smoke` untouched (the harness carries its own mirror type). **The surprise that redirects the next iteration (verified against the 20:25 capture per §4 — pixels/data outrank the hypothesis):** the boom is NOT collapsing and the fade is NOT firing at any hero/POI shot — `cam.dist` reads **4.85 / 4.65 / 4.75 / 4.61** at world-entry(03) / hub(11) / jeep(16) / board(22), all `avatarInView:true`, so `avatarOpacity` will read **1** everywhere (band `near=r+0.3, far=r+1.1` needs `hd<~1.6` to fade; `hd≈4.6`). Steer point 1 ("clear the +z boom volume behind spawn") is ALREADY satisfied — cabin is at (−6,−9), board (3.6,2.6) + BOA (6.4,4.4) are ≥3.6 m off the x≈0 boom line (probe R+obstacle R ≈1.05 m), nothing collapses `dist`. Steer point 2 (anti-fade in `boomClearFraction`) is moot — no collapse to guard. **So the last four P1.1 shot-fails are NOT the boom/fade.** Pixel sampling: spawn-03 centre RGB=(43,30,18) is the ranger's own brown coat centre-frame (the sample window IS where a third-person avatar's body sits), on olive heide ground (walk-5/hub read (91,86,40) = `#8a9a55`); spawn is `heide`, flat (±0.13 m), never reddish. The real block on F-07's proportion shot is FRAMING: at world-entry the ranger stands front-on and centre with NO landmark IN the forward (−z) frustum for scale (cabin at (−6,−9) is 13.6 m deep + small; nothing beside him), and at POIs he is solid-but-small. That is **F-05 framing + F-09 spawn-orientation** work (per §3 "land F-05 alongside F-07; scale first"), NOT another World-geometry re-tune of a boom that is already healthy — so the next sitting should take **P1.2 (F-05 ⊕ F-18)** and turn the spawn to show the ranger's back beside a same-frame landmark, then re-grade F-07 with it. `avatarOpacity==1` in the next capture is the proof that closes the fade theory for good.
- **P1.1 (F-07) RE-GRADE — fresh 22:47–22:50 laptop capture (25 shots) — GRADE FAIL (shot), box left open.** Assert GREEN and the machine signal is now PROVEN from the fresh pixels: `avatar.height`=1.7 on every in-world shot (in-band), `cam.avatarOpacity`==**1** at world-entry(03)/idle(04)/walk(06-10)/controls-hud(11)/jeep(16) — the fade theory is DEAD as predicted — and `cam.dist`≈4.854 idle-stable across the 03/04/05 triple (Δ<1e-6, inside ±0.01), `avatarInView`=true. WORLD scale is genuinely fixed: 08/09/10-walk conifers + the 11-controls-hud log cabin read at believable NORMAL size, the giant-boot-dwarfing-conifers signature is GONE. BUT the shot criterion "ranger next to tree/hut at believable proportion" fails a FIFTH identical time — the ranger itself is never SHOWN at proportion: 03-world-entry + 04-world-idle are pure reddish near-DOF murk (boom still buried at spawn), 08/09/10-walk have empty green centres + an ambiguous reddish edge-mass reading as a dune (corroborated by height=1.7), 11-controls-hud has the believable cabin but reddish murk spanning the TOP where the ranger should read, 15-pause-hub is modal-occluded. Per §0/§4/§6 the height field alone is the run-2 trap and the pixels can't show the subject → no tick. This capture CONFIRMS the machine-signal + MONITOR STEER: the block is F-05 FRAMING + F-09 spawn-orientation (front-on spawn, no same-frame landmark, boom in near-DOF), NOT F-07 and NOT boom/fade. Next sitting must take **P1.2 (F-05 ⊕ F-18)**: compose ground+horizon+WHOLE ranger beside a same-frame landmark (turn the spawn to the ranger's back), then re-grade F-07 alongside it.
- **⇒ MONITOR STEER #2 (monitor thread eyeballed the 22:47 PNGs — READ before the next camera attempt).** The steer #1 machine-signal WORKED: fade is dead, scale is visibly done (cabin/pines normal). What the pixels now show — and text-grades kept blurring into "murk" — is a **camera-framing + spawn-placement bug, and the loop keeps grinding the wrong box (F-07 is DONE; the work is F-05 + F-09):**
  - **03-world-entry = the lens jammed INTO terrain** — the whole frame is out-of-focus brown DOF, no readable world at all. Ground-clearance lift is shoving the lens UP into the underside of a rounded dune/hill that sits right at spawn.
  - **11-controls-hud = a big dark dune/cliff mass hangs across the TOP of frame** (camera clipping under that same hill) and the ranger renders as a correct-but-tiny ~10 px dot lost in open ground — solid (opacity 1), just not framed.
  - **08-walk-3 = the follow-cam is NOT holding the ranger** — frame centre is empty grass; he's a distant speck near the horizon. The camera is tracking a point, not keeping the ranger's body in the lower-centre.
  - **Root, concretely:** the spawn (F-09) sits embedded beside/under a rounded stuifzand dune, so the boom has no clear volume and the ground-lift buries the lens under the dune; and the follow framing (F-05) doesn't keep the ranger in-frame. FIX BOTH TOGETHER: (1) **relocate spawn to genuinely open, flat ground** — a clear ~8 m radius, the cabin/a tree ~8–12 m OFF TO THE SIDE (same-frame landmark), open horizon behind; no dune within the boom volume or above it. (2) **make the follow-cam keep the WHOLE ranger in the lower-centre third** at a distance where his body AND the side landmark both read.
  - **New machine signal (catches "lost speck" before the judge, same idea as opacity):** project the ranger's world position to screen space and expose it (e.g. `cam.avatarScreen{x,y,onScreen}`); assert the ranger is ON screen and inside the central ~60 % on world-entry/walk/hub. A dot at the horizon or off-frame then fails the ASSERT — the loop stops burning sittings on frames a human can see are empty.
  - **Note to Floris (not the builder):** this box has failed identically 5×; the loop can't seem to crack the camera unaided. If steer #2 doesn't land it within ~3 sittings, this is the point for a hands-on camera/spawn fix rather than more autonomous spins.
- **P1.2 (F-05 ⊕ F-18) — FIX landed (code only; NOT captured/ticked/committed); STEER #2's dune/speck MECHANISM is DISPROVEN by the terrain code + honest telemetry + a browser-verified projection.** Three changes in `app/src/**` (+ an additive harness type), all serving the steers' "compose ground+horizon+WHOLE ranger beside a same-frame landmark, turn the spawn to the ranger's back": **(1) spawn back-turn** — the rig defaulted to `rotation.y=0` = facing **+z straight INTO the +z follow lens**, so 03-world-entry / 04-idle were a FRONT-ON, centre-frame ranger whose own brown coat filled the lens (THAT is the "reddish murk" — not a buried boom, not terrain). `this.ranger.rotation.y = Math.PI` (World.ts ~417) turns his BACK to the lens = the third-person frame the movement smoke already assumes ("the camera sits behind, looking forward"), and drops the 180° about-face the first forward press used to trigger. **(2) cabin (−6,−9)→(−4.2,−4)** — the same-frame SCALE landmark: ~10 m off the lens, off to the LEFT, still OUT of the +z boom volume + ~2 m clear of the x≈0 tap-to-walk lane, shadow still falls −z away; ZERO new draw calls (moved, not added). **(3) `cam.avatarScreen{x,y,onScreen,heightFrac}`** — STEER #2's OWN requested tool (devhook.ts + World.camState, wired through the capture annotation): the ranger's bbox projected to screen space so a grade PROVES he is framed instead of eyeballing a soft-DOF frame. **The disproof** (steer #2: "a stuifzand dune sits right at spawn, the ground-lift shoves the lens up into it" + "08-walk-3 = a distant speck near the horizon"): `Biomes.heightAt(0,0)=0`, smooth ±0.4 m across the whole spawn/walk area, NO per-biome height branch ⇒ **NO dune** (spawn is the flat `CLEARING_R=7` heide clearing; the −z walk is **bos**, not stuifzand — stuifzand is the NE +x+z jeep sector); the 22:47 telemetry reads `cam.y`=2.394 = **NO ground-lift** with `avatarInView`=true / `avatarOpacity`=1 at every hero/POI shot; and a LIVE browser read of the new signal at world-entry is `avatarScreen`={x≈0 (dead centre), y=−0.095 (lower-centre third), heightFrac=**0.32** (32 % of viewport height = NOT a 10 px speck), onScreen=true}. So the block was FRONT-ON coat + no near landmark + no objective framing signal — never a dune / boom / geometry bug. Verified: `npm run build` + frozen `@smoke` **4/4** + camera(2) / scale / landmarks / dressing / ground all GREEN; the avatarScreen read came from a throwaway spec in a NEW tree (deleted — `e2e/**` + `@smoke` + `playwright.config.ts` untouched; the capture harness gained only the additive `avatarScreen` mirror type). **Supervisor: capture next.** The grade is the world-entry / walk / hub shot (back-turned WHOLE ranger + cabin landmark off-left + ground + horizon) AND `cam.avatarScreen` (onScreen, |x|/|y|<0.6, heightFrac not tiny) — the objective proof that ends the 5× subjective murk/speck misreads; re-grade F-07's proportion alongside it.
- **P1.1 (F-07) RE-GRADE — fresh 23:27–23:28 laptop capture (25 shots, coherent set, annotations 23:29; NO stale leftovers — all 5 graded PNGs mtime-verified) — GRADE FAIL (shot), box left open, NOT ticked. And the machine signal STEER #2 added is itself DISPROVEN by the pixels (F-18 again).** Assert GREEN (`avatar.height`=1.7 on every in-world shot; scale genuinely fixed — 08-walk-3 shows normal-size conifers on real ground+horizon, 11-controls-hud a normal-size log cabin, giant signature GONE) and the 03/04/05 idle triple `cam.dist`=4.85412196 is stable to <1e-8 (±0.01). BUT the shot criterion "ranger next to tree/hut at believable proportion" fails a SIXTH identical time — the ranger is NEVER SHOWN at proportion: **03-world-entry + 22-board-affordance are full reddish-brown near-DOF murk** (no ranger, no cabin, no ground/horizon), **16-jeep-near is a near-black brown VOID** (no jeep, no ranger, no world — just the "Stap in de jeep" chrome), **11-controls-hud** has the believable cabin but a dark/brown mass hanging across the TOP and no readable ranger where the hook places him, and **08-walk-3 is a fully readable world whose CENTRE IS EMPTY GROUND** (no ranger anywhere). **The redirect for the next sitting: `cam.avatarScreen` — the signal STEER #2 introduced as the OBJECTIVE anti-murk proof — is ITSELF now pixel-contradicted (a NEW F-18 lie in a NEW field).** It reads `onScreen`=true, |x|/|y|<0.11, `heightFrac`=0.29–0.34 and `avatarOpacity`=1 on 03/08/11/16/22 — i.e. it CLAIMS a prominent, solid, centre-frame ranger at ~⅓ viewport height on exactly the frames whose pixels show empty ground (08) or murk/void with no ranger at all (03/16/22). Per §4 pixels are the court of appeal: the avatarScreen projection is lying, so it CANNOT certify the shot — it must be fixed to agree with the render before it can gate anything (the back-turn `rotation.y=π` + cabin-move fix did NOT compose the shot; the "reddish murk = front-on coat" theory is not what the fresh pixels show). **NOTE TO FLORIS (per the monitor's own §8 note): this box has now failed identically 6×, steer #2 did NOT land, and the very machine signal added to end the subjective misreads is itself pixel-contradicted — the autonomous loop cannot crack this camera/spawn framing unaided. This is the hands-on point: a human camera+spawn fix (compose ground+horizon+WHOLE solid ranger beside the cabin at world-entry, and make avatarScreen match the render) rather than more spins.**
- **P1.1 (F-07) — ROOT CAUSE FOUND + FIX landed (code only; NOT captured/ticked/committed). The 6× "murk" was NEVER the camera — it was the ranger rendering as a ~170 m GIANT, and `avatar.height`=1.7 was an F-18-class bind-pose LIE the whole run chased the wrong lever behind.** The prior sitting's own throwaway probes (`app/e2e-probe/probe2,3.spec.ts` + `/tmp/ranger-probe*`) already proved it via a tint test and never made it into §8: the player `char1` SkinnedMesh renders at **posedWorldBBoxY 169.96 m / skeletonWorldBBoxY 160.09 m** while `Box3.setFromObject` (what `avatarState`/`measureAvatar`/`camState` all read) measures only the **bind-pose GEOMETRY box = 1.7 m**. Mechanism (probe scaleChain): the rig is `char1`(1) → `Armature`(0.01) → `Scene`(**188.89**); the mesh geometry is 0.9 units (×1.889 worldScale = 1.7 m, the "honest" number), but the **bones are ~94× the geometry** (≈85 armature-units → ×0.01×188.89 = 160 m), so the bone-driven render is a 170 m giant whose dark coat filled the follow lens = the reddish-brown murk (center RGB≈(31,19,6); it recedes to the frame edge as he walks — 08-walk-3 right-mass, 10-walk-5 left-mass, 11-controls-hud top-mass — and re-buries at the jeep, 16-jeep-near). `prepModel` CAUSED the 188.89: it measured the collapsed bind box (~0.009 m native) and scaled `Scene` up 188.89× to force THAT to 1.7 m, dragging the skeleton to 160 m. Every F-05 boom/fade/spawn/back-turn/avatarScreen iteration was chasing a symptom; all of MONITOR STEER #1/#2's dune/boom/fade/front-on-coat theories are disproven (the boom was healthy, `cam.dist`≈4.85, opacity 1 — the giant just isn't measured by the bind box). **FIX (app/src/**):** new `Models.skinnedRenderBox()` measures the SKELETON's world extent (the size that renders, pose-independent — reliable at bind pose / on a detached clone / live); `prepModel` scales rigs off it (static props unchanged → geometry box); `World.avatarState`/`measureAvatar`/`camState` all read it, so `avatar.height` + `cam.dist`/`avatarInView`/`avatarScreen` finally reflect the render. **Verified (probe4, NEW tree, deleted-able; `e2e/**`+`@smoke`+`playwright.config.ts` untouched):** renderedPosedHeight **1.803 m**, skeletonHeight **1.700 m**, `avatar.height`=**1.700**, feet≈ground; a live browser screenshot shows a believable human-size ranger back-turned on ground+horizon with the log cabin off-LEFT as the same-frame landmark and normal-size conifers/signpost — F-07's proportion shot composes for the first time, and `cam.avatarScreen` (heightFrac 0.32, |x|≈0, y=−0.095) now AGREES with the render (the 6th-grade avatarScreen "lie" was also just the giant). `npm run build` + frozen `@smoke` **4/4** GREEN. **Supervisor: capture next.** The grade is world-entry (whole ranger + cabin + ground/horizon at believable proportion) + `avatar.height`∈[1.5,2.0] + `avatarScreen` matching the pixels; F-05's rig was fine all along, so re-check its shots fall out of the same capture. NB feet sit ~5 cm low (skeleton toe-bone re-seat) — invisible here, flag only if a later grade wants it tightened.
- **P1.2 (F-05 ⊕ F-18) — the whole `app/src` fix ALREADY LANDED coupled with P1.1 (§3/§7: "F-05 ⊕ F-18 land together, scale first"); this sitting adds the ONE unrealized element of F-18's concrete fix — the harness PIXEL COURT OF APPEAL (harness-only; NOT captured/ticked/committed).** Once the P1.1 root-cause `skinnedRenderBox` fix made the ranger honestly 1.7 m, F-05's rig composed — so P1.1's own commit (3fa7c88) already carries every P1.2 element, verified line-by-line in HEAD: the F-05 clearance boom (`camOffset` eye 2.4 / boom 4.6 — steeper than the finding's "few degrees" because the FROZEN tap-to-walk `@smoke` taps 32 % from the top and needs the horizon above that row; World.ts ~103), `boomClearFraction` spherecast push-in + `minFrac` clamp (`avatarRadius`+near+0.2) + `setAvatarOpacity` fade rail (~2718–2827), and F-18's `camState` read off `this.camera` AFTER the frame update — `dist` / quaternion `yaw`+`pitch` / position / whole-bbox-in-frustum `avatarInView` / `avatarScreen` / `avatarOpacity` (~1285) — plus same-frame `drawCalls` (Stage.ts ~152: `lastDrawCalls = renderer.info.render.calls` the instant after `render`) and the `world-idle`/`world-idle-hold` idle pair (capture.spec.ts ~217). Grade-relevant asserts check out on inspection: `cam.dist`≈4.85 (=hypot(4.6, 2.4−0.85)) ≥ 3; the whole 1.7 m box sits inside the 55° frustum at world-entry (head ≈−8.6°, feet ≈−27.5°, both within the +11.5°…−43.5° vertical span) → `avatarInView`=true; the idle pose is fully determined by static `ranger.position` + `followYaw`=π (snapped at spawn) → yaw/dist steady (the §8 grades already saw the idle triple stable to <1e-8). **The gap I closed:** F-18's concrete fix ends "…and the capture harness keeps **pixel-diff as the court of appeal** for every camera assert", but the harness emitted NO pixel signature — the §8 grades md5'd the idle triple BY HAND. Added `pixelHash` (md5 of each shot's PNG bytes via `node:crypto` — a Node built-in, NO new dep) to every annotation in `snap()`, so the `world-idle`/`world-idle-hold` pair reads IDENTICAL hashes when the pose is genuinely still — machine-proving the ±0.01 idle assert from the RENDER, not the hook that moved yaw between pixel-identical frames in Run A (§4). It also catches the inverse (a camera claim that "changed" while the hash holds = a telemetry lie), so future F-16/F-17 zoom/orbit asserts inherit a court of appeal too. Harness-only (`app/e2e-capture/capture.spec.ts`); frozen `e2e/**` + `@smoke` + `playwright.config.ts` untouched; esbuild bundles the spec clean (the full `tsc` capture-check wants `@types/node`, absent in this sandbox but a pre-existing transitive dep the file already required via `node:fs`/`node:path`). **Supervisor: capture next.** Grade = world-entry shot (ground+horizon+whole ranger) + `cam.dist`≥3 + `avatarInView`=true, AND `world-idle` vs `world-idle-hold` with `cam.yaw`/`cam.dist` within ±0.01 **and now their `pixelHash` identical** (the render-side proof); F-05's boom/fade/pose all fall out of the same capture.
- **P1.3 (F-09 · rotate the spawn toward the hub) — FIX landed (code only; NOT captured/ticked/committed).** The titular defect survived P1.1/P1.2: the spawn faces −z with the cabin already forward-left (moved there for the F-07 proportion shot), but the **mission board — the actual content hub — sat at (3.6, 2.6) = +z, straight BEHIND the back-turned ranger and off the world-entry frame**, so holding forward walked AWAY from the hub into the heath (F-09's "hub sits behind the player" verbatim). Rotating can't fix it (a +z object can't enter a −z frame without moving), and the FROZEN tap-to-walk `@smoke` pins the −z/π bearing + downpitch — so per the finding's own alt ("or move the hub into the current sightline") I **moved the case-board to (2.2, −5)**: forward (−z), off to the RIGHT, a mirror of the cabin's forward-LEFT (World.ts ~1631). Now world-entry composes **cabin (left) + Missiebord halo/label (right) + the bos tree line ahead** (`candidates()` keeps forest ≥12 m out, so 0–12 m is open clearing → a real tree line at depth), and the first stride heads TOWARD content. Placement is contract-safe: its r=0.7 circle spans x∈[1.5,2.9] (OFF the x≈0 tap-to-walk lane), ~1.3 m clear of the spawn→watchtower diagonal the `paths` @smoke walks, 5.5 m out (> the 2.4 m board proximity, so the hub prompt does NOT fire at spawn), and inside `candidates()`'s r<12 clearing skip (no tree clips it). **Assert (F-09's "dev-hook landmark-in-frustum boolean"):** added `cam.landmarkInView` (devhook `CamState` + World.camState ~1333 + the capture mirror type) — reuses the live frustum already built for `avatarInView` and tests the cabin / board / beacons (lifted 1 m) with the SAME plane-distance idiom (no new three API); TRUE = a hub landmark is genuinely in the frame (board reads comfortably in at ~13° H / +10° V; the pixels stay the court of appeal, §4). Verified: `npm run build` (tsc+vite) GREEN; capture spec compiles (`--list`); frozen `movement`@smoke (ranger still walks ≥2 m — corridor intact) + `journey`/`ground`/`paths`@smoke + `board.spec` (board still reachable + opens at its new pos; watchtower diagonal not blocked) all GREEN; `e2e/**` + `@smoke` + `playwright.config.ts` untouched (only the additive `landmarkInView` mirror field in `e2e-capture/**`). **Supervisor: capture next.** Grade = world-entry shot (≥1 landmark in frame — cabin left + Missiebord right + tree line) + `cam.landmarkInView`=true at world-entry (and world-entry `drawCalls` well above the old void's 13).
- **P1.4 (F-08 · retune foot speed + tie stride to ground speed) — GRADE GREEN, ticked `[x]` with the +demo suffix (01:54–01:56 laptop capture, 25 shots).** ASSERT green: `annotations-laptop.json` reads `groundSpeed` **1.800** on every walk shot (06–10) and **1.643** at jeep-near (16) — all in-band [1.4, 2.2] while `clip`=walk; idle/hub/board/mission shots read 0.000 (correct 0-while-standing). It is an HONEST measurement, NOT an F-18 echo: `World.groundSpeed()`→`playerSpeed = moved / dt` (post-collision, World.ts ~2538), the 1.643≠1.800 variation proves it's derived from real motion, and the walk-4→walk-5 wall-clock (1.8 m in ~1 s from pos/​mtime) corroborates 1.8 m/s. Retune landed: `speed = 1.8` (World.ts:238, in the ≈1.6–2.0 target); stride TIED — `walk.timeScale = strideRate(speed)` (`speed / STRIDE_MATCH_SPEED` ~1.5, clamped; PlayerRig ~78, PlayerAnim `strideRate`), so distance-per-cycle tracks real speed → foot-slip killed in code. Jeep stays clearly faster (`veh.speed` 4.243 vs foot 1.8). SHOT green: the burst (06/08/10-walk) shows the WHOLE back-turned ranger at believable human proportion walking on ground+horizon past cabin (left) + Missiebord (right), normal-size conifers + a cast shadow, stride pose changing frame-to-frame; burst distance **9.0 m** vs Run A's 21.2 m — the near-jeep glide is gone. `npm run build` + frozen `e2e:smoke` GREEN (tick gate). **+demo:** the box carries "implemented — awaiting demo", NEVER "fixed" — the true gait FEEL / perceived foot-slip is Floris's on-device call (DEMO · Motion comfort).
- **P1.5 (re-check F-10 / F-12 / F-33) — RE-CHECK DONE; all three DISSOLVED; NO survivor → NO `app/src` change (§3 "re-checks, not fixes" + §6 "do not invent a pass"); NOT captured/ticked/committed.** Graded the freshest coherent laptop set (01:54–01:56, 25 shots); did NOT run capture (supervisor next). **F-10 gait judgeable — DISSOLVED:** walk burst 06–10 shows the WHOLE back-turned 1.7 m ranger on ground+horizon (cabin left, Missiebord right), `clip`=walk with `time` cycling (0.41→0.10→0.63→0.91→0.02) and `groundSpeed`=1.8 on every walk shot, stride pose changing frame-to-frame — the Run-A camera-inside-mesh / ankle-between-giant-legs un-judgeability is gone (gait FEEL stays +demo — Floris). **F-12 shadow band — DISSOLVED:** no camera-locked vertical dark band at screen centre across the burst; the ranger's cast shadow is now small + believable (10-walk-5). Root cause killed by F-07: the sun shadow-map ortho frustum (±16 m, World.ts ~389) follows the ranger and is driven by the now-1.7 m mesh, so the F-07-giant directional shadow F-12 blamed can't exist; residual soft ground darkening = the intended drifting cloud-shadow atmosphere (Atmosphere.ts/buildCloudShadows) + front-to-back light gradient = "believable prop shadows" (F-12's own pass wording). Drive-burst jeep shadows move WITH the jeep (under→left→under→right, 18–21) = cast shadows, not a fixed band. **F-33 "blur/DOF" — DISSOLVED:** walk + drive bursts crisp (cabin logs, board papers, jeep, conifers all sharp), no magnified-texture murk, `drawCalls` ≤66 (<150); Run A already code-proved no postprocess pass exists and F-07 removed the "textures magnified 10–30× seen from cm away" cause. **No survivor → no new finding, no code diff.** (Drive FRAMING oddities — jeep off-centre / empty seat — are F-30/F-31 Phase 2, not F-12/F-33.) Supervisor: capture next; grade = the walk-burst shot (gait readable + ground evenly lit + geometry crisp), which the 01:54 set already satisfies.
- **P1.5 (re-check F-10 / F-12 / F-33) — GRADE GREEN on the fresh 02:14–02:16 laptop capture (25 shots); ticked `[x]` with the +demo suffix (F-10 gait FEEL stays Floris's on-device call). No survivor → no `app/src` change (re-check).** Graded MY OWN fresh set per §1 (not the prior 01:54 set the fix-half saw). **F-10 judgeable — DISSOLVED:** 06/08/10-walk show the WHOLE back-turned ~1.7 m ranger centre-frame at believable human proportion (cabin left, Missiebord right, normal-size conifers + horizon), mid-stride with legs visibly moving; `clip`=walk with `time` cycling 0.394→0.112→0.646→0.901→0.005 + `pos` advancing + `groundSpeed`=1.8 → gait reads as walking; the Run-A camera-inside-mesh / ankle-between-giant-legs un-judgeability is gone. **F-12 half-screen band — DISSOLVED:** no camera-locked vertical dark band at screen centre across the burst; ground evenly lit apart from the ranger's own small cast shadow + soft off-centre cloud-shadow drift (shadow masses flip sides 08↔10, not fixed to the lens). **F-33 blur — DISSOLVED:** walk (cabin logs / board papers / conifers) AND drive (17-jeep-in clean jeep model, 20-drive-3 jeep+helicopter+deer+cones) all crisp; the cited jeep-in "white speckled magnified-texture band" is gone; `drawCalls` <150 everywhere (walk 47/32/21/20/15, drive 65/14/28/33, jeep-in 70). **Pixels AGREE with the hook this time (no F-18 lie):** `avatar.height`≈1.70 on every in-world shot, `avatarInView`=true / `avatarOpacity`=1 / `avatarScreen` heightFrac 0.29–0.32 matches the framed ranger the pixels actually show — the P1.1-era avatarScreen lie is resolved by the giant-fix. `npm run build` + frozen `e2e:smoke` GREEN (tick gate). No code diff; no new finding opened.
- **GATE-P1 (Fable re-judge, fresh 02:23–02:24 laptop capture, 25 shots) — ALL FIVE P1 BOXES AGREED GREEN, no re-opens; gate LEFT UNTICKED with three world-look triage boxes added (P1.6–P1.8) per the gate's own "first real look" mandate.** The pixels and the hook finally AGREE everywhere I probed (no F-18-class lie): `avatar.height` 1.68–1.71 on every in-world shot with the ranger visibly ~⅓ frame height (`avatarScreen.heightFrac` 0.32 matches the render); the 04/05 idle pair is camera-identical to ~1e-12 and 03/04 are byte-identical by `pixelHash` (the 04→05 pixel delta is the idle CLIP at time 0.007→0.157, not camera motion). Verdicts — **F-07 ✓** (03/11/12/16: whole ranger at believable proportion beside the cabin, from both sides, and the jeep); **F-05⊕F-18 ✓** (03: ground + horizon + whole SOLID ranger, `cam.dist` 4.854 ≥ 3, `avatarInView` true, opacity 1); **F-09 ✓** (cabin left + Missiebord right + tree line in the entry frame, `landmarkInView` true, drawCalls 51 vs the old void's 13; the board affordance fires by walk-2 — the first stride goes toward content); **F-08 assert ✓** (`groundSpeed` 1.800 on all five walk shots, jeep 4.243 clearly faster — stays "implemented, awaiting demo", NOT upgraded); **F-10/F-12/F-33 re-checks ✓ dissolved** (gait readable at proportion; no camera-locked centre band — the fresh bursts' dark masses migrate 07→10, carry signpost-cross/conifer-tip object shadows and are absent at entry, i.e. drifting cloud layer + long prop shadows; geometry crisp throughout incl. helicopter rotor + cone bands, drawCalls ≤65). TRIAGE: (1) **F-19 and F-24 are ORPHANS — assigned to NO ledger box anywhere** (Self-audit §5 mapped them only as F-07 murk-backdrop; their own framing/content fixes were never boxed — a plan gap this gate closes). Fresh pixels re-prove the surviving halves: 22-board-affordance shows the board edge-on at the frame edge, face never presented → **P1.6**; 24-mission-3d is now lit + composed (board face visible — F-24's framing half landed with F-05/F-07) but the frisling renders as a featureless pale egg and "het gras" as rock lumps → **P1.7**. (2) NEW lighting finding: the drifting cloud shadows black out ~half of walk-4/5 and drive-4 with unreadable murk inside → **P1.8** (soften opacity / feather the edge; drift itself is intended atmosphere). Otherwise the composed world reads coherent — hub (cabin, board, signpost), vehicle pool (jeep + helicopter), watchtower on the drive horizon, fence lines, cones, distant calm-pose animals, dune horizon — so no biome-density or prop-placement box is warranted. Builder: do P1.6–P1.8; a fresh Fable sitting then re-judges GATE-P1.
- **GATE-P1 second Fable sitting (fresh 02:37–02:39 laptop capture, 25 shots, all world frames 03–25 eyeballed) — VERDICT UNCHANGED: P1.1–P1.5 re-affirmed green (no re-opens), P1.6–P1.8 independently RE-CONFIRMED on my own set, NO new triage, gate LEFT UNTICKED because three non-demo Phase-1 boxes are open and unaddressed (no `app/src` change since P1.5's commit — the supervisor spawned this gate before the builder took the triage boxes, so the sitting is a confirm-and-hold).** Pixels + hook agree everywhere: 03-world-entry composes ground+horizon+WHOLE solid ranger at believable proportion, cabin left + Missiebord right + tree line (`avatar.height` 1.66–1.71 all in-world shots, `cam.dist` 4.854 ≥3 idle-stable to ~7e-8 across 03/04/05, opacity 1, `landmarkInView` true, drawCalls 51); proportion holds from the far side (12/13/14), beside the cabin (11) and beside the jeep (16); walk burst 06–10 shows the whole back-turned ranger mid-stride at `groundSpeed` 1.800 with the board affordance firing by walk-2 (jeep 4.243 clearly faster; F-08/F-10 stay "implemented, awaiting demo" — NOT upgraded); no camera-locked band, geometry crisp, drawCalls ≤71. Triage re-proof from MY frames: **P1.6** — 22-board-affordance now cuts the board face OBLIQUE at the RIGHT frame edge (approach-angle nondeterminism, same defect: papers never readable, camera jams tight on a front-on ranger); **P1.7** — 24-mission-3d frisling is still a featureless pale egg beside gras-as-rock-lumps (vignette framing itself composed: board face + spotlight + prompt chips); **P1.8** — cloud murk blacks out half of 09/10-walk with a hard edge and buries the drive burst (18/19 haziest, 21 right-third) — add 18/19 as cites. Everything else seen is already owned downstream (empty driver seat → P2.4, "Het stille zand" label occlusion → P4.3, text-only cards → P4.4, board exit → P3.2, pause links/scrim → P3.2/P4.7, 16-word toast → P3.3, zoom inert → P2.1); biome density + prop placement read coherent (heide/bos/stuifzand transitions, watchtower, fences, cones, calm-pose boar/deer at 15/16/19/20). Builder: land P1.6–P1.8, then a fresh Fable sitting re-judges GATE-P1.
- **GATE-P1 third Fable sitting (fresh 02:45–02:47 supervisor capture, 25 shots, coherent set; STILL zero `app/src` change since P1.5's commit 447f5d3 — second consecutive confirm-and-hold on an unchanged tree) — VERDICT UNCHANGED: P1.1–P1.5 re-affirmed green from my own frames (no re-opens), P1.6–P1.8 independently RE-CONFIRMED, NO new triage, gate LEFT UNTICKED because three non-demo Phase-1 work boxes are open and untouched.** Pixels + hook agree everywhere probed: 03-world-entry composes ground + horizon + WHOLE solid ranger at believable proportion, cabin left + Missiebord face-with-papers right + conifer line (`avatar.height` 1.70 on every in-world shot; `cam.dist` 4.854 ≥3, idle triple stable to <1e-7 with 03/04 `pixelHash` byte-identical — the 04→05 delta is the idle CLIP at time 0.172→0.041, camera fields static; `landmarkInView` true, drawCalls 51, and it flips false exactly on the landmark-free 09/10 frames, so the boolean tracks the render); proportion holds beside the cabin (11/12/14), the board (07/22) and the jeep (16); `groundSpeed` 1.800 on all five walk shots vs jeep 4.243 (F-08/F-10 stay "implemented, awaiting demo" — NOT upgraded); no camera-locked band (dark masses migrate 06→10, carry prop shadows, absent at entry = the P1.8 cloud layer, not F-12 returned); geometry crisp throughout, drawCalls ≤66. Triage re-proof on MY set: **P1.6** — 22-board-affordance shows the board EDGE-ON at the right frame edge, face/papers never presented, cabin centre-frame instead (third distinct approach angle, same defect); **P1.7** — 24-mission-3d is a lit composed vignette but "het gras" props are dark angular rock-lumps and NO recognizable piglet silhouette (ears/snout/legs) exists anywhere in frame; **P1.8** — the cloud layer blacks out the left ~half of 09/10-walk with unreadable murk inside a hard edge, hazes 19-drive mid-frame and buries the right third of 21-drive. Chased the one candidate NEW defect to ground: the dark maroon blob at the ranger's head in 03/25 is his own back-of-head HAIR (verified by 2× crop — board face composes cleanly behind it), not a floating artifact → no new box; 15-pause cabin "tilt" is edge-of-frame wide-angle stretch (level in 03/11/12/14/22). LOOP NOTE: the first unchecked ledger box is P1.6 — a WORK box — yet the supervisor has now spent two Fable gate sittings in a row on an unchanged tree; the next sitting must be the Opus BUILDER on P1.6–P1.8, and only after those land should a fresh Fable sitting re-judge GATE-P1.
- **GATE-P1 fourth Fable sitting (fresh 02:56–02:57 supervisor capture, 25 shots; THIRD consecutive confirm-and-hold — still zero `app/src` change since P1.5's commit 447f5d3) — VERDICT UNCHANGED: P1.1–P1.5 re-affirmed green from my own frames (no re-opens), P1.7 + P1.8 independently RE-CONFIRMED, NO new triage, gate LEFT UNTICKED because three non-demo Phase-1 work boxes stay open.** Re-affirm evidence: 03-world-entry composes ground + horizon + WHOLE solid ranger at believable proportion with cabin left + Missiebord right + conifer line (`avatar.height` 1.69–1.71 on every in-world shot; `cam.dist` 4.854 ≥ 3 with 03/04 `pixelHash` byte-identical and the idle triple's cam fields stable to <1e-7 — the 04→05 pixel delta is the idle clip; opacity 1, `landmarkInView` true, drawCalls 51); proportion holds beside the cabin (11), from the far side (12/14), at the board (07/22) and beside the jeep (16); `groundSpeed` 1.800 on all five walk shots vs jeep 4.243 (F-08/F-10 stay "implemented, awaiting demo" — NOT upgraded); no camera-locked band (dark masses migrate 08→09/10, carry a signpost-cross object shadow, absent at entry); geometry crisp throughout (cabin logs, board papers, heli rotor, cones), drawCalls ≤71. **NEW DATUM on P1.6 (box stays open):** for the FIRST time in four captures, 22-board-affordance composes the board FACE-ON with papers readable — the straight-line −z spawn approach presents the +z face the P1.3 move gave it, so F-19's "angle the face toward the natural approach" half already works when the harness walks the natural line; the box still can't close because the pass is approach-luck (3 of 4 captures landed edge-on/oblique), the damp-turn reframe on near-radius entry is unbuilt, and the `board-in-frustum while board.near` assert does not exist. P1.7 re-proven on my set: 24-mission-3d is a lit composed vignette but the frisling is STILL a featureless pale egg (no ears/snout/legs) beside "het gras" as dark rock lumps. P1.8 re-proven: 09/10-walk lose the left ~half to unreadable murk behind a hard edge; 18-drive buries the bottom third (incl. an ambiguous hard-edged dark slab inside the murk — expect the feather/opacity fix to resolve or expose it) and 19-drive hazes mid-frame. **LOOP MISCONFIGURATION, to Floris:** three Fable gate sittings in a row have now burned on an unchanged tree while the first unchecked ledger box is the WORK box P1.6 — the supervisor must dispatch the Opus BUILDER at P1.6–P1.8 (or Floris lands them by hand) before any further gate sitting spends tokens re-proving the same three defects.
