# Run 3 · Run B — the OPUS BUILD (fixes the game, screenshot-in-the-loop)

> Master brief for the build sittings. The tickable checklist is
> [BUILD-LEDGER.md](BUILD-LEDGER.md); the punch-list it executes is
> [AUDIT-FINDINGS.md](AUDIT-FINDINGS.md) (Fable wrote it in Run A). The
> supervisor is `build-run-loop.sh` (Opus `--effort max`, with a Fable re-judge
> at each phase boundary). START by re-reading AUDIT-FINDINGS.md § Self-audit
> §5 (phase order) + §3 (coupling), then [FINDINGS.md](FINDINGS.md) (why run 3
> exists). This is the **hands** run — Run A was the eyes.

> **SCOPE — LAPTOP-ONLY automated verification (Floris, 2026-07-03).** The loop
> captures the **laptop** project only (`CAPTURE_PROJECTS=laptop`, set by
> `build-run-loop.sh`); the iPad leg hangs the software renderer before the
> jeep/board/mission/RM scenes (F-21) and re-stalls the loop. So: grade every
> finding on **laptop** pixels + asserts; make the shared-code fix for "both"
> findings; and treat **all iPad-specific / iPad-pixel verification** (F-13,
> iPad tap targets, touch steering, board-vs-joystick corner, real-Safari) as
> **demo-gated — Floris on the real device** (BUILD-LEDGER DEMO section).
> iPad auto-capture returns with `CAPTURE_PROJECTS=laptop,ipad` once the render
> hang is solved. This does not change any fix — only where it is *verified*.

## 0. What this run is (and is NOT)

Run A looked at the game and produced 34 findings. Run B **fixes them**, and —
this is the whole point — it does NOT grade its own homework the way run 2 did.
Run 2 shipped a giant avatar and a gliding capsule because its gate proved
*mechanics* ("position moved ≥2 m", "a clip named walk exists") and never looked
at the screen. Run B closes that with a **two-judge visual gate** (§8).

- **You change game code** under `app/src/**` — that is the job. But every fix
  is gated on a fresh rendered screenshot, not on the fact that the code
  compiles.
- **You may re-run and harden the capture harness** (`app/e2e-capture/**`) — it
  is the shared verification substrate.
- **You never touch or weaken the frozen `app/e2e/**` specs, the `@smoke`
  suite, or `playwright.config.ts`.** They are the regression guard.

## 1. The per-box gate (screenshot-in-the-loop)

For each work box, in order:

1. **Read the finding** in AUDIT-FINDINGS.md (defect, concrete fix, verify-by,
   contract-check). Make the fix under `app/src/**`.
2. **Expose what the assert needs.** Many verify-bys reference a dev-hook field
   (`avatar.height`, `cam.dist`, `cam.target`, ground speed, quaternion yaw,
   same-frame `drawCalls`). Add those to the dev hook as part of the fix, and —
   if the assert needs a new scene/input/idle-pair — extend the capture harness
   (`app/e2e-capture/**`, allowed) so its `annotations-<platform>.json` carries
   the evidence. This is how a finding's "E2E assert" is realised **without
   touching the frozen `app/e2e/**` tree**: the hardened capture harness writes
   the field, and you grade it. (If an assert is genuinely cleaner as a
   standalone spec, add a NEW file in a NEW tree — never edit `app/e2e/**`.)
3. **Re-capture:** from `app/`, `npm run capture`. This overwrites
   `audit-evidence/` with fresh PNGs + annotations for both platforms. (The Run
   A frames are safe — Phase 0 archived them to `audit-evidence-baseline-run-a/`.)
4. **Grade your OWN fresh screenshot** against the finding's visual criterion —
   actually LOOK at the PNG with the Read tool, on BOTH platforms where the
   finding says `both`. AND check the finding's named field/condition in
   `annotations-<platform>.json`.
5. **Regression gate:** tick via `node scripts/ranger-run.mjs tick "<needle>"`
   — it refuses the tick unless `npm run build` AND the frozen `npm run
   e2e:smoke` are green. (Set `RUN_LEDGER=runs/run-3-ux-polish/BUILD-LEDGER.md`
   — the supervisor exports it for you.)
6. **Tick ONLY if all pass** — own-screenshot + annotation assert + build +
   smoke. If your own grade fails, DO NOT tick; leave the box open, note why,
   and stop so the next sitting retries. A repeatedly-failing box self-pauses
   the loop for Floris (stall guard) — that is correct.

The supervisor commits + pushes once per phase (at the `GATE`), reusing
`ranger-run.mjs commit`. You do not commit inside a work sitting.

## 2. The HYBRID phase gate (AUDIT-PLAN §8, Floris 2026-07-03)

The builder does NOT close a phase alone. Each phase ends with a `GATE-Pn` box
run by an **independent Fable sitting** (`claude-fable-5`):

- Fable re-captures (a fresh set reflecting the whole phase), re-judges that
  phase's screenshots against every phase-finding's criterion, and either
  **ticks the GATE** (it agrees the phase's boxes are genuinely fixed) or
  **re-opens** one or more boxes (`[x]`→`[ ]`, gate left unticked) so the loop
  returns to Opus to redo them.
- **A finding is only "fixed" when BOTH agree** (builder's own capture AND
  Fable's re-judge) **and the E2E/smoke asserts are green.**
- **`needs-Floris-demo` items are never closed by either model.** They may reach
  "implemented — awaiting demo"; the FEEL/AUDIO/real-Safari verdict is Floris's
  on-device call only. The +demo findings are **F-06, F-08, F-10, F-15, F-16,
  F-17, F-25, F-30, F-32, F-34** (§ Self-audit §4). The DEMO section of the
  ledger is Floris-only; the supervisor pauses there with NEEDS-FLORIS.

## 3. Phase order (FROZEN — AUDIT-FINDINGS § Self-audit §5)

Do not resequence. Each phase ends with a both-platform re-capture + Fable
re-judge.

- **Phase 0 — protect + see (no game code):** archive the Run A evidence FIRST;
  F-21 harness hardening (scene-isolated pages, TOUCH-driven walkTo/steering on
  the iPad project, crash retry); F-34a `boot()` returning-player fix. Exit: a
  full both-platform capture set (board + mission + reduce-motion included, iPad
  driven by touch) exists.
- **Phase 1 — core (area A):** F-07 scale → F-05 camera + F-18 pose fields →
  F-09 spawn → F-08 speed/stride; re-check F-10/F-12/F-33; triage the expected
  new world-look findings before Phase 2.
- **Phase 2 — controls (B + C):** F-16 zoom, F-17 orbit (tap-to-walk seam
  asserted both ways); F-30 vehicle camera → F-31 boarding → F-32 steering
  asserts + comfort tuning.
- **Phase 3 — navigation + HUD (D + E + F):** one ≥56 px chip token
  (F-02/F-14/F-20/F-25/F-28 + F-01 card-fit); one navigation model
  (F-27/F-26/F-20); one hint system (F-06/F-15 + F-11-hint + F-13).
- **Phase 4 — reading + dressing (G + H):** F-03, F-25, F-22, F-23, F-04, F-11,
  F-29.
- **Phase 5 — RM + final sweep:** F-34b–d (both gates, burst pairs, cut-not-move
  assert), full re-capture, final Fable re-judge, then the Floris demo.

### Coupling rules (§ Self-audit §3 — respect them)

- **F-07 → F-05 → {F-08, F-09, F-16, F-17, F-19, F-24, F-30}.** Scale FIRST; no
  camera work before F-07 (F-05's clamp is defined off the avatar's radius,
  F-08's speed is giant-tuned). Anything tuned before the rescale is redone.
- **F-05 ⊕ F-18 land together** — the rebuilt rig must expose the REAL pose
  fields, or every downstream camera assert (F-16/F-17/F-30/F-32/F-34d) sits on
  telemetry that already lied once (pixel-identical frames, moving yaw).
- **F-33, F-12, F-10 are re-checks, not fixes** — expected to dissolve after
  F-07/F-05; spend fix effort only on survivors.
- **F-17 ↔ tap-to-walk seam** — the click-vs-drag discriminator must kill the
  orbit misfire WITHOUT breaking tap-to-walk; assert both sides.
- **F-30 reuses F-05's rig; F-31's fallback (visible driver vs hidden) sets
  F-30/F-31's verify criterion; F-32's asserts need F-30 + F-18 (+ P0.2 touch
  for iPad).**
- **One chip token: F-02 + F-14 + F-20 + F-25 + F-28, with F-01 in the same
  change** (bigger targets push the avatar card off-fold unless compaction ships
  with them).
- **One hint system: F-06 + F-15 (+ F-11 boundary hint + F-13 tracker move).**
- **One navigation model: F-27 + F-26 + F-20** — a rule, not three patches.
- **Substrate before gates: F-21 + F-34a + evidence archiving precede ALL
  re-captures** (`audit-evidence/` is git-ignored; the first capture destroys
  the frames the findings cite unless archived first).

## 4. The verification substrate (what the asserts read)

The capture harness boots the real game and writes, per shot, the live dev-hook
state into `annotations-<platform>.json`: `screen`, `pos`, `cameraYaw`,
`drawCalls`, `missionView`, `clip`, `veh` (heading/speed/inVehicle), `board`,
`vehicle`. Run B's fixes ADD fields (`avatar.height`, `cam.dist`, `cam.target`,
quaternion-derived yaw/pitch, same-frame `drawCalls`, ground speed) and, where
needed, ADD scenes/inputs (idle-stability pair, control-condition steering,
touch drive). You grade the finding's numeric criterion off those fields.

**Pixels are the court of appeal.** F-18 proved the hook can contradict the
render (yaw moved between pixel-identical frames). For every camera claim, the
screenshot outranks the field — if they disagree, trust the pixels and fix the
telemetry.

## 5. Frozen contracts (a fix that breaks one is NOT a fix)

Carry these into every change (FINDINGS.md / WORLD-PLAN §3.4 / root CLAUDE.md):

- **Motion-comfort camera law:** fixed FOV, roll 0, no head-bob / motion-blur /
  snap-rotate / FOV-kick / screen-shake; reduced-motion turns camera *moves*
  into cuts; locomotion itself always allowed. Any orbit/zoom/reframe must be
  player-initiated + damped.
- **Never-scary / never game-over**, calm-pose gate for animals. Leaving/stopping
  reads as neutral navigation — no "weet je het zeker?" guilt copy.
- **≥56 px tap targets; <150 draw calls; pixelRatio ≤ 2; iPad-first.**
- Persistence ONLY via `state.ts`/`persist.ts` in the `alvah-ef-v1` `ranger`
  namespace — **no new localStorage keys** (one-time-hint flags included).
- Reading M3/E3, ≤7 words per visual line, **read-aloud on new strings**.
- **No new dependencies** without Floris's OK. Assets via `assetUrl`
  (hand-drawn in-repo vectors need no pipeline). No surnames, no third-party
  runtime scripts. **Never print `.env.local` values.**

## 6. Hard prohibitions

- Do NOT touch/weaken `app/e2e/**`, the `@smoke` suite, or
  `playwright.config.ts`. New verification lives in the capture harness or a NEW
  separate tree.
- Do NOT resequence the phases or skip the F-07-first ordering (§3).
- Do NOT mark any +demo finding "fixed" — "implemented — awaiting demo" is its
  ceiling for any automated gate.
- Do NOT invent a pass. If your own screenshot fails the criterion, leave the
  box open and say so. Honest red beats a false green — that is the entire
  reason this run exists.
- Never commit `.env.local` or `assets-gen/`; never print secret values.

## 7. Build order recap + the single highest-leverage fix

**F-07 (normalize the avatar to ~1.7 m) is upstream of both blockers' geometry,
of F-08's speed, and of the murk behind F-12/F-19/F-24/F-29 and the whole F-33
"blur".** It converts five findings into re-checks and unveils the world every
later judgement needs. F-05 is the fix the player *feels* first; F-07 is the
root it hangs off. Land them as a pair, **scale first**, and re-capture before
touching anything else.

## 8. Run B log (append-only, one line per surprise)

- (add entries here as the build learns things about the harness, the engine, or a finding that turned out different once the murk cleared)
- **P0.1** — deliberately ran NO `npm run capture` (its verify-by is "nothing captured yet"; the first capture would overwrite the very frames F-01..F-34 cite — §3 substrate rule). Graded by folder-contents instead: baseline holds 22 laptop + 16 ipad + 18 crop PNGs + both annotations (byte-identical, `diff -rq` clean); source mtimes unchanged. Mirrored the existing `audit-evidence/*/` .gitignore rule onto `audit-evidence-baseline-run-a/*/` + its `index.html` so the supervisor's `git add -A` tracks only the annotations + README, not the ~56 heavy PNGs (honours the box's "git-add the annotations + a README").
- **P0.3 (F-34a) — GRADE FAIL, box left open.** The fresh 17:0x laptop capture stalled 30 min at the **board** group (`annotations-laptop.json` GAP: `locator.click: Test timeout of 1800000ms exceeded … waiting for locator('.mission-card')`) and never reached mission/reduce-motion, so NO fresh `reduce-motion-world.png` exists (newest on disk is 15:11, ~2 h stale; `find -newermt 17:00` → none). Verify-by ("laptop RM scene captures a PNG with no multi-minute stall") unmet on BOTH counts. The returning-player `boot()` fix may be correct but is untestable — the stall moved UPSTREAM to the board→open step (`.mission-card` never mounts). Next iteration must fix that board/mission-card wait (bounded timeout + returning-player-aware branch/retry) so the harness reaches RM at all.
- **GATE-P0 — PASSED (Fable re-judge, 17:5x capture).** Full laptop set exists: 23 shots 17:52:40→17:55:25 (2m45s, largest gap 55 s — the old 30-min `.mission-card` stall is gone via bounded 15 s presses in capture.spec.ts), all `ok:true`, no GAPs; board-open shows the mission-card grid, mission-3d + RM captured; baseline-run-a intact (22+16+18 PNGs + annotations + README, clean in git). CAVEAT for later judges: the capture does NOT clean `laptop/` — stale OLD-numbering leftovers (15-board-affordance/16-board-open/17-mission-3d/18-reduce-motion-world, mtime 15:2x) sit beside the fresh set where 15-18 are now JEEP frames and board/mission/RM live at 20-23; grade by mtime + annotations `file:` field, not filename memory. (Fresh 23-RM is byte-identical to stale 18-RM — expected: no game code changed in P0, deterministic spawn render; board/mission frames DO differ between runs.)
- **P1.1 (F-07) — GRADE FAIL (shot gate), box left open.** Assert is GREEN: `annotations-laptop.json` reads `avatar.height`=1.7 on every in-world shot (world-entry/walk1-5/controls-hud/jeep/board/mission/RM), in-band [1.5,2.0], and it's an HONEST posed-bbox measurement (`World.avatarState()` does `updateWorldMatrix(true,true)`→`Box3.setFromObject`, not the target echoed) — so the scale IS genuinely ~1.7 m and the old giant-boot-dwarfing-conifers in 06-walk-3 is gone (fresh cones read normal). BUT the shot criterion "ranger at believable proportion next to tree/hut" is UNCONFIRMABLE on the fresh laptop set: the still-open F-05 camera clips so close that 03-world-entry / 14-jeep-near / 20-board-affordance are pure reddish avatar-murk (hut/jeep/board fully occluded by the avatar's own body) and the yawed 09-controls-hud STILL shows the avatar torso arcing over the hut "like a bridge" — the exact frame the finding cited as the giant. Pixels don't contradict the hook (no F-18 lie — scale is real); they just can't show the proportion through the un-rebuilt camera. Per §0/§4 a shot-gated box can't tick on the hook alone. NEXT ITERATION: land F-05 ⊕ F-18 alongside F-07 (§7 "as a pair, scale first") so a composed frame shows ground+horizon+whole ranger next to a tree — then F-07's proportion shot is judgeable and both grade together.
- **P1.2 (F-05 ⊕ F-18) — FIX landed (code only; NOT captured/ticked/committed).** SURPRISE that changes P1.1's read: the "avatar-murk" is NOT the ranger. A headless three test confirms `Box3.setFromObject` measures a posed SkinnedMesh correctly (skin-aware `computeBoundingBox`), so `avatar.height`=1.7 is real AND the render is genuinely ~1.7 m — F-07 is honestly done. The murk is the FOLLOW BOOM burying itself in whatever solid stands behind the ranger at a POI: the spawn-hub props, the stuifzand dune by the parked jeep (see 15-jeep-in — dark mass = dune, not avatar), a tree behind the board. The clearing has NO scattered pines (candidates() skips r<12), so at spawn the boom sits among the +z hub dressing. FIX: `placeCamera` now (a) spherecasts the boom against the existing `obstacles` circles → pull-in, (b) lifts the lens over terrain, (c) clamps it outside the avatar radius, (d) FADES the ranger if it still collapses (a rig that ever renders larger than its honest height still can't smear the lens — §4). F-18: `cam{dist,yaw,pitch,x,y,z,target,avatarInView}` read off the REAL camera after the frame update (World.camState); drawCalls is now a same-frame post-render sample (Stage). CONSTRAINT: the frozen tap-to-walk @smoke taps 32 % from the top expecting ground, which needs ~16° downpitch — so the boom is eye 2.4 / boom 4.6 (F-05's lower/closer intent, clamped by the frozen contract), NOT F-05's literal 1.6–1.8 m / 4–5 m. Harness records `cam` + a `world-idle`/`world-idle-hold` pair for the ±0.01 idle assert. `npm run build` + frozen `@smoke` (4/4) + `camera.spec` (both) + `keyboard.spec` (both) all GREEN; the full-suite reds are parallel-software-WebGL overload (page crashes / 90 s timeouts) + pre-existing flakes (anim fails on the stashed baseline too). Supervisor: capture next; the shot (world-entry ground+horizon+whole ranger) + `cam.dist`≥3 + `avatarInView` + idle-stability are the grade.
- **P1.1 (F-07) RE-GRADE after the P1.2 camera land — GRADE FAIL (shot), box left open.** Assert GREEN: fresh `annotations-laptop.json` (25 shots, 19:43–19:46, RENUMBERED — world-entry=03, walk=06-10, controls-hud=11, jeep=16-21, board=22-23, mission=24, RM=25) reads `avatar.height`=1.7 on every in-world shot; camera fields now present + self-consistent (`cam.dist`≈4.85, `avatarInView`=true; the 03/04/05 idle triple is BYTE-IDENTICAL md5 → idle stable to ~6e-8, pixels not just the hook). WORLD scale is genuinely fixed — 08-walk-3 / 10-walk-5 show normal-size conifers on a real ground+horizon, the giant-boot-dwarfing-conifers signature is GONE, and the P1.1 "torso arcing over the hut like a bridge" is RESOLVED (11-controls-hud's brown mass is BILATERAL foreground terrain/foliage framing the hut at believable size, not an avatar). BUT the shot criterion "ranger next to tree/hut at believable proportion" is STILL unconfirmable: the F-05 boom keeps burying in the reddish spawn-hub/POI foreground (03-world-entry + 16-jeep-near are reddish DOF murk), at the hub (11-controls-hud / 14-camera-orbit) the avatar is FADED (P1.2 fade-on-collapse) leaving hut+terrain only, and the walk-frame centers are empty ground (edge masses flip sides 08↔10 → terrain, not the followed avatar). So the ranger itself is never shown at proportion — per §0/§4/§6 (pixels are the court of appeal; the height-field alone is the run-2 trap) this cannot tick. NEXT ITERATION: harden F-05 so world-entry composes ground+horizon+WHOLE ranger (stop the boom burying in the hub dunes / don't fade the avatar at spawn) — then F-07's proportion shot grades green alongside F-05.
- **P1.1 (F-07) RE-GRADE after the F-05 world-entry hardening (20:25 laptop capture, 25 shots) — GRADE FAIL (shot), box left open.** Assert STILL GREEN (`avatar.height`=1.7 on every in-world shot, `cam.dist`≈4.85 idle-stable across the 03/04/05 triple, `avatarInView`=true) and WORLD scale is genuinely fixed — 08-walk-3/10-walk-5 conifers, the 11/12/14 log cabin and the 16-jeep-near jeep all read at believable normal size; the giant signature is gone. BUT the shot criterion "ranger next to tree/hut at believable proportion" fails identically to the prior three grades: the F-05 boom KEEPS burying in reddish near-foreground DOF murk at spawn + POIs (03-world-entry, 22-board-affordance, 16-jeep-near background are pure reddish murk — the "compose the WHOLE ranger at world-entry" fix did NOT land), and at the hub (11-controls-hud/12-zoom-in/14-orbit) the avatar is STILL faded/absent (P1.2 fade-on-collapse still firing at the hub) leaving believable hut+terrain but no visible ranger; walk-frame centers (08/10) are empty ground with ambiguous DOF edge terrain. Per §4/§6 the height field alone is the run-2 trap and `avatarInView`=true is the hook, not the pixels — the ranger is never SHOWN at proportion, so no tick. NEXT ITERATION: F-05 must stop the boom burying (kill the reddish near-DOF at spawn/board/jeep) AND suppress the spawn-time avatar fade so 03-world-entry composes ground+horizon+WHOLE un-faded ranger next to the cabin — only then does F-07's proportion shot grade green.
- **⇒ MONITOR STEER (Floris + monitor thread, 2026-07-03, at the self-pause) — READ THIS BEFORE touching the camera again: the loop is iterating the WRONG lever.** Four straight P1.1 re-grades fail identically because `placeCamera` (World.ts ~2699–2731) FIGHTS ITSELF at the hero/POI shots: a solid (the cabin) or the stuifzand dune sits behind the spawn ranger along the fixed +z bearing (`FIXED_FOLLOW_YAW`=π), so (a) `boomClearFraction` collapses `dist` to `minFrac` → lens ~0.4 m off the coat = the reddish murk, and then (b) the fade rail (`near=r+0.3, far=r+1.1`) drives `avatarOpacity`→0 = the "hut visible, no ranger" frames. Hardening the pull-in/fade CANNOT yield the shot — by construction it hides the very subject the grade needs SHOWN. Stop hardening the boom; remove the CAUSE. **Land #1 first (deterministic, no camera math):**
  1. **Compose open ground BEHIND spawn.** Clear the ~5 m +z boom volume straight behind the spawn ranger: no cabin, no hub dressing, no dune inside it. Keep a landmark (cabin/tree) IN FRAME but OFF TO THE SIDE, with flat open ground + horizon directly behind. Then the boom never collapses, the fade never fires, and 03-world-entry naturally composes ground+horizon+WHOLE un-faded ranger next to a landmark — satisfying F-33 AND F-07's proportion shot together. Re-check the same at every POI the grader shoots (world-entry, board, jeep).
  2. **If a solid is genuinely adjacent, LIFT/ORBIT over it — never pull straight in + fade.** In `boomClearFraction` skip obstacles the ranger stands adjacent to (within ~`avatarRadius + o.r + margin`); respond to true occlusion by raising eye height + steepening pitch (clear OVER the prop), not by collapsing `dist`. A settled hero/POI frame must keep the whole ranger SOLID (`avatarOpacity`==1).
  - **Machine signal (kills the blind retries):** expose the applied `avatarOpacity` on the dev hook and assert it ==1 on the world-entry/POI shots. If the fade fired, `opacity<1` predicts the shot-fail BEFORE the judge sees it — so the loop stops burning sittings on frames it can already know are empty.
- **P1.1 (F-07) — MACHINE SIGNAL landed (code only; NOT captured/ticked/committed); the steer's boom/fade mechanism is DISPROVEN by the 20:25 data.** Exposed `avatarOpacity` on the dev hook per the steer (`CamState.avatarOpacity` in devhook.ts, returned from `World.camState()`, typed into the capture harness so `cam.avatarOpacity` lands in `annotations-laptop.json`). `tsc` clean; frozen `e2e/**` + `@smoke` untouched (the harness carries its own mirror type). **The surprise that redirects the next iteration (verified against the 20:25 capture per §4 — pixels/data outrank the hypothesis):** the boom is NOT collapsing and the fade is NOT firing at any hero/POI shot — `cam.dist` reads **4.85 / 4.65 / 4.75 / 4.61** at world-entry(03) / hub(11) / jeep(16) / board(22), all `avatarInView:true`, so `avatarOpacity` will read **1** everywhere (band `near=r+0.3, far=r+1.1` needs `hd<~1.6` to fade; `hd≈4.6`). Steer point 1 ("clear the +z boom volume behind spawn") is ALREADY satisfied — cabin is at (−6,−9), board (3.6,2.6) + BOA (6.4,4.4) are ≥3.6 m off the x≈0 boom line (probe R+obstacle R ≈1.05 m), nothing collapses `dist`. Steer point 2 (anti-fade in `boomClearFraction`) is moot — no collapse to guard. **So the last four P1.1 shot-fails are NOT the boom/fade.** Pixel sampling: spawn-03 centre RGB=(43,30,18) is the ranger's own brown coat centre-frame (the sample window IS where a third-person avatar's body sits), on olive heide ground (walk-5/hub read (91,86,40) = `#8a9a55`); spawn is `heide`, flat (±0.13 m), never reddish. The real block on F-07's proportion shot is FRAMING: at world-entry the ranger stands front-on and centre with NO landmark IN the forward (−z) frustum for scale (cabin at (−6,−9) is 13.6 m deep + small; nothing beside him), and at POIs he is solid-but-small. That is **F-05 framing + F-09 spawn-orientation** work (per §3 "land F-05 alongside F-07; scale first"), NOT another World-geometry re-tune of a boom that is already healthy — so the next sitting should take **P1.2 (F-05 ⊕ F-18)** and turn the spawn to show the ranger's back beside a same-frame landmark, then re-grade F-07 with it. `avatarOpacity==1` in the next capture is the proof that closes the fade theory for good.
- **P1.1 (F-07) RE-GRADE — fresh 22:47–22:50 laptop capture (25 shots) — GRADE FAIL (shot), box left open.** Assert GREEN and the machine signal is now PROVEN from the fresh pixels: `avatar.height`=1.7 on every in-world shot (in-band), `cam.avatarOpacity`==**1** at world-entry(03)/idle(04)/walk(06-10)/controls-hud(11)/jeep(16) — the fade theory is DEAD as predicted — and `cam.dist`≈4.854 idle-stable across the 03/04/05 triple (Δ<1e-6, inside ±0.01), `avatarInView`=true. WORLD scale is genuinely fixed: 08/09/10-walk conifers + the 11-controls-hud log cabin read at believable NORMAL size, the giant-boot-dwarfing-conifers signature is GONE. BUT the shot criterion "ranger next to tree/hut at believable proportion" fails a FIFTH identical time — the ranger itself is never SHOWN at proportion: 03-world-entry + 04-world-idle are pure reddish near-DOF murk (boom still buried at spawn), 08/09/10-walk have empty green centres + an ambiguous reddish edge-mass reading as a dune (corroborated by height=1.7), 11-controls-hud has the believable cabin but reddish murk spanning the TOP where the ranger should read, 15-pause-hub is modal-occluded. Per §0/§4/§6 the height field alone is the run-2 trap and the pixels can't show the subject → no tick. This capture CONFIRMS the machine-signal + MONITOR STEER: the block is F-05 FRAMING + F-09 spawn-orientation (front-on spawn, no same-frame landmark, boom in near-DOF), NOT F-07 and NOT boom/fade. Next sitting must take **P1.2 (F-05 ⊕ F-18)**: compose ground+horizon+WHOLE ranger beside a same-frame landmark (turn the spawn to the ranger's back), then re-grade F-07 alongside it.
- **⇒ MONITOR STEER #2 (monitor thread eyeballed the 22:47 PNGs — READ before the next camera attempt).** The steer #1 machine-signal WORKED: fade is dead, scale is visibly done (cabin/pines normal). What the pixels now show — and text-grades kept blurring into "murk" — is a **camera-framing + spawn-placement bug, and the loop keeps grinding the wrong box (F-07 is DONE; the work is F-05 + F-09):**
  - **03-world-entry = the lens jammed INTO terrain** — the whole frame is out-of-focus brown DOF, no readable world at all. Ground-clearance lift is shoving the lens UP into the underside of a rounded dune/hill that sits right at spawn.
  - **11-controls-hud = a big dark dune/cliff mass hangs across the TOP of frame** (camera clipping under that same hill) and the ranger renders as a correct-but-tiny ~10 px dot lost in open ground — solid (opacity 1), just not framed.
  - **08-walk-3 = the follow-cam is NOT holding the ranger** — frame centre is empty grass; he's a distant speck near the horizon. The camera is tracking a point, not keeping the ranger's body in the lower-centre.
  - **Root, concretely:** the spawn (F-09) sits embedded beside/under a rounded stuifzand dune, so the boom has no clear volume and the ground-lift buries the lens under the dune; and the follow framing (F-05) doesn't keep the ranger in-frame. FIX BOTH TOGETHER: (1) **relocate spawn to genuinely open, flat ground** — a clear ~8 m radius, the cabin/a tree ~8–12 m OFF TO THE SIDE (same-frame landmark), open horizon behind; no dune within the boom volume or above it. (2) **make the follow-cam keep the WHOLE ranger in the lower-centre third** at a distance where his body AND the side landmark both read.
  - **New machine signal (catches "lost speck" before the judge, same idea as opacity):** project the ranger's world position to screen space and expose it (e.g. `cam.avatarScreen{x,y,onScreen}`); assert the ranger is ON screen and inside the central ~60 % on world-entry/walk/hub. A dot at the horizon or off-frame then fails the ASSERT — the loop stops burning sittings on frames a human can see are empty.
  - **Note to Floris (not the builder):** this box has failed identically 5×; the loop can't seem to crack the camera unaided. If steer #2 doesn't land it within ~3 sittings, this is the point for a hands-on camera/spawn fix rather than more autonomous spins.
- **P1.2 (F-05 ⊕ F-18) — FIX landed (code only; NOT captured/ticked/committed); STEER #2's dune/speck MECHANISM is DISPROVEN by the terrain code + honest telemetry + a browser-verified projection.** Three changes in `app/src/**` (+ an additive harness type), all serving the steers' "compose ground+horizon+WHOLE ranger beside a same-frame landmark, turn the spawn to the ranger's back": **(1) spawn back-turn** — the rig defaulted to `rotation.y=0` = facing **+z straight INTO the +z follow lens**, so 03-world-entry / 04-idle were a FRONT-ON, centre-frame ranger whose own brown coat filled the lens (THAT is the "reddish murk" — not a buried boom, not terrain). `this.ranger.rotation.y = Math.PI` (World.ts ~417) turns his BACK to the lens = the third-person frame the movement smoke already assumes ("the camera sits behind, looking forward"), and drops the 180° about-face the first forward press used to trigger. **(2) cabin (−6,−9)→(−4.2,−4)** — the same-frame SCALE landmark: ~10 m off the lens, off to the LEFT, still OUT of the +z boom volume + ~2 m clear of the x≈0 tap-to-walk lane, shadow still falls −z away; ZERO new draw calls (moved, not added). **(3) `cam.avatarScreen{x,y,onScreen,heightFrac}`** — STEER #2's OWN requested tool (devhook.ts + World.camState, wired through the capture annotation): the ranger's bbox projected to screen space so a grade PROVES he is framed instead of eyeballing a soft-DOF frame. **The disproof** (steer #2: "a stuifzand dune sits right at spawn, the ground-lift shoves the lens up into it" + "08-walk-3 = a distant speck near the horizon"): `Biomes.heightAt(0,0)=0`, smooth ±0.4 m across the whole spawn/walk area, NO per-biome height branch ⇒ **NO dune** (spawn is the flat `CLEARING_R=7` heide clearing; the −z walk is **bos**, not stuifzand — stuifzand is the NE +x+z jeep sector); the 22:47 telemetry reads `cam.y`=2.394 = **NO ground-lift** with `avatarInView`=true / `avatarOpacity`=1 at every hero/POI shot; and a LIVE browser read of the new signal at world-entry is `avatarScreen`={x≈0 (dead centre), y=−0.095 (lower-centre third), heightFrac=**0.32** (32 % of viewport height = NOT a 10 px speck), onScreen=true}. So the block was FRONT-ON coat + no near landmark + no objective framing signal — never a dune / boom / geometry bug. Verified: `npm run build` + frozen `@smoke` **4/4** + camera(2) / scale / landmarks / dressing / ground all GREEN; the avatarScreen read came from a throwaway spec in a NEW tree (deleted — `e2e/**` + `@smoke` + `playwright.config.ts` untouched; the capture harness gained only the additive `avatarScreen` mirror type). **Supervisor: capture next.** The grade is the world-entry / walk / hub shot (back-turned WHOLE ranger + cabin landmark off-left + ground + horizon) AND `cam.avatarScreen` (onScreen, |x|/|y|<0.6, heightFrac not tiny) — the objective proof that ends the 5× subjective murk/speck misreads; re-grade F-07's proportion alongside it.
- **P1.1 (F-07) RE-GRADE — fresh 23:27–23:28 laptop capture (25 shots, coherent set, annotations 23:29; NO stale leftovers — all 5 graded PNGs mtime-verified) — GRADE FAIL (shot), box left open, NOT ticked. And the machine signal STEER #2 added is itself DISPROVEN by the pixels (F-18 again).** Assert GREEN (`avatar.height`=1.7 on every in-world shot; scale genuinely fixed — 08-walk-3 shows normal-size conifers on real ground+horizon, 11-controls-hud a normal-size log cabin, giant signature GONE) and the 03/04/05 idle triple `cam.dist`=4.85412196 is stable to <1e-8 (±0.01). BUT the shot criterion "ranger next to tree/hut at believable proportion" fails a SIXTH identical time — the ranger is NEVER SHOWN at proportion: **03-world-entry + 22-board-affordance are full reddish-brown near-DOF murk** (no ranger, no cabin, no ground/horizon), **16-jeep-near is a near-black brown VOID** (no jeep, no ranger, no world — just the "Stap in de jeep" chrome), **11-controls-hud** has the believable cabin but a dark/brown mass hanging across the TOP and no readable ranger where the hook places him, and **08-walk-3 is a fully readable world whose CENTRE IS EMPTY GROUND** (no ranger anywhere). **The redirect for the next sitting: `cam.avatarScreen` — the signal STEER #2 introduced as the OBJECTIVE anti-murk proof — is ITSELF now pixel-contradicted (a NEW F-18 lie in a NEW field).** It reads `onScreen`=true, |x|/|y|<0.11, `heightFrac`=0.29–0.34 and `avatarOpacity`=1 on 03/08/11/16/22 — i.e. it CLAIMS a prominent, solid, centre-frame ranger at ~⅓ viewport height on exactly the frames whose pixels show empty ground (08) or murk/void with no ranger at all (03/16/22). Per §4 pixels are the court of appeal: the avatarScreen projection is lying, so it CANNOT certify the shot — it must be fixed to agree with the render before it can gate anything (the back-turn `rotation.y=π` + cabin-move fix did NOT compose the shot; the "reddish murk = front-on coat" theory is not what the fresh pixels show). **NOTE TO FLORIS (per the monitor's own §8 note): this box has now failed identically 6×, steer #2 did NOT land, and the very machine signal added to end the subjective misreads is itself pixel-contradicted — the autonomous loop cannot crack this camera/spawn framing unaided. This is the hands-on point: a human camera+spawn fix (compose ground+horizon+WHOLE solid ranger beside the cabin at world-entry, and make avatarScreen match the render) rather than more spins.**
- **P1.1 (F-07) — ROOT CAUSE FOUND + FIX landed (code only; NOT captured/ticked/committed). The 6× "murk" was NEVER the camera — it was the ranger rendering as a ~170 m GIANT, and `avatar.height`=1.7 was an F-18-class bind-pose LIE the whole run chased the wrong lever behind.** The prior sitting's own throwaway probes (`app/e2e-probe/probe2,3.spec.ts` + `/tmp/ranger-probe*`) already proved it via a tint test and never made it into §8: the player `char1` SkinnedMesh renders at **posedWorldBBoxY 169.96 m / skeletonWorldBBoxY 160.09 m** while `Box3.setFromObject` (what `avatarState`/`measureAvatar`/`camState` all read) measures only the **bind-pose GEOMETRY box = 1.7 m**. Mechanism (probe scaleChain): the rig is `char1`(1) → `Armature`(0.01) → `Scene`(**188.89**); the mesh geometry is 0.9 units (×1.889 worldScale = 1.7 m, the "honest" number), but the **bones are ~94× the geometry** (≈85 armature-units → ×0.01×188.89 = 160 m), so the bone-driven render is a 170 m giant whose dark coat filled the follow lens = the reddish-brown murk (center RGB≈(31,19,6); it recedes to the frame edge as he walks — 08-walk-3 right-mass, 10-walk-5 left-mass, 11-controls-hud top-mass — and re-buries at the jeep, 16-jeep-near). `prepModel` CAUSED the 188.89: it measured the collapsed bind box (~0.009 m native) and scaled `Scene` up 188.89× to force THAT to 1.7 m, dragging the skeleton to 160 m. Every F-05 boom/fade/spawn/back-turn/avatarScreen iteration was chasing a symptom; all of MONITOR STEER #1/#2's dune/boom/fade/front-on-coat theories are disproven (the boom was healthy, `cam.dist`≈4.85, opacity 1 — the giant just isn't measured by the bind box). **FIX (app/src/**):** new `Models.skinnedRenderBox()` measures the SKELETON's world extent (the size that renders, pose-independent — reliable at bind pose / on a detached clone / live); `prepModel` scales rigs off it (static props unchanged → geometry box); `World.avatarState`/`measureAvatar`/`camState` all read it, so `avatar.height` + `cam.dist`/`avatarInView`/`avatarScreen` finally reflect the render. **Verified (probe4, NEW tree, deleted-able; `e2e/**`+`@smoke`+`playwright.config.ts` untouched):** renderedPosedHeight **1.803 m**, skeletonHeight **1.700 m**, `avatar.height`=**1.700**, feet≈ground; a live browser screenshot shows a believable human-size ranger back-turned on ground+horizon with the log cabin off-LEFT as the same-frame landmark and normal-size conifers/signpost — F-07's proportion shot composes for the first time, and `cam.avatarScreen` (heightFrac 0.32, |x|≈0, y=−0.095) now AGREES with the render (the 6th-grade avatarScreen "lie" was also just the giant). `npm run build` + frozen `@smoke` **4/4** GREEN. **Supervisor: capture next.** The grade is world-entry (whole ranger + cabin + ground/horizon at believable proportion) + `avatar.height`∈[1.5,2.0] + `avatarScreen` matching the pixels; F-05's rig was fine all along, so re-check its shots fall out of the same capture. NB feet sit ~5 cm low (skeleton toe-bone re-seat) — invisible here, flag only if a later grade wants it tightened.
- **P1.2 (F-05 ⊕ F-18) — the whole `app/src` fix ALREADY LANDED coupled with P1.1 (§3/§7: "F-05 ⊕ F-18 land together, scale first"); this sitting adds the ONE unrealized element of F-18's concrete fix — the harness PIXEL COURT OF APPEAL (harness-only; NOT captured/ticked/committed).** Once the P1.1 root-cause `skinnedRenderBox` fix made the ranger honestly 1.7 m, F-05's rig composed — so P1.1's own commit (3fa7c88) already carries every P1.2 element, verified line-by-line in HEAD: the F-05 clearance boom (`camOffset` eye 2.4 / boom 4.6 — steeper than the finding's "few degrees" because the FROZEN tap-to-walk `@smoke` taps 32 % from the top and needs the horizon above that row; World.ts ~103), `boomClearFraction` spherecast push-in + `minFrac` clamp (`avatarRadius`+near+0.2) + `setAvatarOpacity` fade rail (~2718–2827), and F-18's `camState` read off `this.camera` AFTER the frame update — `dist` / quaternion `yaw`+`pitch` / position / whole-bbox-in-frustum `avatarInView` / `avatarScreen` / `avatarOpacity` (~1285) — plus same-frame `drawCalls` (Stage.ts ~152: `lastDrawCalls = renderer.info.render.calls` the instant after `render`) and the `world-idle`/`world-idle-hold` idle pair (capture.spec.ts ~217). Grade-relevant asserts check out on inspection: `cam.dist`≈4.85 (=hypot(4.6, 2.4−0.85)) ≥ 3; the whole 1.7 m box sits inside the 55° frustum at world-entry (head ≈−8.6°, feet ≈−27.5°, both within the +11.5°…−43.5° vertical span) → `avatarInView`=true; the idle pose is fully determined by static `ranger.position` + `followYaw`=π (snapped at spawn) → yaw/dist steady (the §8 grades already saw the idle triple stable to <1e-8). **The gap I closed:** F-18's concrete fix ends "…and the capture harness keeps **pixel-diff as the court of appeal** for every camera assert", but the harness emitted NO pixel signature — the §8 grades md5'd the idle triple BY HAND. Added `pixelHash` (md5 of each shot's PNG bytes via `node:crypto` — a Node built-in, NO new dep) to every annotation in `snap()`, so the `world-idle`/`world-idle-hold` pair reads IDENTICAL hashes when the pose is genuinely still — machine-proving the ±0.01 idle assert from the RENDER, not the hook that moved yaw between pixel-identical frames in Run A (§4). It also catches the inverse (a camera claim that "changed" while the hash holds = a telemetry lie), so future F-16/F-17 zoom/orbit asserts inherit a court of appeal too. Harness-only (`app/e2e-capture/capture.spec.ts`); frozen `e2e/**` + `@smoke` + `playwright.config.ts` untouched; esbuild bundles the spec clean (the full `tsc` capture-check wants `@types/node`, absent in this sandbox but a pre-existing transitive dep the file already required via `node:fs`/`node:path`). **Supervisor: capture next.** Grade = world-entry shot (ground+horizon+whole ranger) + `cam.dist`≥3 + `avatarInView`=true, AND `world-idle` vs `world-idle-hold` with `cam.yaw`/`cam.dist` within ±0.01 **and now their `pixelHash` identical** (the render-side proof); F-05's boom/fade/pose all fall out of the same capture.
- **P1.3 (F-09 · rotate the spawn toward the hub) — FIX landed (code only; NOT captured/ticked/committed).** The titular defect survived P1.1/P1.2: the spawn faces −z with the cabin already forward-left (moved there for the F-07 proportion shot), but the **mission board — the actual content hub — sat at (3.6, 2.6) = +z, straight BEHIND the back-turned ranger and off the world-entry frame**, so holding forward walked AWAY from the hub into the heath (F-09's "hub sits behind the player" verbatim). Rotating can't fix it (a +z object can't enter a −z frame without moving), and the FROZEN tap-to-walk `@smoke` pins the −z/π bearing + downpitch — so per the finding's own alt ("or move the hub into the current sightline") I **moved the case-board to (2.2, −5)**: forward (−z), off to the RIGHT, a mirror of the cabin's forward-LEFT (World.ts ~1631). Now world-entry composes **cabin (left) + Missiebord halo/label (right) + the bos tree line ahead** (`candidates()` keeps forest ≥12 m out, so 0–12 m is open clearing → a real tree line at depth), and the first stride heads TOWARD content. Placement is contract-safe: its r=0.7 circle spans x∈[1.5,2.9] (OFF the x≈0 tap-to-walk lane), ~1.3 m clear of the spawn→watchtower diagonal the `paths` @smoke walks, 5.5 m out (> the 2.4 m board proximity, so the hub prompt does NOT fire at spawn), and inside `candidates()`'s r<12 clearing skip (no tree clips it). **Assert (F-09's "dev-hook landmark-in-frustum boolean"):** added `cam.landmarkInView` (devhook `CamState` + World.camState ~1333 + the capture mirror type) — reuses the live frustum already built for `avatarInView` and tests the cabin / board / beacons (lifted 1 m) with the SAME plane-distance idiom (no new three API); TRUE = a hub landmark is genuinely in the frame (board reads comfortably in at ~13° H / +10° V; the pixels stay the court of appeal, §4). Verified: `npm run build` (tsc+vite) GREEN; capture spec compiles (`--list`); frozen `movement`@smoke (ranger still walks ≥2 m — corridor intact) + `journey`/`ground`/`paths`@smoke + `board.spec` (board still reachable + opens at its new pos; watchtower diagonal not blocked) all GREEN; `e2e/**` + `@smoke` + `playwright.config.ts` untouched (only the additive `landmarkInView` mirror field in `e2e-capture/**`). **Supervisor: capture next.** Grade = world-entry shot (≥1 landmark in frame — cabin left + Missiebord right + tree line) + `cam.landmarkInView`=true at world-entry (and world-entry `drawCalls` well above the old void's 13).
- **P1.4 (F-08 · retune foot speed + tie stride to ground speed) — GRADE GREEN, ticked `[x]` with the +demo suffix (01:54–01:56 laptop capture, 25 shots).** ASSERT green: `annotations-laptop.json` reads `groundSpeed` **1.800** on every walk shot (06–10) and **1.643** at jeep-near (16) — all in-band [1.4, 2.2] while `clip`=walk; idle/hub/board/mission shots read 0.000 (correct 0-while-standing). It is an HONEST measurement, NOT an F-18 echo: `World.groundSpeed()`→`playerSpeed = moved / dt` (post-collision, World.ts ~2538), the 1.643≠1.800 variation proves it's derived from real motion, and the walk-4→walk-5 wall-clock (1.8 m in ~1 s from pos/​mtime) corroborates 1.8 m/s. Retune landed: `speed = 1.8` (World.ts:238, in the ≈1.6–2.0 target); stride TIED — `walk.timeScale = strideRate(speed)` (`speed / STRIDE_MATCH_SPEED` ~1.5, clamped; PlayerRig ~78, PlayerAnim `strideRate`), so distance-per-cycle tracks real speed → foot-slip killed in code. Jeep stays clearly faster (`veh.speed` 4.243 vs foot 1.8). SHOT green: the burst (06/08/10-walk) shows the WHOLE back-turned ranger at believable human proportion walking on ground+horizon past cabin (left) + Missiebord (right), normal-size conifers + a cast shadow, stride pose changing frame-to-frame; burst distance **9.0 m** vs Run A's 21.2 m — the near-jeep glide is gone. `npm run build` + frozen `e2e:smoke` GREEN (tick gate). **+demo:** the box carries "implemented — awaiting demo", NEVER "fixed" — the true gait FEEL / perceived foot-slip is Floris's on-device call (DEMO · Motion comfort).
- **P1.5 (re-check F-10 / F-12 / F-33) — RE-CHECK DONE; all three DISSOLVED; NO survivor → NO `app/src` change (§3 "re-checks, not fixes" + §6 "do not invent a pass"); NOT captured/ticked/committed.** Graded the freshest coherent laptop set (01:54–01:56, 25 shots); did NOT run capture (supervisor next). **F-10 gait judgeable — DISSOLVED:** walk burst 06–10 shows the WHOLE back-turned 1.7 m ranger on ground+horizon (cabin left, Missiebord right), `clip`=walk with `time` cycling (0.41→0.10→0.63→0.91→0.02) and `groundSpeed`=1.8 on every walk shot, stride pose changing frame-to-frame — the Run-A camera-inside-mesh / ankle-between-giant-legs un-judgeability is gone (gait FEEL stays +demo — Floris). **F-12 shadow band — DISSOLVED:** no camera-locked vertical dark band at screen centre across the burst; the ranger's cast shadow is now small + believable (10-walk-5). Root cause killed by F-07: the sun shadow-map ortho frustum (±16 m, World.ts ~389) follows the ranger and is driven by the now-1.7 m mesh, so the F-07-giant directional shadow F-12 blamed can't exist; residual soft ground darkening = the intended drifting cloud-shadow atmosphere (Atmosphere.ts/buildCloudShadows) + front-to-back light gradient = "believable prop shadows" (F-12's own pass wording). Drive-burst jeep shadows move WITH the jeep (under→left→under→right, 18–21) = cast shadows, not a fixed band. **F-33 "blur/DOF" — DISSOLVED:** walk + drive bursts crisp (cabin logs, board papers, jeep, conifers all sharp), no magnified-texture murk, `drawCalls` ≤66 (<150); Run A already code-proved no postprocess pass exists and F-07 removed the "textures magnified 10–30× seen from cm away" cause. **No survivor → no new finding, no code diff.** (Drive FRAMING oddities — jeep off-centre / empty seat — are F-30/F-31 Phase 2, not F-12/F-33.) Supervisor: capture next; grade = the walk-burst shot (gait readable + ground evenly lit + geometry crisp), which the 01:54 set already satisfies.
- **P1.5 (re-check F-10 / F-12 / F-33) — GRADE GREEN on the fresh 02:14–02:16 laptop capture (25 shots); ticked `[x]` with the +demo suffix (F-10 gait FEEL stays Floris's on-device call). No survivor → no `app/src` change (re-check).** Graded MY OWN fresh set per §1 (not the prior 01:54 set the fix-half saw). **F-10 judgeable — DISSOLVED:** 06/08/10-walk show the WHOLE back-turned ~1.7 m ranger centre-frame at believable human proportion (cabin left, Missiebord right, normal-size conifers + horizon), mid-stride with legs visibly moving; `clip`=walk with `time` cycling 0.394→0.112→0.646→0.901→0.005 + `pos` advancing + `groundSpeed`=1.8 → gait reads as walking; the Run-A camera-inside-mesh / ankle-between-giant-legs un-judgeability is gone. **F-12 half-screen band — DISSOLVED:** no camera-locked vertical dark band at screen centre across the burst; ground evenly lit apart from the ranger's own small cast shadow + soft off-centre cloud-shadow drift (shadow masses flip sides 08↔10, not fixed to the lens). **F-33 blur — DISSOLVED:** walk (cabin logs / board papers / conifers) AND drive (17-jeep-in clean jeep model, 20-drive-3 jeep+helicopter+deer+cones) all crisp; the cited jeep-in "white speckled magnified-texture band" is gone; `drawCalls` <150 everywhere (walk 47/32/21/20/15, drive 65/14/28/33, jeep-in 70). **Pixels AGREE with the hook this time (no F-18 lie):** `avatar.height`≈1.70 on every in-world shot, `avatarInView`=true / `avatarOpacity`=1 / `avatarScreen` heightFrac 0.29–0.32 matches the framed ranger the pixels actually show — the P1.1-era avatarScreen lie is resolved by the giant-fix. `npm run build` + frozen `e2e:smoke` GREEN (tick gate). No code diff; no new finding opened.
- **GATE-P1 (Fable re-judge, fresh 02:23–02:24 laptop capture, 25 shots) — ALL FIVE P1 BOXES AGREED GREEN, no re-opens; gate LEFT UNTICKED with three world-look triage boxes added (P1.6–P1.8) per the gate's own "first real look" mandate.** The pixels and the hook finally AGREE everywhere I probed (no F-18-class lie): `avatar.height` 1.68–1.71 on every in-world shot with the ranger visibly ~⅓ frame height (`avatarScreen.heightFrac` 0.32 matches the render); the 04/05 idle pair is camera-identical to ~1e-12 and 03/04 are byte-identical by `pixelHash` (the 04→05 pixel delta is the idle CLIP at time 0.007→0.157, not camera motion). Verdicts — **F-07 ✓** (03/11/12/16: whole ranger at believable proportion beside the cabin, from both sides, and the jeep); **F-05⊕F-18 ✓** (03: ground + horizon + whole SOLID ranger, `cam.dist` 4.854 ≥ 3, `avatarInView` true, opacity 1); **F-09 ✓** (cabin left + Missiebord right + tree line in the entry frame, `landmarkInView` true, drawCalls 51 vs the old void's 13; the board affordance fires by walk-2 — the first stride goes toward content); **F-08 assert ✓** (`groundSpeed` 1.800 on all five walk shots, jeep 4.243 clearly faster — stays "implemented, awaiting demo", NOT upgraded); **F-10/F-12/F-33 re-checks ✓ dissolved** (gait readable at proportion; no camera-locked centre band — the fresh bursts' dark masses migrate 07→10, carry signpost-cross/conifer-tip object shadows and are absent at entry, i.e. drifting cloud layer + long prop shadows; geometry crisp throughout incl. helicopter rotor + cone bands, drawCalls ≤65). TRIAGE: (1) **F-19 and F-24 are ORPHANS — assigned to NO ledger box anywhere** (Self-audit §5 mapped them only as F-07 murk-backdrop; their own framing/content fixes were never boxed — a plan gap this gate closes). Fresh pixels re-prove the surviving halves: 22-board-affordance shows the board edge-on at the frame edge, face never presented → **P1.6**; 24-mission-3d is now lit + composed (board face visible — F-24's framing half landed with F-05/F-07) but the frisling renders as a featureless pale egg and "het gras" as rock lumps → **P1.7**. (2) NEW lighting finding: the drifting cloud shadows black out ~half of walk-4/5 and drive-4 with unreadable murk inside → **P1.8** (soften opacity / feather the edge; drift itself is intended atmosphere). Otherwise the composed world reads coherent — hub (cabin, board, signpost), vehicle pool (jeep + helicopter), watchtower on the drive horizon, fence lines, cones, distant calm-pose animals, dune horizon — so no biome-density or prop-placement box is warranted. Builder: do P1.6–P1.8; a fresh Fable sitting then re-judges GATE-P1.
- **GATE-P1 second Fable sitting (fresh 02:37–02:39 laptop capture, 25 shots, all world frames 03–25 eyeballed) — VERDICT UNCHANGED: P1.1–P1.5 re-affirmed green (no re-opens), P1.6–P1.8 independently RE-CONFIRMED on my own set, NO new triage, gate LEFT UNTICKED because three non-demo Phase-1 boxes are open and unaddressed (no `app/src` change since P1.5's commit — the supervisor spawned this gate before the builder took the triage boxes, so the sitting is a confirm-and-hold).** Pixels + hook agree everywhere: 03-world-entry composes ground+horizon+WHOLE solid ranger at believable proportion, cabin left + Missiebord right + tree line (`avatar.height` 1.66–1.71 all in-world shots, `cam.dist` 4.854 ≥3 idle-stable to ~7e-8 across 03/04/05, opacity 1, `landmarkInView` true, drawCalls 51); proportion holds from the far side (12/13/14), beside the cabin (11) and beside the jeep (16); walk burst 06–10 shows the whole back-turned ranger mid-stride at `groundSpeed` 1.800 with the board affordance firing by walk-2 (jeep 4.243 clearly faster; F-08/F-10 stay "implemented, awaiting demo" — NOT upgraded); no camera-locked band, geometry crisp, drawCalls ≤71. Triage re-proof from MY frames: **P1.6** — 22-board-affordance now cuts the board face OBLIQUE at the RIGHT frame edge (approach-angle nondeterminism, same defect: papers never readable, camera jams tight on a front-on ranger); **P1.7** — 24-mission-3d frisling is still a featureless pale egg beside gras-as-rock-lumps (vignette framing itself composed: board face + spotlight + prompt chips); **P1.8** — cloud murk blacks out half of 09/10-walk with a hard edge and buries the drive burst (18/19 haziest, 21 right-third) — add 18/19 as cites. Everything else seen is already owned downstream (empty driver seat → P2.4, "Het stille zand" label occlusion → P4.3, text-only cards → P4.4, board exit → P3.2, pause links/scrim → P3.2/P4.7, 16-word toast → P3.3, zoom inert → P2.1); biome density + prop placement read coherent (heide/bos/stuifzand transitions, watchtower, fences, cones, calm-pose boar/deer at 15/16/19/20). Builder: land P1.6–P1.8, then a fresh Fable sitting re-judges GATE-P1.
- **GATE-P1 third Fable sitting (fresh 02:45–02:47 supervisor capture, 25 shots, coherent set; STILL zero `app/src` change since P1.5's commit 447f5d3 — second consecutive confirm-and-hold on an unchanged tree) — VERDICT UNCHANGED: P1.1–P1.5 re-affirmed green from my own frames (no re-opens), P1.6–P1.8 independently RE-CONFIRMED, NO new triage, gate LEFT UNTICKED because three non-demo Phase-1 work boxes are open and untouched.** Pixels + hook agree everywhere probed: 03-world-entry composes ground + horizon + WHOLE solid ranger at believable proportion, cabin left + Missiebord face-with-papers right + conifer line (`avatar.height` 1.70 on every in-world shot; `cam.dist` 4.854 ≥3, idle triple stable to <1e-7 with 03/04 `pixelHash` byte-identical — the 04→05 delta is the idle CLIP at time 0.172→0.041, camera fields static; `landmarkInView` true, drawCalls 51, and it flips false exactly on the landmark-free 09/10 frames, so the boolean tracks the render); proportion holds beside the cabin (11/12/14), the board (07/22) and the jeep (16); `groundSpeed` 1.800 on all five walk shots vs jeep 4.243 (F-08/F-10 stay "implemented, awaiting demo" — NOT upgraded); no camera-locked band (dark masses migrate 06→10, carry prop shadows, absent at entry = the P1.8 cloud layer, not F-12 returned); geometry crisp throughout, drawCalls ≤66. Triage re-proof on MY set: **P1.6** — 22-board-affordance shows the board EDGE-ON at the right frame edge, face/papers never presented, cabin centre-frame instead (third distinct approach angle, same defect); **P1.7** — 24-mission-3d is a lit composed vignette but "het gras" props are dark angular rock-lumps and NO recognizable piglet silhouette (ears/snout/legs) exists anywhere in frame; **P1.8** — the cloud layer blacks out the left ~half of 09/10-walk with unreadable murk inside a hard edge, hazes 19-drive mid-frame and buries the right third of 21-drive. Chased the one candidate NEW defect to ground: the dark maroon blob at the ranger's head in 03/25 is his own back-of-head HAIR (verified by 2× crop — board face composes cleanly behind it), not a floating artifact → no new box; 15-pause cabin "tilt" is edge-of-frame wide-angle stretch (level in 03/11/12/14/22). LOOP NOTE: the first unchecked ledger box is P1.6 — a WORK box — yet the supervisor has now spent two Fable gate sittings in a row on an unchanged tree; the next sitting must be the Opus BUILDER on P1.6–P1.8, and only after those land should a fresh Fable sitting re-judge GATE-P1.
- **GATE-P1 fourth Fable sitting (fresh 02:56–02:57 supervisor capture, 25 shots; THIRD consecutive confirm-and-hold — still zero `app/src` change since P1.5's commit 447f5d3) — VERDICT UNCHANGED: P1.1–P1.5 re-affirmed green from my own frames (no re-opens), P1.7 + P1.8 independently RE-CONFIRMED, NO new triage, gate LEFT UNTICKED because three non-demo Phase-1 work boxes stay open.** Re-affirm evidence: 03-world-entry composes ground + horizon + WHOLE solid ranger at believable proportion with cabin left + Missiebord right + conifer line (`avatar.height` 1.69–1.71 on every in-world shot; `cam.dist` 4.854 ≥ 3 with 03/04 `pixelHash` byte-identical and the idle triple's cam fields stable to <1e-7 — the 04→05 pixel delta is the idle clip; opacity 1, `landmarkInView` true, drawCalls 51); proportion holds beside the cabin (11), from the far side (12/14), at the board (07/22) and beside the jeep (16); `groundSpeed` 1.800 on all five walk shots vs jeep 4.243 (F-08/F-10 stay "implemented, awaiting demo" — NOT upgraded); no camera-locked band (dark masses migrate 08→09/10, carry a signpost-cross object shadow, absent at entry); geometry crisp throughout (cabin logs, board papers, heli rotor, cones), drawCalls ≤71. **NEW DATUM on P1.6 (box stays open):** for the FIRST time in four captures, 22-board-affordance composes the board FACE-ON with papers readable — the straight-line −z spawn approach presents the +z face the P1.3 move gave it, so F-19's "angle the face toward the natural approach" half already works when the harness walks the natural line; the box still can't close because the pass is approach-luck (3 of 4 captures landed edge-on/oblique), the damp-turn reframe on near-radius entry is unbuilt, and the `board-in-frustum while board.near` assert does not exist. P1.7 re-proven on my set: 24-mission-3d is a lit composed vignette but the frisling is STILL a featureless pale egg (no ears/snout/legs) beside "het gras" as dark rock lumps. P1.8 re-proven: 09/10-walk lose the left ~half to unreadable murk behind a hard edge; 18-drive buries the bottom third (incl. an ambiguous hard-edged dark slab inside the murk — expect the feather/opacity fix to resolve or expose it) and 19-drive hazes mid-frame. **LOOP MISCONFIGURATION, to Floris:** three Fable gate sittings in a row have now burned on an unchanged tree while the first unchecked ledger box is the WORK box P1.6 — the supervisor must dispatch the Opus BUILDER at P1.6–P1.8 (or Floris lands them by hand) before any further gate sitting spends tokens re-proving the same three defects.
- **GATE-P1 fifth Fable sitting (fresh 03:04–03:06 supervisor capture, 25 shots, all frames eyeballed incl. title/avatar; FOURTH consecutive confirm-and-hold — still zero `app/src` change since P1.5's commit 447f5d3; the only tree change is the P1.6 DEFER park, b76cc5f) — VERDICT: P1.1–P1.5 re-affirmed green from my own frames (no re-opens), P1.7 + P1.8 independently RE-CONFIRMED broken, NO new triage, gate LEFT UNTICKED because two non-demo Phase-1 work boxes (P1.7, P1.8) are open and untouched.** Re-affirm evidence: 03-world-entry composes ground + horizon + WHOLE solid ranger at believable proportion, cabin left + Missiebord right + conifer line (`avatar.height` 1.69–1.71 on every in-world shot; `cam.dist` 4.854 ≥ 3 with 03/04 `pixelHash` byte-identical and the idle triple's cam fields stable to <1e-7 — the 04→05 delta is the idle clip at time 0.225→0.089; opacity 1 everywhere; `landmarkInView` true at entry and false exactly on the landmark-free 09/10 frames, so the boolean tracks the render; drawCalls 51, max 70 across the set, all ok:true, no GAPs); proportion holds beside the cabin (11), from the far side (12/14), at the board (07/22) and beside the jeep (16); `groundSpeed` 1.800 on all five walk shots vs jeep 4.243 with stride pose changing frame-to-frame (F-08/F-10 stay "implemented, awaiting demo" — NOT upgraded); no camera-locked band (dark masses migrate 08→09/10, absent at entry, signpost shadow readable INSIDE the 08 mass); geometry crisp throughout (cabin logs, board papers, heli rotor, cones, deer), F-12/F-33 stay dissolved. P1.7 re-proven on my frame: 24-mission-3d is a lit composed vignette but the frisling is STILL a featureless pale egg (no ears/snout/legs) beside "het gras" as dark angular rock lumps. P1.8 re-proven: 09/10-walk lose the left ~half to unreadable murk behind a hard diagonal edge; 18-drive buries the bottom third (hard-edged dark slab inside), 19 hazes mid-frame, 21 blacks the right third over the conifers. P1.6 (now DEFERRED, parked 03:04 after 3 failed builder sittings): accepted per the P0.2/GATE-P0 precedent — a parked box moves out of the loop's scope and does not block the gate; its fix is the hands-on look DEFERRED.md assigns. NEW DATUM for that hands-on fix: this capture's 22-board-affordance composes the board FACE-ON with papers readable (second face-on in five captures) — the "angle the face toward the natural approach" half already works when the harness walks the straight −z line; what remains unbuilt is the damp-turn reframe + the `board-in-frustum while board.near` assert that would make it deterministic. Everything else seen is already owned downstream (empty driver seat + `clip`=idle while `inVehicle` → P2.4; zoom/orbit inert, `cam.dist` unchanged across 12/13/14 → P2.1/P2.2; 16-word toast → P3.3; pause/board link sizes + scrim → P3.1/P3.2/P4.7; text-only mission cards → P4.4; title subtitle flow + sparse backdrop → P4.1/P4.5). **LOOP DISPATCH, to Floris: the P1.6 park did NOT change what the loop must do next — the first unchecked non-DEFERRED box is the WORK box P1.7, yet a FIFTH Fable gate sitting was spawned on an unchanged tree. No gate sitting can change this verdict until the Opus BUILDER lands P1.7 then P1.8; dispatch the builder (or hand-fix) before spending another gate sitting.**
- **GATE-P1 sixth Fable sitting (fresh 03:15–03:16 supervisor capture, 25 shots, all frames eyeballed; FIFTH consecutive confirm-and-hold — still zero `app/src` change since P1.5's commit 447f5d3) — VERDICT UNCHANGED: P1.1–P1.5 re-affirmed green from my own frames (no re-opens), P1.7 + P1.8 independently RE-CONFIRMED broken, NO new world-look triage, gate LEFT UNTICKED because two non-demo Phase-1 work boxes (P1.7, P1.8) remain open and untouched.** Re-affirm: 03-world-entry composes ground + horizon + WHOLE solid ranger at believable proportion, cabin left + Missiebord face right + conifer line (`avatar.height` 1.695–1.712 on every in-world shot; `cam.dist` 4.854 ≥ 3 with the idle triple stable to <2e-7 and 03/04 `pixelHash` byte-identical — the 04→05 delta is the idle clip; opacity 1 everywhere; drawCalls 51 at entry, max 66, all ok:true, no GAPs); proportion holds at the board (07/22 — board FACE-ON with papers readable, third face-on in six captures, more weight to the parked P1.6's "natural approach already presents the face" datum), beside the jeep (16) and from the far side (12/13/14); `groundSpeed` 1.800 on all five walk shots vs jeep 4.243, stride pose changing frame-to-frame, burst 9.2 m vs Run A's 21.2 m (F-08/F-10 stay "implemented, awaiting demo" — NOT upgraded); no camera-locked band (dark masses migrate 08→09/10, absent at entry); geometry crisp (cabin logs, board papers, heli rotor, cones, deer) — F-12/F-33 stay dissolved. P1.7 re-proven on my frame: 24-mission-3d frisling is a featureless pale egg (no ears/snout/legs) beside "het gras" as dark angular rock-lumps. P1.8 re-proven: 09/10-walk lose the left ~half to unreadable murk behind a hard edge; 18-drive buries the bottom third (hard-edged dark slab inside), 19 hazes mid-frame, 21 darkens the right third. **NEW SUBSTRATE DATUM (no box — substrate, not world-look; flagged for the P2 sittings): shot 11-controls-hud's cam telemetry does not describe its own pixels.** The annotation reads cam (2.0, 2.1, −7.1) yaw 0.475 — north of the ranger looking away from the hub, `landmarkInView` false — while the RENDER is from ≈yaw π SOUTH of the ranger with cabin + board-back + vehicle pool in frame (≈ 12/14's settled pose; the pixels of 11/12/14 are near-identical compositions). Mechanism: the controls-hud drag misfires tap-to-walk (F-17's known seam, P2.2), the follow camera swings ~180° during the step, and the harness's state-read and PNG snap race on that fast-moving frame — an F-18-class residue confined to IN-MOTION shots (every settled shot's cam fields are pixel-corroborated: 03/04/05 and 12/13/14 match their renders exactly, so no P1 criterion is touched and P1.2 stays ticked on its own verify-by). P2.2/P2.5's moving-camera asserts must settle-wait or same-frame the state-read + screenshot in `app/e2e-capture/**` before trusting cam fields on such frames — otherwise F-17's "drag changes real camera yaw" assert can pass or fail off pixels from a different instant. **LOOP DISPATCH unchanged, to Floris: the first unchecked non-DEFERRED box is STILL the WORK box P1.7, and this was the SIXTH consecutive gate sitting on an unchanged tree. Dispatch the Opus BUILDER (or hand-fix) at P1.7 then P1.8; no gate sitting can change this verdict until they land.**
- **GATE-P1 seventh Fable sitting (fresh 03:28–03:30 supervisor capture, 25 shots, all ok:true, all frames eyeballed; SIXTH consecutive confirm-and-hold — `git diff 447f5d3..HEAD -- app/src app/e2e-capture` is EMPTY and the working tree carries no app/ change, so these pixels render the exact code five prior sittings judged) — VERDICT UNCHANGED: P1.1–P1.5 re-affirmed green from my own frames (no re-opens, +demo boxes NOT upgraded), P1.7 + P1.8 independently RE-CONFIRMED broken, NO new triage, gate LEFT UNTICKED because two non-demo Phase-1 work boxes (P1.7, P1.8) remain open and untouched.** Re-affirm: 03-world-entry composes ground + horizon + WHOLE solid ranger at believable proportion, cabin left + Missiebord face-with-papers right + conifer line (`avatar.height` 1.685–1.712 on every in-world shot; `cam.dist` 4.854 ≥ 3 with the idle triple stable to <1e-13 and 03/04 `pixelHash` byte-identical — the 04→05 delta is the idle clip at time 0.221→0.091; opacity 1 everywhere; `landmarkInView` true at entry and false exactly on the landmark-free 09/10; drawCalls 51 at entry, max 65, no GAPs); proportion holds beside the cabin (11/12/13/14), at the board (07) and beside the jeep (16); `groundSpeed` 1.800 on all five walk shots vs jeep 4.243, stride pose changing frame-to-frame; no camera-locked band (murk masses sit left in the walk burst but bottom/mid/right across 18/19/21 — they move against the lens, F-12 stays dissolved); geometry crisp outside the murk (cabin logs, board papers, heli rotor, cones, watchtower, deer/boar) — F-33 stays dissolved. P1.7 re-proven on my frame: 24-mission-3d is a lit composed vignette (F-24's framing half holds) but the frisling is STILL a featureless pale egg — no ears/snout/legs/tail — beside "het gras" as dark angular rock-lumps, and NO mission-target-in-frustum field exists in the annotations. P1.8 re-proven: 09/10-walk lose the left ~half to unreadable murk behind a hard diagonal edge; 18-drive buries the bottom third (hard-edged dark slab inside), 19 hazes mid-frame, 21 blacks the right third over the conifers; 20 is mostly clean. P1.6 (DEFERRED, stays parked, does not block per the P0.2/GATE-P0 precedent) datum: THIS capture is an oblique miss again — 22-board-affordance shows the board angled away at the right frame edge, face/papers not presented at the affordance moment (face-on luck now 3 of 7 captures — the deterministic damp-turn remains unbuilt). Sitting six's substrate datum RECONFIRMED in fresh pixels: shot 11's cam telemetry (yaw 0.473, `landmarkInView` false) does not describe its own render (settled hub composition, cabin + board-back + vehicle pool plainly in frame) — the in-motion state-read/snap race is real and P2.2/P2.5 must settle-wait before trusting cam fields on moving frames; every settled frame's telemetry is pixel-corroborated, so no P1 verify-by is touched. Everything else seen stays owned downstream (zoom/orbit inert with `cam.dist` identical across 12/13/14 → P2.1/P2.2; empty driver seat + `clip`=idle while `inVehicle` → P2.4; "Het stille zand" label occluded by the jeep roof → P4.3; text-only mission cards → P4.4; 16-word toast → P3.3; pause/board link sizes + scrim → P3.1/P3.2/P4.7; title subtitle flow + sparse backdrop → P4.1/P4.5; avatar-card CTA below fold → P3.1). **LOOP DISPATCH, to Floris, now at SEVEN gate sittings on an unchanged tree: this is pure token burn — every future gate sitting will return this same verdict until code lands. Do not spawn another Fable sitting; dispatch the Opus BUILDER at P1.7 then P1.8 (or hand-fix), then ONE fresh gate sitting can close Phase 1.**
- **GATE-P1 eighth Fable sitting (fresh 03:38–03:40 supervisor capture, 25 shots, all ok:true, all frames eyeballed; SEVENTH consecutive confirm-and-hold — I re-verified `git diff 447f5d3..HEAD -- app/src app/e2e-capture` is EMPTY and the working tree carries no app/ change, so these pixels render the exact code seven prior sittings judged) — VERDICT UNCHANGED, with ONE ledger-state change since sitting seven: the supervisor DEFERRED P1.7 (b63335b), so the gate is now blocked by exactly ONE open non-deferred non-demo work box, P1.8 — and I RE-CONFIRM P1.8 broken in my own frames, so the gate stays UNTICKED.** P1.1–P1.5 re-affirmed green from my own frames (no re-opens; +demo boxes NOT upgraded): 03-world-entry composes ground + horizon + the WHOLE solid ranger at believable proportion, cabin left + Missiebord face-with-papers right + conifer line (`avatar.height` 1.689–1.712 on every in-world shot; `cam.dist` 4.854 ≥ 3 with the idle triple stable to <1e-7 and 03/04 `pixelHash` byte-identical; `landmarkInView` true at entry; drawCalls 51 at entry, max 67, all <150); proportion holds at the board (07/22), beside the jeep (16) and across the hub views (11/12/14); `groundSpeed` 1.800 on all five walk shots vs jeep 4.243, burst distance 9.2 m, stride pose changing frame-to-frame and readable as walking; geometry crisp outside the murk (cabin logs, board papers, heli rotor, army truck, cones, boar) — F-12/F-33 stay dissolved (the dark masses sit LEFT in the walk burst but bottom-left in 18/19 and RIGHT in 21 — they move against the lens, not with it). P1.8 re-proven on my frames: 09/10-walk lose the left ~half to unreadable murk behind a hard edge; 18/19-drive bury the bottom-left; 21 blacks the right third; 20 is mostly clean. P1.7 (now DEFERRED) re-proven broken for the record: the 24-mission-3d frisling is STILL a featureless pale egg — no ears/snout/legs/tail — beside "het gras" as dark angular rock-lumps, and no mission-target-in-frustum field exists in the annotations. P1.6 (DEFERRED) datum: my 22-board-affordance catches the board oblique at frame-LEFT, papers visible but edge-cut, not squarely presented. NO new world-look triage — everything else seen stays owned downstream (zoom/orbit inert with `cam.dist` identical across 12/13/14 → P2.1/P2.2; no visible driver + `clip`=idle while `inVehicle` → P2.4; labels/pictograms/toast/scrim/title/backdrop/card-fit → P3.x/P4.x). **LOOP DISPATCH, to Floris: EIGHT gate sittings on an unchanged tree — the sole gate-blocker is now the WORK box P1.8; dispatch the Opus builder (or hand-fix) there. And a policy question only you can answer: if the stall guard defers P1.8 too, should a phase close with THREE content boxes deferred? The P0.2/GATE-P0 precedent covered an iPad-scoped, laptop-unverifiable box — P1.7/P1.8 are laptop-PROVEN content defects, so I did not extend that precedent on my own.**
- **GATE-P1 ninth Fable sitting (fresh 03:49–03:51 supervisor capture, 25 shots, all ok:true, all frames eyeballed; EIGHTH consecutive confirm-and-hold — I re-verified `git diff 447f5d3..HEAD -- app/src app/e2e-capture` is EMPTY and the working tree carries no app/ change, so these pixels render the exact code eight prior sittings judged) — VERDICT UNCHANGED: P1.1–P1.5 re-affirmed green from my own frames (no re-opens; +demo boxes NOT upgraded), P1.8 RE-CONFIRMED broken in my own frames, NO new world-look triage, gate LEFT UNTICKED — the sole non-deferred non-demo blocker remains the OPEN WORK box P1.8.** Re-affirm: 03-world-entry composes ground + horizon + the WHOLE solid ranger at believable proportion, cabin left + Missiebord face-with-papers right + conifer line (`avatar.height` 1.677–1.713 on every in-world shot; `cam.dist` 4.854 ≥ 3 with the 03/04/05 idle triple stable to <1e-7 and 03/04 byte-identical by pixelHash; `landmarkInView` true at entry; drawCalls 51 at entry, max 67, all <150); proportion holds beside the cabin (11/12/14), at the board (07/22) and beside the jeep (16); `groundSpeed` exactly 1.800 on all five walk shots vs jeep 4.243, whole ranger visible with stride pose changing frame-to-frame and readable as walking (F-08/F-10 stay "implemented, awaiting Floris demo" — NOT upgraded); the dark masses move AGAINST the lens (left in the walk burst, bottom in 18/19, right in 21) and geometry outside them is crisp (cabin logs, board papers, heli rotor, army truck, watchtower, deer) — F-12/F-33 stay dissolved. P1.8 re-proven in MY frames: 09/10-walk lose the left ~half to unreadable murk behind a hard diagonal edge (a jagged black silhouette inside 09's bottom-left); 18-drive buries the bottom third with a hard-edged dark slab inside the murk; 19 sweeps a haze band across mid-frame; 21 darkens the right third over the conifer stand; 20 is mostly clean. Parked boxes re-proven broken for the record (they stay DEFERRED, off the loop's path): P1.7 — the 24-mission-3d frisling is STILL a featureless pale egg (no ears/snout/legs/tail) beside "het gras" as dark angular rock-lumps, and no mission-target-in-frustum field exists in the annotations; P1.6 — 22-board-affordance catches the papers plainly readable this time but the board is cut by the left frame edge (face-on luck ~4 of 9 captures — the deterministic damp-turn + `boardInFrustum` assert remain unbuilt). Sitting six's substrate datum re-confirmed in fresh pixels: shot 11's cam telemetry (cam.yaw 0.486, `landmarkInView` false) contradicts its own settled-hub render (cabin + board-back + heli/truck plainly in frame) — the in-motion state-read/snap race; P2.2/P2.5 must settle-wait or same-frame the read+snap before trusting cam fields on moving frames (no P1 verify-by rests on shot 11; every settled frame's telemetry is pixel-corroborated). Everything else seen stays owned downstream (zoom/orbit inert, `cam.dist` 4.854 identical across 12/13/14 → P2.1/P2.2; no visible driver + `clip`=idle while `inVehicle` → P2.4; 16-word toast → P3.3; pause/board link sizes + avatar-card CTA below fold → P3.1/P3.2; "Het stille zand" label occlusion → P4.3; text-only mission cards → P4.4; scrim → P4.7; title subtitle flow + sparse backdrop → P4.1/P4.5). **LOOP DISPATCH, to Floris: NINE gate sittings, EIGHT identical verdicts on an unchanged tree — the supervisor keeps spawning PHASE-GATE sittings while the first unchecked non-DEFERRED box, P1.8, is BUILDER work no gate sitting may perform (judges change no game code). Dispatch the Opus builder (or hand-fix) at P1.8 — soften the cloud-shadow opacity and/or feather its edge so ground detail stays readable inside a passing shadow — then ONE fresh gate sitting can close Phase 1. Sitting eight's policy question stands if the stall guard defers P1.8 too.**
- **GATE-P1 tenth Fable sitting (fresh 03:58–04:00 supervisor capture, 25 shots, all ok:true, all frames eyeballed; NINTH consecutive confirm-and-hold — I re-verified `git diff 447f5d3..HEAD -- app/src app/e2e-capture` is EMPTY and the working tree carries no app/ change, so these pixels render the exact code nine prior sittings judged) — VERDICT UNCHANGED: P1.1–P1.5 re-affirmed green from my own frames (no re-opens; +demo boxes NOT upgraded), P1.8 RE-CONFIRMED broken in my own frames, NO new world-look triage, gate LEFT UNTICKED — the sole non-deferred non-demo blocker remains the OPEN WORK box P1.8.** Re-affirm: 03-world-entry composes ground + horizon + the WHOLE solid ranger at believable proportion, cabin left + Missiebord face-with-papers right + conifer line (`avatar.height` 1.683–1.712 on every in-world shot; `cam.dist` 4.854 ≥ 3 with the 03/04/05 idle triple stable to <1e-7 and 03/04 byte-identical by pixelHash — the 04→05 delta is the idle clip at time 0.228→0.101; opacity 1 everywhere; `landmarkInView` true at entry and false exactly on the landmark-free 09/10; drawCalls 51 at entry, max 71, all <150); proportion holds beside the cabin (11/14), at the board (07/22) and beside the jeep (16); `groundSpeed` exactly 1.800 on all five walk shots vs jeep 4.243, burst distance 9.25 m, whole ranger visible with stride pose changing frame-to-frame and readable as walking (F-08/F-10 stay "implemented, awaiting Floris demo" — NOT upgraded); the dark masses move AGAINST the lens (left in the walk burst, bottom in 18, mid in 19, right in 21, absent at entry) and geometry outside them is crisp (cabin logs, board papers, heli rotor, army truck, cones, deer/boar) — F-12/F-33 stay dissolved. P1.8 re-proven in MY frames: 09/10-walk lose the left ~half to unreadable murk behind a hard diagonal edge (jagged black silhouette inside 09's bottom-left); 18-drive buries the bottom third with a hard-edged dark slab inside the murk; 19 sweeps a haze band across mid-frame; 21 darkens the right third over the conifer stand; 20 is mostly clean. Parked boxes re-proven broken for the record (they stay DEFERRED, off the loop's path): P1.7 — the 24-mission-3d frisling is STILL a featureless pale egg (no ears/snout/legs/tail) beside "het gras" as dark angular rock-lumps, and no mission-target-in-frustum field exists in the annotations; P1.6 — 22-board-affordance catches the board FACE-ON with papers plainly readable this capture (face-on luck ~5 of 10 — the deterministic damp-turn + `boardInFrustum` assert remain unbuilt). Sitting six's substrate datum re-confirmed in fresh pixels: shot 11's cam telemetry (cam.yaw 0.473, `landmarkInView` false) contradicts its own settled-hub render (cabin + board-back + vehicle pool plainly in frame) — the in-motion state-read/snap race; P2.2/P2.5 must settle-wait or same-frame the read+snap before trusting cam fields on moving frames (no P1 verify-by rests on shot 11; every settled frame's telemetry is pixel-corroborated). Everything else seen stays owned downstream (zoom/orbit inert, `cam.dist` 4.854 identical across 12/13/14 → P2.1/P2.2; no visible driver + `clip`=idle while `inVehicle` → P2.4; 16-word toast → P3.3; pause/board link sizes + avatar-card CTA below fold → P3.1/P3.2; text-only mission cards → P4.4; scrim → P4.7; title subtitle flow + sparse backdrop → P4.1/P4.5). **LOOP DISPATCH, to Floris: TEN gate sittings, NINE identical verdicts on an unchanged tree. A gate sitting cannot perform P1.8 — it is builder work (judges change no game code), and every further gate sitting on this tree will return this same verdict. Dispatch the Opus builder (or hand-fix) at P1.8 — reduce cloud-shadow opacity and/or feather the edge so ground detail stays readable inside a passing shadow — then ONE fresh gate sitting can close Phase 1. Sitting eight's policy question stands if the stall guard defers P1.8 too.**
- **GATE-P1 TICKED — eleventh Fable sitting (fresh 04:07–04:08 supervisor capture, 25 shots, all `ok:true`, every frame eyeballed; re-verified `git diff 447f5d3..HEAD -- app/src app/e2e-capture` EMPTY + clean worktree, so these pixels render the code ten prior sittings judged).** My own rulings: **P1.1–P1.3 genuinely fixed** (`avatar.height` 1.692–1.710 on every world frame; `cam.dist` 4.854 ≥3, idle triple stable to ~1e-7 with frames 03/04 byte-identical by pixelHash; world entry composes ground + horizon + whole ranger with cabin left, board FACE + papers right, conifer line ahead, `landmarkInView:true`, drawCalls 51; first stride walks AT the board). **P1.4/P1.5 asserts green** (`groundSpeed` 1.8000 on all five walk frames while `clip`=walk vs jeep 4.24; gait fully judgeable; F-12 band is gone — darkening is world-anchored, walk-1/2 bright with object shadows only; F-33 crisp everywhere, board papers readable at distance) — both stay **"implemented, awaiting Floris demo"**, not upgraded. **Nothing re-opened, nothing new to triage** (title backdrop/subtitle/zoom/orbit/driver/chips/scrim/cards all owned by Phase 2–4 boxes). **The three DEFERRED boxes re-proven still broken in fresh pixels:** P1.8 walk-4/5 left half = flat unreadable murk behind a HARD diagonal edge, drive-1 bottom slab / drive-2 mid haze / drive-4 center-right mass; P1.7 piglet = featureless pale egg beside rock-lump "gras" in 24-mission-3d; P1.6 board centered this capture but occluded by the ranger head — reframe still luck-of-the-capture, no board-in-frustum field exists. **Why the gate ticks anyway:** the GATE box charge (re-judge core + triage INTO the ledger) is fully met; DEFERRED boxes are off the loop path by the ledger rule + the supervisor stall-guard decision ("DEFERRING and moving on (not a halt)", commits b76cc5f/b63335b/69f0719); GATE-P0 precedent (ticked over deferred P0.2); and an unticked gate with zero re-opened boxes dispatches nothing but a twelfth identical judge sitting. **This tick closes Phase 1 LOOP-scope ONLY — it does NOT call P1.6/P1.7/P1.8 fixed; they stay `[ ]` in DEFERRED.md, owned by Floris.** **Root cause for Floris (why they parked without one builder attempt):** their box text contains "(GATE-P1 triage …)" and the dispatcher pattern-matches "GATE" in box text as a JUDGE sitting — each box burned 3 judge sittings (judges change no code) and auto-parked. To un-park: remove the DEFERRED tag AND reword "GATE-P1 triage" to e.g. "Phase-1 triage" so Opus actually gets dispatched; future triage boxes must never contain the literal string "GATE".
- **P2.2 (F-17 · laptop drag-orbit + click-vs-drag seam) — GRADE: ORBIT half GREEN, CLICK-WALK half FAILS on the fresh 05:02–05:04 laptop set (26 shots, all ok:true, coherent — not stale); box left OPEN, NOT ticked.** ORBIT ✓ (assert + pixels agree, no F-18 lie): the 216 px right-drag swung the REAL render `cam.yaw` π→−2.064 (≈ +1.08 rad = normalize(π + `cam.orbit.yaw` 1.0799)) while `pos` held byte-identical (z −11.19654) and FOV stayed 55; `orbit.yaw`=1.0799≠0; `pixelHash` changed (render genuinely moved) and 14-camera-orbit.png shows a real ~62° orbit around the STATIONARY ranger (cabin swings centre→far-right edge, new conifers revealed left) — the Run-A teleport (0.93 m pos move) + 180°-flip-into-void is gone, and the seam code (onPointerDown/Move/Up → `walkToPointer` only when `!dragMoved`; drag past 6 px orbits the lens only) is correct. CLICK-WALK ✗ (the seam's other, REQUIRED half is NOT demonstrated): 15-camera-click-walk `pos` is byte-identical to 14-camera-orbit (z −11.19654), `clip`=idle, `groundSpeed`=0, tracker holds "16 m", and the two PNGs are pixel-identical — the clean click did NOT walk. Root cause = HARNESS, not the fix: the probe clicks at `box.y+height*0.32` (32 % from top) AFTER the zoom-out (dist 9) + orbit left the walk cam nearly level (pitch −7.8°), so that ray goes ABOVE the horizon and `walkToPointer`'s ground raycast misses → no target set → no step (frozen `@smoke` tap-to-walk still walks ≥2 m at the default cam, so tap-to-walk itself is intact — this is a probe-aim bug). FIX next iteration (harness only, `app/e2e-capture/**`; do NOT touch the correct World.ts seam): make the camera-click-walk probe click a point that rays the GROUND — click lower (~55–70 % down), and/or reset zoom+orbit before the click, or tap a visible marker/board — so `pos` provably shifts and BOTH sides of the §3 tap-to-walk seam are asserted. The finding's verify-by + §3 both mandate asserting both sides, so orbit-only green cannot close P2.2. (P2.2 is +demo: even once the click-walk assert goes green it ticks as "implemented — awaiting Floris demo", drag feel/damping is Floris's on-device call.)
- **P2.3 (F-30 · vehicle camera re-anchors on boarding) — GRADE GREEN (shot + assert), ticked `[x]` with the +demo suffix on the fresh 05:40–05:42 laptop set (26 shots, all `ok:true`, coherent). Graded the FRESH drive frames ONLY (`18-jeep-in`/`19-jeep-drive-1`…`22-jeep-drive-4`, mtime 05:42, the exact files the 05:42:49 annotations point to); the `16-jeep-near`…`21-jeep-drive-4` frames at mtime 04:34 are STALE leftovers from the prior P2.2 capture (fresh set renumbered +1 after inserting `16-pause-hub`) — NOT graded.** SHOT ✓: the F-30 defect (6/8 drive frames edge-to-edge murk, camera stuck at/inside the standing avatar, jeep only ever seen 15–20 m from outside) is gone — all four fresh drive frames + the boarding frame show the jeep from a rear/3-quarter boom (~10 m) with clear ground + horizon (`22-jeep-drive-4` the cleanest: jeep centre-frame from behind, cabin left, board top-right, tree line + horizon); `18-jeep-in` shows the boarding CUT re-anchored onto the jeep (green step-in ring, speed 0). ASSERT ✓ (pixels + hook agree, no F-18 lie): `cam.target`="vehicle" and `veh.inVehicle`=true on every in-vehicle frame; `cam.dist` 9.73–10.38 = the intentional `camOffsetVehicle (0,4.5,9)` geometric boom (~10.06 m) minus F-05 spherecast push-in (`JEEP_COLLIDE`+near clamp, World.ts ~2960), bounded not overshoot; `cam.target` derived from `inVehicle` (World.ts:1440). Non-trivial pixel-diff: the four drive `pixelHash`es are all distinct (`1fd3f6…`/`b11d20…`/`f09170…`/`c6dfc9…`) and jeep `pos` moves each frame ((11.8,12.2)→(6.7,16.5)→(8.7,21.3)→(13.8,21.8), `speed` 4.24). The fix (re-anchor ranger to `jeepPos` on board + `followYaw`=jeepHeading, `placeCamera` CUT on both enter/exit for everyone, jeep boom gets F-05 clearance) is real and coherent. NOT P2.3's charge and left for the next boxes: `clip`="idle"/`groundSpeed`=0 while seated is F-31/P2.4 (ranger boards, sit/none pose); the dark shadow slab under the jeep is DEFERRED P1.8. `npm run build` + frozen `@smoke` GREEN (tick gate). +demo: drive feel/comfort stays Floris's on-device call — box carries "implemented, awaiting Floris demo (NOT fixed)", not "fixed".
- **GATE-P2 TICKED — Fable re-judge (fresh 06:44–06:46 supervisor capture, 24 shots, all `ok:true`; graded ONLY the frames the 06:46 annotations' `file:` fields name — the 04:34/06:05 leftovers on disk are stale prior-numbering, per the GATE-P0 caveat). ALL FIVE P2 boxes AGREED from my own eyeballing + hook fields, none re-opened; the four +demo boxes NOT upgraded (they stay "implemented, awaiting Floris demo").** F-16 ✓: `cam.zoom.dist` rides BOTH clamps (zoom-in 1.6139 = min, zoom-out 9.5 = max), FOV 55 in both, and the 12/13 pair differs dramatically in pixels (close over-the-shoulder at `heightFrac` 0.55 vs wide hub vista at 0.18, cabin + board + vehicle pool + horizon composed) with no mesh clipping at the min clamp. F-17 ✓ BOTH seam halves this time (the 05:0x sitting's failed click probe is fixed): drag → real `cam.yaw` π→−2.064 (`orbit.yaw` 1.0799) while `pos` holds byte-identical at (0, −11.014919999957081) and shot 14 renders a true ~62° orbit around the stationary ranger (cabin swings centre→right edge; no teleport, no 180° flip, no boot-wedge); clean click → `pos` moves to (−0.159, −11.100) and the tracker bearing flips naar-links→recht-vooruit — tap-to-walk provably alive. F-30 ✓: every in-vehicle frame has `cam.target`="vehicle", `cam.dist` 9.73–10.60, and all four drive frames + the boarding cut show the jeep from behind/above with ground + horizon + landmarks (signpost, board, NPC, heli); yaw-follow damped not locked (cam.yaw lags heading+π by 0.04–0.22 rad in-turn, 0 at rest); all drive `pixelHash`es distinct. F-31 ✓ — the phase's ONLY non-demo box, genuinely FIXED via the ledger-blessed hidden-driver fallback: `driverHidden`=true + `clip`="sit" (never idle/walk) on every in-vehicle frame, the seat reads empty in pixels AND no standing ranger is left anywhere in any vehicle frame (the Run-A giant-at-the-boarding-spot is gone); the post-jeep board scene shows the avatar back at 1.698 m. F-32 ✓ asserts (the exact control conditions the finding demanded): straight pair `headingUnwrapped` 4.0241730409285275 → IDENTICAL 3 s later (drift exactly 0) while `pos` moved ~11.0 m dead straight past the heli — the doughnut is dead; turn burst monotonic 0→0.449→1.635→2.366→3.263 rad at ~0.62 rad/s implied (inside the 0.6–0.9 comfort cap, corroborated by the wide calm arc in pixels); top speed 4.24 turning / 5.53 straight vs foot 1.8 — jeep clearly faster; consecutive drive frames differ massively. Known-and-owned, NOT re-triaged here: P1.8's cloud murk still slabs the drive/straight frames (its DEFERRED box), iPad touch steering stays demo-gated (P0.2). Tick via `ranger-run.mjs tick` — `npm run build` + frozen `@smoke` GREEN. Phase 2 closes; next is P3.1.
- **GATE-P3 — Fable re-judge (fresh 09:08–09:11 supervisor capture, 31 shots, all `ok:true`; graded ONLY the frames the 09:11 annotations' `file:` fields name — the 04:34/06:05/07:14 leftovers on disk are stale prior-numbering, per the GATE-P0/P2 caveat). VERDICT: P3.2 AGREED ticked, P3.3's laptop criteria MET (stays "implemented, awaiting Floris demo" — NOT upgraded), P3.1 RE-OPENED on exactly ONE control, gate left UNTICKED.** P3.2 ✓ from my own eyeballing + hook: the hub now shows five real chips + "Naar het hoofdmenu" (`.ph-hoofdmenu` 232×56, house icon, 3 words, calm) above the amber resume (294×56); the round-trip lands `screen`="title" with NO password re-ask (17-title-return) and re-entry restores the same avatar + progress with the one-time-hint flags intact (18-title-return-world: avatar.height 1.6999, walkSeen/tapSeen still true, no onboarding re-shown); Pauze stays visible at 96×56 while `missionView`="3d" (29, `.mission-pause`); stop returns `screen`="world" calmly (30-mission-stopped — slight transitional vignette, nothing scary); the board exit is a real bordered chip, `.mb-back` 245×56, visibly button-shaped (28). P3.3 laptop half ✓: exactly ONE hint at entry ("Loop met de pijltjes." — 4 words, keyboard glyph, persists through the 3 s idle pair per hint.active="walk"), fades after the first walk (walkSeen=true at walk-1), then the tap hint fires as a short transient ("Tik op een dier om te spelen." — 7 words; the 16-word toast is DEAD), steady state is hint-free (11: hint.active=null) with a 56×56 "?" help chip, and the tracker now sits top-left UNDER Pauze (shared code — out of the iPad joystick quadrant; the ipad ∅-intersection assert + read-aloud stay demo-gated). P3.1 ✗ on ONE named control: Begin 116×56 ✓, avatar CTA fully on-screen without scrolling (y 638–694 in 800, sticky footer) ✓, swatches 72×88 + 56×56 chips ✓, name chips ≥56 ✓, Pauze ✓, board-exit ✓, hub links LOOK tappable (outlined pictogram chips) ✓ — but the **prompt-speaker** got only the invisible hit area: `.zoeken-speak` reports 56×56 while the PIXELS show a bare ~20 px speaker glyph floating on the prompt bar, NO visible circle/chip (Run A measured a ~46 px visible circle — the visible affordance SHRANK while the assert went green; exactly the hit-vs-look trap the F-20 A8 addendum names, and the box text puts prompt-speaker in the VISIBLE-box token list). Re-opened P3.1 with a fix-only-the-speaker note; give the speaker the same visible-chip treatment as `.mb-back`/`.explore-help` (P4.2's prompt-stacking will re-lay-out the bar later, but the speaker's visible ≥56 chip is THIS box's deliverable). Datum for P4.1, no box opened (F-03 is P4.1's charge): there are TWO title-card implementations — boot title (01, `.btn-start`, Begin at y 652) wraps the subtitle mid-sentence ("…Kies een / missie…"), while the return title (17, `.ra-title-begin`, Begin at y 470) already renders clean per-sentence lines — P4.1 must fix BOTH paths or unify them. Also seen, already owned elsewhere, NOT re-triaged: mission prompt still two sentences in two sizes on one line (P4.2); mission cards text-only (P4.4); board-open backdrop scrim (P4.7); the 29-mission-3d "gras" lumps render greener than the P1.7-era captures but still read as faceted rocks with no piglet visible in frame (P1.7 stays DEFERRED, Floris-owned).
- **GATE-P3 second Fable sitting (fresh 09:23–09:25 supervisor capture, 31 shots, all `ok:true`; confirm-and-hold — HEAD is still P3.3's commit 87db65d and the working tree carries no `app/` change, so these pixels render the exact code the first GATE-P3 sitting judged) — VERDICT UNCHANGED: P3.2 re-affirmed ticked and P3.3's laptop half re-affirmed "implemented, awaiting Floris demo" from my own frames + crops (NOT upgraded), P3.1 RE-CONFIRMED broken on the same ONE control, nothing new re-opened, gate LEFT UNTICKED.** P3.1 ✗ re-proven in MY 4× sharp crop of 29-mission-3d: `.zoeken-speak` still asserts 56×56 at (1018,12) while the pixels show a bare ~20 px dark speaker glyph floating directly on the prompt bar — NO visible circle/chip around it (F-25's own fix text mandates "the visible circle should also grow — it is the affordance"; F-20's A8 addendum: make it LOOK ≥56, not just hit ≥56). Every OTHER P3.1 control re-verified good from my own crops: Begin a real amber chip 116×56 (01); `.av-klaar` CTA a sticky amber pill at y 638–694, fully on-fold without scrolling (02); swatches 72×88 with a clear selected ring; name chips real outlined pills; Pauze a visible white pill 96×56 in-world (11) AND in-mission (29); `.mb-back` a real bordered pill "Terug naar de open plek" 245×56 (28); hub links five outlined pictogram chips ≥261×56 (16); help "?" a true white circle 56×56 (11). P3.2 ✓ re-verified: `.ph-hoofdmenu` 232×56 house-icon pill → 17-title-return lands the TITLE card with NO gate re-ask → 18-title-return-world restores the same ranger + hub with walkSeen/tapSeen intact and no onboarding re-shown; Pauze visible while `missionView`="3d"; 30-mission-stopped returns to the calm world hub (mild transition vignette only, never-scary holds). P3.3 laptop ✓ re-verified: exactly ONE hint at entry ("Loop met de pijltjes." — 4 words, keyboard glyph, 03), persists the idle pair (hint.active="walk" on 04/05), fades after walk-1 into the 7-word tap transient ("Tik op een dier om te spelen."), steady state hint-free (11, hint.active=null) with the 56×56 help chip; tracker sits top-left UNDER Pauze, out of the iPad joystick quadrant in shared code (ipad ∅-assert + read-aloud stay demo-gated). LOOP DISPATCH, to Floris (the GATE-P1 lesson — sittings 2–10 burned on an unchanged tree): the first unchecked non-DEFERRED box is the WORK box P3.1 — builder work no judge may perform. Do not spawn another gate sitting; dispatch the Opus builder at P3.1 (give `.zoeken-speak` the same visible-chip treatment as `.mb-back`/`.explore-help`), then ONE fresh Fable sitting can close Phase 3.
- **GATE-P3 third Fable sitting (fresh 09:35–09:38 supervisor capture, 31 shots, all `ok:true`; confirm-and-hold — HEAD still 87db65d and zero `app/` working-tree change, so these pixels render the exact code both prior sittings judged) — VERDICT UNCHANGED: P3.1 RE-CONFIRMED broken on the same ONE control, P3.2 re-affirmed ticked, P3.3's laptop half re-affirmed "implemented, awaiting Floris demo" (NOT upgraded), nothing new re-opened, gate LEFT UNTICKED — and the judge-loop's ROOT CAUSE found and patched in ledger prose (see below).** P3.1 ✗ re-proven in MY OWN 4× crop of 29-mission-3d: `.zoeken-speak` asserts 56×56 at (1018,12) while the pixels show a bare ~20 px dark speaker glyph floating directly on the prompt bar — no visible circle/chip around it (the F-20 A8 hit-vs-look trap; every other P3.1 control re-verified good from my frames: Begin amber chip 116×56, `.av-klaar` sticky CTA on-fold at y 638–694, swatches 72×88 with clear selected ring, name chips real outlined pills, Pauze white pill 96×56 in-world AND in-mission, `.mb-back` bordered pill 245×56, hub links five outlined pictogram chips, help "?" true 56×56 circle). P3.2 ✓ from my frames: `.ph-hoofdmenu` 232×56 house-icon pill → 17-title-return lands the TITLE card with NO password re-ask (and per-sentence subtitle lines on the return path — P4.1's boot-path datum stands) → 18-title-return-world restores the same ranger + hub with walkSeen/tapSeen intact and no onboarding re-shown; `.mission-pause` 96×56 visible while `missionView`="3d"; 30-mission-stopped returns the calm world hub (soft vignette only — never-scary holds). P3.3 laptop ✓ from my frames: exactly ONE hint at entry ("Loop met de pijltjes." — 4 words, keyboard glyph) persisting the idle pair (hint.active="walk" on 04/05), fading at walk-1 into the 7-word tap transient ("Tik op een dier om te spelen."), steady state hint-free (11: hint.active=null) with the 56×56 help chip; tracker top-left UNDER Pauze (ipad ∅-assert + read-aloud stay demo-gated). **ROOT CAUSE of the mis-dispatch (sittings 29 + 30 burned as judges on an unchanged tree): `build-run-loop.sh:177` matches `case "$box" in *GATE-*)` against the WHOLE box text, and sitting 28's re-open note wrote the literal "GATE-P3" into P3.1's box text — the exact dispatcher bug the eleventh GATE-P1 sitting documented ("future triage boxes must never contain the literal string GATE"). PATCHED within judge mandate (ledger prose only, no code): P3.1's note now reads "RE-OPENED by the Phase-3 re-judge (2026-07-04)" — no "GATE-" substring — so `next_work_box` now serves P3.1 as a plain build box and the NEXT sitting dispatches the OPUS BUILDER (fix: give `.zoeken-speak` a visible ≥56 chip like `.mb-back`/`.explore-help`). The edit also resets the stall counter (it stood at 1/3 after sitting 29's no-ledger-change hold; two more mis-dispatched judges would have DEFER-parked this laptop-proven one-line defect on the read-aloud control — the wrong box to ever park for this player). After the builder lands the chip, ONE fresh Fable sitting closes Phase 3.**
- **GATE-P3 TICKED — fourth Fable sitting (fresh 10:08–10:11 supervisor capture, 31 shots, all `ok:true`, graded only the frames the 10:11 annotations' `file:` fields name; HEAD = c202fd4, the builder's P3.1 speaker redo, and the only working-tree change is the fresh annotations — so these pixels render exactly the judged code). VERDICT: P3.1 NOW AGREED FIXED on its one re-opened control, P3.2 + P3.3 re-affirmed, NO re-opens, gate ticked after mechanical build+smoke green.** The prompt-speaker verdict, measured not vibed: ring-stroke edge scans on 29-mission-3d fit a circle of radius ~27 px centred on the 56×56 hit box (chord crossings at y20/22/58/60 and x1024/26/66/68), i.e. a visible ~54–56 px disc — exactly what a 56 px CSS pill with a 2 px inset border renders; the `.explore-help` chip measures r≈28 by the same method, within a pixel of noise. The circle is the RESTING style, not a capture/focus artifact: c202fd4's only `app/src` change is `zoeken.css` giving `.zoeken-speak` a permanent `border: 2px solid var(--green-line)` via the same visible-box recipe as `.ra-chip`. Against the affordance baseline it's strictly better than Run A's borderless ~46 px disc (re-cropped `audit-evidence-baseline-run-a/laptop/22-mission-3d.png` to confirm) — the F-20 A8 "LOOK ≥56, not just hit ≥56" standard is met. Rest of the phase re-verified on MY OWN fresh frames + asserts, all green: Begin 116×56 amber button (01); avatar CTA sticky-footer on-screen y638–694, swatches 72×88 + 56×56 chips, name chips ≥56 (02); Pauze 96×56 (11, 29 while `missionView`="3d"); board exit `.mb-back` 245×56 bordered chip (28); five outlined pictogram hub chips ≥56 + `.ph-hoofdmenu` 232×56 + `.ph-back` 294×56 (16); round-trip title-return with NO gate re-ask (17) and world restored with avatar 1.6999 + hint flags intact, no onboarding re-shown (18); stop-de-missie lands calmly in `screen`="world" (30); ONE 4-word hint at entry (03), walk→tap hand-off with tracker relocated under Pauze (06), steady state hint-free with the 56×56 "?" chip (11). P3.3 stays "implemented, awaiting Floris demo" — NOT upgraded (read-aloud + iPad tracker ∩ joystick = ∅ are Floris's). Seen-but-owned-elsewhere, deliberately NOT re-triaged: boot-title still wraps mid-sentence while return-title renders per-sentence lines (P4.1's two-implementation datum re-confirmed on 01 vs 17), prompt still one long two-size line (P4.2), text-only mission cards (P4.4), no scrim behind hub/board panels (P4.7), gras-lump/piglet look (P1.7 DEFERRED), board face edge-on on approach (P1.6 DEFERRED). Phase 4 is next; the supervisor commits this gate.
- **P4.7 (F-29 · shared static scrim behind modals) — GRADE FAIL (stale/partial evidence, NOT a fix defect), box left OPEN, NOT ticked.** The supervisor's post-fix capture (started 12:24:28) CRASHED — Playwright timeout (30 min) with `browserContext.newPage: Protocol error (Target.createTarget)` at `capture.spec.ts:233` (F-21 render/newPage instability) after writing shot 27 (`board-affordance`); the harness itself printed "⚠ capture exited non-zero — grader may see partial/stale shots (will not tick)." The fresh `annotations-laptop.json` (12:54) holds only 27 shots ending at `board`, and the three OPEN modal overlays F-29 must re-verify — `28-board-open`, `29-mission-3d`, `30-mission-stopped` — are STALE 12:07 PNGs from the pre-fix P4.6-era run (mtime-confirmed) and ABSENT from the annotations. The one FRESH modal, `16-pause-hub` (12:25), DOES render the ~46% dim scrim (world visibly muted vs the fresh `04-world-idle`), so the fix looks right where it can be seen — but F-29's verify-by requires pause AND board (AND mission) overlays to show the SAME new backdrop, and the board/mission pixels on disk pre-date the fix, so the match cannot be confirmed. Blocker is the CAPTURE, not the code: needs a clean re-capture that reaches the board/mission scenes (the F-21 hang recurs on laptop too). Loop retries next iteration.
- **P4.7 (F-29 · shared static scrim behind modals) — the app/src scrim fix is DONE + complete; this retry HARDENS THE CAPTURE so the board/mission evidence can finally be shot (code only; NOT captured/ticked/committed).** Took the GRADE FAIL above at its word ("Blocker is the CAPTURE, not the code") and traced it: `capture.spec.ts:233` opened each scene group's page with a BARE `context.newPage()` sitting OUTSIDE the group's try/retry, so when the software renderer wedged and `Target.createTarget` HUNG (the 30-min timeout), the failure was uncaught and killed the whole run before the `board` group's `board-open`/`mission-3d`/`mission-stopped` could be re-shot — and `isCrash` didn't match the `Target.createTarget`/newPage signature either, so even the group crash-retry could never have fired for it. FIX (`app/e2e-capture/**` only — frozen `e2e/**` + `@smoke` + `playwright.config.ts` UNTOUCHED): a new `openGroupPage()` opens every group's page through a BOUNDED (20 s race, so a wedged target can't eat the 30-min budget) + backed-off (2→3→4 s) 4× retry, MOVED INSIDE the group try so a total failure degrades to that group's GAP (later groups still run) instead of a fatal uncaught hang; `isCrash` now also matches `Target.createTarget`/`newPage` so the group-level crash-retry fires and re-boots the page. Re-verified the app/src scrim (prior sitting's, left intact) is complete + consistent across EVERY over-world modal: pause hub + prikbord + briefing/fact/reward via `card()`→`.ra-scrim::before`, mission-board + lodge paint the SAME `var(--modal-scrim)` full-screen, in-mission pause via `.mission-pause-menu` — one `--modal-scrim: rgba(20,24,16,.46)` token, plain rgba, no backdrop-filter, instant state change (reduced-motion-safe), zero draw calls. `npm run build` (tsc+vite) + frozen `@smoke` 4/4 GREEN; capture spec compiles clean (`--list`, laptop project only). Supervisor: capture next — the hardened `newPage` should now reach the `board`/`missie` groups so the grade can confirm pause AND board show the same dim (`16-pause-hub` already does).
- **GATE-P4 — Fable re-judge (fresh 13:26–13:31 supervisor capture, 32 shots, all `ok:true`, graded ONLY the frames the annotations' `file:` fields name `01-title`…`32-boundary-rim`; the 04:34/06:05/07:14 leftovers are stale prior-numbering per the standing caveat). VERDICT: P4.1/P4.4/P4.5/P4.7 AGREED ticked, P4.2 re-affirmed "implemented, awaiting Floris demo" (NOT upgraded), P4.3 + P4.6 RE-OPENED, gate LEFT UNTICKED.** The passes, from my own crops + numbers: P4.1 ✓ 01-title renders the subtitle as two per-sentence lines (6 + 7 words); P4.4 ✓ 28-board-open shows TEN distinct pictogram chips (purple piglet, fawn, green boar, blue frog, bridge, sand bird, deer, squirrel, fox, purple snowflake) at 2× crop; P4.5 ✓ soft blob shadows confirmed under trunks at 4×, mixed cone+round species/sizes, sand path, heather clumps, board landmark, drawCalls 12; P4.7 ✓ MEASURED same scrim — mean sky band (0-300,0-50) is (146,134,119) on BOTH 16-pause-hub and 28-board-open vs (248,229,210) undimmed (≈41% dark, byte-identical means = one shared token), and world labels correctly dim UNDER the scrim; P4.2 shot ✓ (two stacked 6-word lines, one size; speaker's visible ~54–56 px ring holds from GATE-P3) but stays +demo. **P4.3 ✗ re-opened:** occlusion is genuinely solved (chips draw over the jeep roll cage/board, no geometry eats a glyph) yet EVERY world-projected chip clips its LAST glyph at the chip's own right edge — zero right padding vs ~25 px left: "Missiebord"→"Missieborc" (27-board-affordance 4× crop), "Het stille zand"→"Het stille zanc" (19-jeep-near AND 24-jeep-drive-4) — Run A's exact "…tille zanc…" string re-broken by chip width/overflow instead of depth; HUD tracker chips are fine, so it is the world-label component's box measure. **P4.6 ✗ re-opened:** rim + hint + clamp pass (tree-line rim, "Hier stopt het bos." 4 words, `atRim` true, dist 112.7 < bound 116, groundSpeed 0) but 32-boundary-rim has NO RANGER IN FRAME — bottom ⅔ featureless murk; the boom sank to `cam.y` 1.367 (vs 2.106 on walk-5) into the berm hollow and TERRAIN fully occludes the avatar while the devhook swears `avatarInView`/`onScreen` true, opacity 1, heightFrac 0.337 — the projection is frustum-math, not occlusion-aware, i.e. the same honest-telemetry trap as F-18's wrapped yaw: the assert grades a ranger the pixels do not contain. A child whose avatar vanishes at the world edge is the "silently lost" F-11 exists to prevent. Builder: (1) give the label chip symmetric padding after text measure and re-shoot 19/24/27; (2) give the rim boom ground-height clearance (or fade/lower the berm in the boom path) AND make the avatar-visibility signal occlusion-aware (e.g. short ray/spherecast camera→avatar chest) so an empty rim frame FAILS the assert; then one fresh Fable sitting closes Phase 4.
- **GRADE P4.6 (F-11) — FAIL, box left OPEN (fresh 14:30 laptop capture, `32-boundary-rim.png`).** The re-shoot reproduces the re-open defect UNCHANGED: the ranger is STILL nowhere in frame — bottom ⅔ is featureless olive murk, no avatar figure anywhere, despite the hook predicting a ~33%-screen-height ranger at frame centre (`heightFrac` 0.334, `avatarScreen` x≈0/y≈−0.10 → a ~267 px ranger would be unmistakable; nothing is there). Rim/hint/clamp still pass (conifer tree-line rim, "Hier stopt het bos." 4 words, `atRim` true, dist 112.77 < bound 116, groundSpeed 0). BOTH re-open fixes missed: (1) the boom got NO ground-height clearance — `cam.y` 1.253 m, even LOWER than the flagged 1.367, so the rising berm still fully occludes the avatar; (2) the added `cam.avatarScreen.visible` field is NOT occlusion-aware — it reports `true` on a frame with no visible ranger (`avatarInView`/`onScreen` true, opacity 1), so an empty rim frame still PASSES the assert. Pixels outrank the hook (F-18): not ticked. Loop retries — raise/pull the boom out of the hollow so the whole ranger reads at the rim, and make `visible` a real camera→chest ray/spherecast that returns false here.
- **GRADE P4.6 (F-11) — FAIL AGAIN, box left OPEN (next iteration, fresh 15:01:22 laptop capture, annotation+PNG same-stamp).** The re-fix did NOT move the needle: `32-boundary-rim.png` STILL has no ranger anywhere — bottom ⅔ is smooth olive murk, nothing at frame-centre where the hook predicts a ~33%-height figure (`heightFrac` 0.334, `avatarScreen` x≈0.002 / y≈−0.099, `onScreen`/`avatarInView` true, `avatarOpacity` 1, and the new `avatarScreen.visible` STILL reports `true`). Rim/hint/clamp remain green (conifer tree-line, "Hier stopt het bos." 4 words, `atRim` true, boundary.dist 112.79 < bound 116, `groundSpeed` 0). BOTH re-open fixes still absent: (1) boom got NO ground-height clearance — `cam.y` 1.257 m, essentially unchanged from the 14:30 fail's 1.253 and far under the ~2.1 m walk baseline, so the rising berm still fully occludes the avatar; (2) `visible` is NOT a real camera→chest ray/spherecast — it returns `true` on a frame with no visible ranger, so an empty rim frame still PASSES the assert. Pixels outrank the hook (F-18): not ticked — second consecutive identical fail on this box. Loop retries: lift/pull the boom out of the hollow so the whole ranger reads at the rim, AND make `visible` occlusion-aware so this frame fails the assert.
- **P4.6 (F-11) — ROOT CAUSE FOUND + FIX landed (code only; NOT captured/ticked/committed). The rim "ranger nowhere in frame" is NOT terrain occlusion or a sunk boom — grades 245/246 chased the wrong lever — it is a SkinnedMesh VIEW-SPACE FLOAT-PRECISION COLLAPSE far from the world origin, amplified by F-07's pathological rig scale.** Diagnosed with live Playwright probes against a fresh dev server (temp `window.__world` handle, since reverted): **(a) terrain does NOT occlude** — marching `heightAt` along the exact lens→avatar sightline clears by ≥0.2 m at every step (his ground is −1.14 m, the camera 2.4 m above it — the SAME relative geometry as the visible 11-controls-hud frame, whose projection `avatarScreen` x/y/heightFrac is near-identical). **(b) `frustumCulled=false` IS live on the `char1` SkinnedMesh and has ZERO effect** — its transformed bind-pose boundingSphere is a ~1 cm point at his body centre, so three was never culling him; the prior sitting's cull fix targeted a non-cause. **(c) The smoking gun:** TELEPORTING the ranger to the exact rim position renders him PERFECTLY at 70/85/100/108/114 m, but WALKING out he deforms into triangle shards by ~100 m and vanishes (mesh AND shadow) by ~110 m — the disappearance tracks the CAMERA being far from origin (teleport-from-near = renders while the lens eases out; camera settled far = gone), i.e. classic modelView (viewMatrix×modelMatrix) catastrophic cancellation when BOTH camera and object sit ~110 m out. The amplifier is the Meshy rig (`char1` geometry ~1 unit on a ~170-unit skeleton scaled ×0.01 in `Models.prepModel`): a tiny view-space error blows the skinned vertices apart. He renders SOLID well within ~85 m. A proper root fix (bake the ×0.01 scale into geometry+skeleton+clips so the rig is native ~1.7 m) is real skeleton surgery — too risky to land+verify in one sitting without breaking the avatar everywhere. **FIX (app/src/**):** `limits.bound` **116 → 85** — one value that pulls the whole edge in (rim tree-line, resolveMove clamp, RIM_EASE/HINT bands, camera rim clamp, boundaryState all read it), so the gentle-stop rim lands at 85 m, inside the precision-safe radius. Browser-verified end-to-end (walked to the new rim): the WHOLE solid ranger reads at the pine tree-line WITH his blob shadow, `cam.y` 2.02 (not the sunk 1.26), `avatarScreen.visible` true AND now pixel-true. This serves F-11 better too — the Run A "featureless void after 20 m" meets a forest edge much sooner; all content is inside ~50 m (ven basin), COMPACT_RADIUS 22, so the world stays spacious. Corrected the two overclaiming comments (devhook `avatarScreen.visible` + World.camState): `visible` catches TERRAIN occlusion ONLY, never this precision collapse — no geometric signal can, so PIXELS stay the court (§4). Kept the prior sitting's harmless bits (frustumCulled=false hygiene, terrainClearTo/visible terrain check, placeCamera lift + lens-cap). Updated Sandbox.ts/test.ts stale "116 m" bound docs to 85 (clearing 22 m ≪ 85, assertion still green). `npx tsc --noEmit` clean; frozen `e2e/**`+`@smoke`+`playwright.config.ts` untouched. **Supervisor: capture next.** Grade = 32-boundary-rim (WHOLE solid ranger at the tree-line rim) + `atRim` true + `boundary.dist` ≤ bound (85); note `avatarScreen.visible` is NOT the empty-frame catch here — the pixels are.
- **P4.6 (F-11) — REFINED the root-cause fix: bound 85 → 75 for a real margin (code only; NOT captured/ticked/committed).** Took the prior sitting's diagnosis as sound and independently re-confirmed it from the code: the walk lens is placed RIGIDLY at eye = `rp.y`+2.4 / boom 4.6 off the ranger's feet, looking at his sternum (World.ts ~3216–3257), so it physically cannot "sink below" him — the judge's "boom sank into a berm hollow" was a misread (`cam.y` 1.25 is just low because that terrain is low; framing is unaffected). With terrain occlusion ruled out (prior probe), fade ruled out (opacity 1) and frustum-cull off, the ONLY remaining cause is the SkinnedMesh view-space precision collapse — so pulling the rim inside the precision-safe radius is right in KIND. BUT the value had no margin: the rim-ease parks the ranger ~3 m short of the bound (the 116 capture stopped at 112.79 = bound−3.2), so **bound 85 stopped him at ~82 m — AT the edge** of the prior sitting's own "renders SOLID well within ~85 m" zone, and the capture's SOFTWARE renderer (SwiftShader) — whose float precision the dev-browser check never exercised — could still shard him there (a 5th identical fail trips the stall guard). **FIX: `limits.bound` 85 → 75**, so the ease parks him at **~72 m — genuinely "well within"** the ~85 m zone (~4× the precision tolerance of 85, i.e. tolerates a software renderer up to ~15 % worse vs ~3.5 %). Still spacious (all content ≤ ~50 m, COMPACT_RADIUS 22, so ~25 m of heide past the ven before the forest edge). One value moves the whole edge (rim tree-line, resolveMove clamp, RIM_EASE/HINT, camera lens-cap, boundaryState, terrainClearTo all read `limits.bound`); `walkToBoundary` is bound-aware (targets `-(bound+50)`, gated on `atRim`) so no harness change. Also corrected the rig-scale numbers to match Models.ts (**~85-unit skeleton ×0.02, ~94× skeleton-to-geometry conditioning**, not "~170 ×0.01") and refreshed every stale "85 m"/"116 m" bound doc to 75 (World.ts bound block + rim header + buildRim + camState `visible`, devhook `avatarScreen.visible`, Sandbox.ts/test.ts → `radius < 75`). Kept the prior sitting's harmless safety (frustumCulled=false, terrainClearTo/`visible` terrain check, placeCamera boom-wide lift + lens-cap). `npm run build` (tsc+vite) clean; Sandbox unit test 9/9 green; frozen `e2e/**`+`@smoke`+`playwright.config.ts` untouched. **Supervisor: capture next.** Grade = 32-boundary-rim (WHOLE solid ranger + blob shadow at the tree-line rim) + `atRim` true + `boundary.dist` ≤ bound (**75**); `avatarScreen.visible` catches terrain occlusion only — the empty-frame catch here is the PIXELS (§4).
- **GATE-P4 — Fable re-judge #2 (fresh 16:31–16:34 supervisor capture, 32 shots, all `ok:true`). VERDICT: both re-opened boxes AGREED FIXED, all other P4 boxes re-affirmed on the fresh set, P4.2 stays "+demo implemented" (NOT upgraded) — GATE-P4 TICKED.** P4.6 ✓ from pixels: `32-boundary-rim` shows the WHOLE solid ranger centred at the conifer tree-line rim (heightFrac 0.338 matches what the frame actually contains this time), "Hier stopt het bos." = 4 words, `atRim` true, dist 71.67 parked under bound 75 — the bound-116→75 precision fix is pixel-verified; `cam.y` 0.98 with clean framing confirms sitting 248's "cam.y is terrain-relative, not a sunk boom" correction; `avatarScreen.visible` verified in code as genuinely terrain-occlusion-aware (terrainClearTo marches the height field lens→head/chest, ≤48 steps + lensInRim + opacity + onScreen, World.ts ~1549–1560) with the honest documented caveat that no geometric signal catches precision collapse — pixels stay the court. P4.3 ✓ from pixels: "● Missiebord" renders WHOLE with symmetric padding (27-board-affordance 3× crop), the exact former-"zanc" string ends "…and"-plus-right-padding (24-jeep-drive-4 5× crop), the in-mission board chip is whole (29-mission-3d), and the watchtower's three stacked far chips all keep their rounded padded right ends; NOTE for future graders: in 19-jeep-near the POI chip slides off the RIGHT VIEWPORT edge (anchor at frame edge — dot visible, text off-frame) — that is screen-space projection at the frame boundary, NOT the re-opened glyph-overflow (do not re-litigate it as such; if edge-clamping/fade-at-edge is ever wanted it is a new polish finding, not F-22). Re-affirmed on fresh pixels: P4.1 ✓ (subtitle two per-sentence lines, 6+7 words, holds on 17-title-return too), P4.4 ✓ (distinct animal pictograms on every visible card at 3×: boar/fawn/badger/bird/antlered-deer/marten/fox…), P4.5 ✓ (mixed species+sizes, trunks, soft blob shadows, heather clumps, sand path, board landmark, drawCalls 12), P4.7 ✓ (MEASURED identical scrim — sky band mean (146,134,119) on BOTH 16-pause-hub and 28-board-open vs (247,230,214) undimmed ≈ 41% dark, one shared token; world chips dim correctly UNDER it), P4.2 shot-half ✓ (two stacked 6-word lines, one size) and stays awaiting the Floris read-aloud demo. Phase 4 closes; Phase 5 (RM + final sweep) is next.
- **GATE-P5 — final Fable re-judge, whole game (fresh 18:21–18:26 supervisor capture, 42 shots, all `ok:true`, no GAPs; graded ONLY the frames the 18:26 annotations name — the 04:34/06:05/07:14/16:3x/17:xx leftovers are stale prior-numbering per the standing caveat; every fresh frame eyeballed, byte-identical duplicates verified by md5). VERDICT: every screenshot-closable box across all phases re-judged; ALL agreed genuinely fixed EXCEPT ONE — P3.2 RE-OPENED on the board screen — plus ONE new world-look box P5.5; gate LEFT UNTICKED; the +demo boxes (P1.4, P1.5/F-10, P2.1, P2.2, P2.3, P2.5, P3.3, P4.2, P5.3) stay "implemented, awaiting Floris demo" — NOT upgraded.** The Phase-5 RM evidence is the cleanest of the run: `.rm` body class via BOTH gates (31 OS-media, 33/34 the in-game "Rustige beweging" toggle — visibly ON in Instellingen); the toggle-RM world diffs the normal still ref at ratio **0** (nothing structural); the RM idle pair 34/35 is **byte-identical** (md5 `c5c4bfec…`, secondary motion truly frozen — and 31 is byte-identical to both, so the two gates render the SAME world); the RM walk burst still animates (36 vs 35 ratio 0.49, `clip`=walk cycling 0.14→0.95→0.08 at `groundSpeed` 1.8 — keepLocomotion holds); the reframe is a true STEP function (39 dist 4.854 → 40 dist 9.626 ~1 frame after the wheel, diff 0.668; 40/41 byte-identical md5 `fcff4332…` AND cam-identical after the full damp window — a cut, zero interpolation). Cross-phase re-affirms from my own frames + hooks: P0.1 baseline intact (22+16 PNGs + annotations + README); P1.1–P1.3 (`avatar.height` 1.675–1.721 everywhere; entry composes ground + horizon + WHOLE solid ranger, cabin left + board FACE right + conifer line, `landmarkInView` true, dc 51; idle triple stable to 2e-7); P1.4/P1.5 asserts (gs 1.8000 on all five walks AND the RM walks; jeep 4.24 turning / 6.00 straight; no camera-locked band; crisp geometry); P2.1 (`zoom.dist` rides BOTH clamps 1.6139/9.5, FOV 55 both, pair hf 0.55/0.18); P2.2 (orbit yaw→−2.064 with `pos` byte-held, then the clean click moves `pos` (0,−12.362)→(−0.309,−12.527)); P2.3 (`cam.target`="vehicle", dist 9.73–10.62, 4 distinct drive hashes, jeep from behind with ground + horizon); P2.4 (`driverHidden` true + `clip`=sit on every in-vehicle frame); P2.5 (straight-pair unwrapped heading IDENTICAL to 15 decimals across 3 s at spd 5.2→6.0 — drift exactly 0; turn monotonic 0.46→1.74→2.63→3.88); P3.1 (every chip ≥56 incl. the speaker's VISIBLE ring, re-crop-confirmed); P3.3 (4-word walk hint → persists the idle pair → 7-word tap transient → steady state hint-free with the 56 px "?"; flags survive the title round-trip); P4.1 (BOTH title paths per-sentence lines); P4.3 (Missiebord + both "Het stille zand" + the in-mission chip all end WHOLE with symmetric padding — "zanc" stays dead); P4.4 (10 distinct pictograms); P4.5 (grounded varied backdrop, dc 12); P4.6 (WHOLE solid ranger at the tree-line rim, "Hier stopt het bos." 4 words, `atRim` true, dist 71.8 < 75 — the bound-75 fix holds in fresh pixels); P4.7 (sky-band means byte-EQUAL (146.0,134.3,118.9) on pause AND board vs (248.4,228.6,210.2) undimmed ≈ 41% — one token, measured). **The two failures:** (1) P3.2 — the board screen has NO VISIBLE WAY BACK: `.mb-back` asserts 245×56 but sits at y=882 in the 800 px viewport (below the fold) and the 10th card is cut mid-card — the P4.4 pictogram cards grew the grid past the fold; the assert half (round-trip, in-mission Pauze, calm stop) still passes, so the re-open is scoped to the board exit's visibility only. (2) NEW P5.5 — in `42-boundary-rim` a purple heather clump FLOATS at the ranger's head height (no ground contact at any believable depth; distant clumps render 10–15 px, this one 52 px at ~y355 where its depth's ground line is ~y450+) — a rim-band prop at the wrong height in the world-edge hero frame. **Data, no boxes:** 26-jeep-straight-2 shows an almost-empty sand frame (jeep sliver at the corner) while the geometry PROVES the boom held — cam 10.6 m behind the jeep at the SAME yaw/pitch as the perfectly-framed 25 — i.e. momentary dune-crest occlusion at top speed plus the known in-motion snap race; no verify-by rests on that frame's framing. Deferred boxes re-proven for the record: P1.7 frisling still an egg (beside now-GREENER gras lumps in 29 — closer to gras, still faceted); P1.6 luck-datum: 27 composes the board face-on with readable papers + amber CTA this capture; P1.8's cloud layer reads visibly SOFTER this burst (09/10 shadow soft-edged with ground detail readable inside — Floris's hand-fix may be lighter than the box text expects). Shot-11's cam telemetry race footnote persists (yaw 0.514 / `landmarkInView` false vs settled-hub pixels — substrate, owned, no box rests on it). Builder: land P3.2 (board exit visible without scroll, no chip below 56) then P5.5 (seat/cull rim props); ONE fresh Fable sitting then closes the run.
- **P5.5 GRADE FAIL (shot) — fresh 19:21 laptop capture (`42-boundary-rim.png`, mtime 19:21 = annotations 19:21; sole fresh rim frame — 32/34/39-boundary-rim are stale earlier runs). Box left OPEN, NOT ticked.** The fix did NOT land: the purple faceted heather/rock clump STILL floats at the ranger's head/ear height (crop 440,290–760,640 @2×: clump ~x520–590 y315–370, immediately left of the head; NO stem, NO base, NO shadow, empty tan terrain below it all the way down to the boots — no ground contact at any believable depth). This is the identical P5.5 defect, unchanged by the fix. The PASS half holds — the WHOLE solid ranger is in frame at the tree-line rim, "Hier stopt het bos." (4 words), `boundary.atRim` true, `dist` 71.61 < `bound` 75, `cam.avatarScreen.visible`=true/opacity 1 (P4.6 kept) — but the box needs BOTH and "every prop grounded" FAILS. The annotation can't catch it: `ok`=true and the new `boundary.scatterMax`=74.91 (<75, radial only) expose NO prop-HEIGHT field, so the hook is blind to a clump left at head-Y (F-18: pixels are the court of appeal). Redirect next sitting: actually seat rim-band props on `heightAt` (or cull any within the rim hollow / above ~0.5 m); a radius-only clamp does not fix a wrong-height prop — then re-shoot 42.
