# Run D — deferred boxes (auto-parked; Floris batch worklist)

WORK boxes the autonomous Run D loop could not close on its own — either it failed the independent Fable grade 2 sittings running, or a sitting flagged it needs Floris. Each stays parked so the run keeps moving. Work these hands-on.

- **D1.0 · **Capture harness fit for Run D: green inside its budget + honest evidence [laptop]** (from Run C P1.10/P1.15 capture legs; re-verified 2026-07-05: the "audit capture flow" test timed out at its 30-min cap at 11:27 — the `ven` group + a transient newPage protocol error — and the `40–44 game3d-*` frames shoot `/?sandbox`, NOT the mission path the player reaches, while orphan PNGs from older shot-numbering runs still sit beside fresh ones under `laptop/`). This box is harness-only (`app/e2e-capture/**`), no game code, and lands FIRST because every Phase-1 drive-burst box adds scenes to this harness: (a) bound/isolate the slow groups so the whole capture finishes green inside the budget, with a retry on the newPage protocol error; (b) shoot each of the 5 games' 3D surface on the player-reachable mission path (not the sandbox), keeping the five 2D-floor frames; (c) make capture REMOVE orphan PNGs not present in the fresh annotations. **Verify by:** `npm run capture` exits green with all groups complete; fresh set contains named player-path frames for all five games ×{3D, 2D floor} with per-frame `drawCalls` <150; every PNG under `laptop/` matches a shot in `annotations-laptop.json`.**
  - parked 2026-07-05 16:55:37 · failed the independent Fable grade 2 sittings running
  - evidence: RUN-D-LOOP.log around this time + runs/run-3-ux-polish/audit-evidence/laptop/
  - 2026-07-11 GATE-D1 audit: SUPERSEDED by ledger box **D1.4** (sharper scope off the fresh 23:10 capture — measured-runtime group budgets, best-effort settle backstops, burst-subdir prune). The autonomous loop works D1.4; no hands-on needed here unless D1.4 also fails twice. For the record: this box's (b) player-path re-point and (c) top-level orphan prune DID land and hold (the fresh set is all player-path; 32 orphans pruned) — only (a) completeness-inside-budget remains, now D1.4's.
- **D1.4 · **Capture completeness: every group lands inside its budget — zero GAP entries, burst dirs pruned [laptop]** (poort-append 2026-07-11, supersedes the parked D1.0 residue with current evidence — the fresh 23:10 audit capture ran its main flow 39.6 min and GAPped FIVE groups: `camera-attempts` ("camera boom never settled within 6000 ms", so ZERO zoom frames exist — D1.1's evidence), `jeep` (~300s, zero world jeep shots), `board` (~300s, only 26-caseboard landed), `boundary` (~360s, zero rim frames — D3.14's future evidence), `game3d-simon` (~240s — rescued only by the separate mission-cam spec); AND stale 17:0x PNGs sit beside fresh 23:5x ones inside `laptop/d12-ground-burst/` + `laptop/p15a-ven-burst/` because the orphan-prune skips subdirs — mixed-vintage evidence in one dir misleads the audit). Harness-only (`app/e2e-capture/**`): re-balance the group time budgets against the MEASURED headless runtime (the D1.3 sitting's own diagnosis: throttled rAF runs eased moves ~13× slower and walks crawl under load — budget for reality or make the scene cheaper, e.g. fresh-boot instead of long walk-backs), give every camera settle-wait the same bounded best-effort backstop `settleMissionCam` got, and extend the prune to burst subdirs (keep only PNGs the fresh burst JSONs/annotations name). **Verify by:** `npm run capture` exits green with ZERO GAP entries in the fresh annotations — camera-zoom pair + jeep + full board + boundary-rim + all five game3d groups present with their shots — and every PNG under `laptop/` INCLUDING subdirs matches a fresh annotation or burst record.**
  - parked 2026-07-11 01:31:35 · failed the independent Fable grade 2 sittings running
  - evidence: RUN-D-LOOP.log around this time + runs/run-3-ux-polish/audit-evidence/laptop/
  - 2026-07-11 GATE-D1 audit #2: the substance verifiably LANDED in the fresh 02:53 capture — group budgets rebalanced (every group except `jeep` comfortably inside: camera 100 s/240, board 87 s/420, boundary 84 s/510, all five game3d 49–63 s, ven 66 s/510), 9 orphan PNGs pruned, burst subdirs all-fresh (no mixed-vintage evidence). The residue is ONE number: the `jeep` group at 421 s vs its 420 s budget (zero world jeep frames). Ledger box **D1.6** carries that residue in the autonomous loop; no hands-on needed here unless D1.6 also fails twice.
  - 2026-07-11 GATE-D1 audit #3: the jeep residue is NOT a budget number after all — the 720 s raise GAPped too (721 s, zero jeep shots, third capture running): the walk-to-jeep poll-correct-hold loop DIVERGES under full-capture load and orbits the `near` radius, consuming any budget (the same loop family failed the heli spec's walk-to-pad this run). D1.6 re-opened with the convergence fix (pulsed-key or F-17 click-walk approach steering, heli spec included, burst JSONs wiped at spec start); still autonomous-loop work, no hands-on needed unless D1.6 fails twice more.
  - 2026-07-11 GATE-D1 audit #4: HALF landed — the pulsed walk fixed the JEEP group for real (complete at 367 s/540, all eight world shots, first complete jeep group in four captures; that half is frozen). But the heli spec failed a fourth capture (the pulse never converged ~33 m on the isolated page) and the pulse's stale-near acceptance NEWLY GAPped the board pair (affordance snapped 2.87 m out, near=false at snap). D1.6 re-opened once more, now directed to the F-17 click-walk branch + confirmed-near + step-flush-on-failure. This is its second consecutive audit fail — if the NEXT capture fails the same legs again, park it here and flag NEEDS-FLORIS per the two-strikes rule.

- **D1.6 · The jeep capture group + heli-enter + walkTo-convergence [laptop]**
  - parked 2026-07-11 08:5x · door Floris: fase-1 bleef rondcirkelen op de capture-pijplijn; jeep-helft is bewezen + bevroren, de rest (heli-enter convergentie, walkTo stale-near, mission-scene evidence) is tooling — opzij om door te stoten naar fase 2 (Alvah). Los dit later hands-on op als de evidence-kwaliteit het vraagt.
- **GATE-D1 · Fable re-judge — is the game mechanically TRUE? [shelved]**
  - parked 2026-07-11 08:5x · door Floris: de speelbaarheid (P1.5a/b/c, D1.2, D1.3) is gebouwd + eerder ge-audit; de openstaande heropeningen waren capture-tooling (nu geparkeerd). Gate opzijgezet i.p.v. formeel gesloten. Later evt. opnieuw auditen.

- **CI-BOOT · World init sneller onder software-GL (SwiftShader) [beide]**
  - parked 2026-07-12 · door Floris (deploy-fix hand-off, keuze "beide"): de CI-smoke faalde vanaf ~2026-07-05 doordat de volledige journey (boot → avatar → world-init → shots) op GitHub's 2-core GPU-loze runner nét over het 30 s default-testbudget kruipt — de wereld bereikt 'world' wél, het budget verloopt tijdens de slot-asserts/screenshots. Interim-fix geland: CI-testbudget 120 s in `app/playwright.config.ts` (elke frozen assert + per-stap timeout onveranderd). DEZE box is het structurele deel: de world-init onder software-rendering aantoonbaar versnellen/verlichten (Run D voegde three-mesh-bvh, ground-snap raycasts, occluder-fades en zwaardere scènes toe) zodat de journey ook op de CI-runner ruim binnen 30 s landt en het budget terug kan naar de default. **Verify by:** CI-run van de smoke toont journey-test <30 s wall-clock op de ubuntu-runner; daarna `timeout` in playwright.config.ts terug naar default in dezelfde commit.
